# PC-BAL-002: 檔案合併遺漏子功能，但檔頭仍宣稱完整承接

## 症狀

兩個模組合併為一時，部分子功能未被移植，但合併後檔案的檔頭 docstring 仍宣稱「合併自 X」而未標註遺漏項。原檔通常同時被解除註冊或標記為 superseded，於是：

- 該子功能在系統中完全失效——原檔有實作但不執行，後繼者會執行但無實作
- 失效不產生錯誤，只是某個判斷分支永遠不觸發，症狀是「該被抑制的沒被抑制」「該被分類的沒被分類」這類沉默劣化
- 後續維護者讀到檔頭的「合併自 X」宣稱，會將原檔判定為 dead code 而刪除，永久移除該子功能的唯一實作

## 根因

合併作業以「主流程可運作」為完成判準，未逐一盤點原檔的公開函式與常數。主流程通常由最常觸發的路徑構成，邊緣判斷（抑制條件、分類邏輯、特例處理）不在該路徑上，測試若只覆蓋主流程也不會紅燈。

檔頭宣稱是人工撰寫的自然語言，不受任何檢查約束。「合併自 X」在合併當下是意圖陳述，完成後未被校正為事實陳述，於是意圖與實際的落差被固化為文件。

**generic 化**：跨領域可重現於任何「多來源合併為單一實作」的作業——模組合併、服務整併、設定檔統一、函式庫升級時的相容層改寫。共同結構是「合併後的產物自述其來源，但無機制驗證該自述」。

## 案例

flutter_balance 專案 0.0.1-W1-003 與 0.0.1-W1-005。

`cli-error-feedback-hook.py:12` 檔頭載明「合併自 `skill-cli-error-feedback-hook.py`（ticket/skill CLI 錯誤偵測）」。實際比對函式清單：

| 子功能 | 原檔 | 後繼者（合併宣稱完成後） |
|--------|------|------------------------|
| `is_skill_cli_command` | 有 | 有 |
| `is_excluded_error` | 有 | 有 |
| `detect_skill_error_type` | 有 | 有 |
| `check_skill_cli_error` | 有 | 有 |
| `is_envelope_output` + `ENVELOPE_VERSION_MARKER`（W17-008.5.5） | 有（line 97/220/405） | 無 |
| `classify_error` / `detect_system_gap`（W3-073） | 有 | 無 |

四個主流程函式完整移植，兩組邊緣判斷遺漏。行為實測：以含 `__error_envelope_v1__` 標記與不含標記的兩組 payload 餵入後繼者，`additionalContext` 皆為 532 字元，證實抑制判斷不存在。

原檔未在 settings.json 註冊，故該功能在框架中處於雙重失效狀態。0.0.1-W1-003 原規劃將原檔作為 superseded dead code 刪除，刪除前的函式比對才揭露遺漏，據此撤回刪除並建票移植（0.0.1-W1-005 完成移植後刪除，`classify_error` 組另建 0.0.1-W1-008 決策）。

遺漏兩處而非一處，是判定其為系統性而非單點疏漏的依據。

## 案例二：比對通過的對照組，以及介面比對的失效（2026-07-27）

flutter_balance 專案 0.2.1-W3-038。

`post-test-hook.py:9-12` 檔頭宣稱「合併以下 2 個 Hook：1. test-timeout-post.py — 測試超時監控」，原檔 `test-timeout-post.py` 未於 settings.json 註冊，形態與案例一完全相同。依本 pattern 防護執行刪除前比對，結果為**吸收完整**，刪除放行（處置票 0.2.1-W3-064）。

這是本 pattern 首次記錄到「比對通過」的結果。案例一只有攔截結果，單一方向的結果無法證明該檢查是判別器而非否決器——兩種結果都出現過，才確立它會依事實分流。

**介面比對在本案例失效**：防護原文建議「一次 `grep -c "def "` 對照」，但本案兩檔的函式名零重疊——

| | 原檔 | 繼任者 |
|---|------|--------|
| 公開函式 | `main()` | `check_test_timeout(input_data, tool_input, logger)` |
| 介面比對結論 | \- | 零重疊，會誤判為「完全未吸收」 |

合併吸收經常伴隨重新命名（獨立 hook 的 `main()` 降級為子邏輯函式），函式名是最不穩定的比對錨點。實際有效的比對維度是**行為定義值**：

| 比對維度 | 原檔 | 繼任者 | 結論 |
|---------|------|--------|------|
| 閾值常數 | 300 / 900 / 1800 | 300 / 900 / 1800 | 全等 |
| 輸出訊息字串（4 段） | :84,87,90,93 | :217,220,223,226 | 逐字相同 |
| 讀寫檔路徑 | `test-monitor.json`、`test-duration-history.jsonl` | 同 | 全等 |
| 外部副作用 | `pkill -f flutter_tester` | 同（多 encoding 參數） | 等價 |
| 錯誤處理 | 裸 `open` | `try/except OSError` + logger | 繼任者更嚴謹 |

行為定義值在重新命名中不變，因為改動它們就是改動行為。**繼任者比原檔更嚴謹是吸收完整的正向訊號**——它顯示合併時有人讀懂了這段邏輯並改進，而非機械複製。

## 防護

防護目標是使「合併不完整」在合併當下就暴露，而非在數月後的刪除作業中偶然被發現。

**合併完成判準**：主流程可運作不等於合併完成。合併前列出原檔的全部公開函式與模組級常數，逐一標註「已移植 / 刻意棄用（附理由）」，兩者以外的狀態不得視為完成。

| 訊號 | 判定 | 動作 |
|------|------|------|
| 檔頭宣稱「合併自 X」但無遺漏項標註 | 宣稱未經驗證 | 比對原檔公開介面後才採信 |
| 原檔存在但未註冊 / 未被 import | 該檔功能全面失效 | 刪除前必須比對介面，不得僅憑檔頭宣稱判定 dead code |
| 合併後測試全綠 | 不構成完整性證據 | 測試覆蓋主流程，邊緣判斷需另行確認 |

**刪除前的強制比對**：判定某檔為 superseded 而準備刪除時，比對其實際行為與宣稱的後繼者。此檢查是遺漏的最後攔截點——一旦刪除，唯一實作消失。

比對維度依合併形態選擇（案例二實證：選錯維度會得出相反結論）：

| 合併形態 | 有效比對維度 | 失效維度 |
|---------|------------|---------|
| 函式名保留（案例一） | 公開函式與模組級常數清單 | \- |
| 函式降級為子邏輯並重新命名（案例二） | 行為定義值：閾值常數、輸出字串、讀寫路徑、外部副作用 | 函式名（零重疊會誤判為未吸收） |

無法確認屬何種形態時，直接比對行為定義值——它在兩種形態下都有效。「繼任者的實作比原檔嚴謹」（多錯誤處理、多日誌）是吸收完整的正向訊號；「繼任者只有名稱相符但行為值不同」則須逐項追查差異是刻意或遺漏。

**檔頭校正**：合併完成後將檔頭的意圖陳述改為事實陳述，明列已承接與未承接項。未承接項附理由與追蹤 ticket ID，使後續維護者不需重新調查。

## 相關

- 同源「宣稱與實際不符」：PC-BAL-001（驗證端清單過期使 canonical 結構被判違規）。兩者差別在 PC-BAL-001 是清單漏列、本 pattern 是功能漏移植，共同訊號為「檔案自述的內容不等於實際行為」，處理原則一致——以程式碼實際內容為準，不採信自述
- 測試綠燈不等於 runtime 正確：`.claude/rules/core/quality-baseline.md` 規則 1 的邊界條款（本 pattern 是其在合併作業上的具體樣態）
- 失敗案例學習原則：`.claude/rules/core/quality-baseline.md` 規則 6（撤回刪除而非硬推進，教訓建票）

---

Last Updated: 2026-07-27 | Source: flutter_balance 0.0.1-W1-003 刪除前驗證、0.0.1-W1-005 移植執行、0.2.1-W3-038 比對通過對照組與介面比對失效
