# PC-MON-002: 必填不等於有效 — CLI 必填欄位無值驗證，自由文字穿透防護

**類別**: process-compliance
**發現日期**: 2026-07-02
**相關 Ticket**: 0.3.6-W1-006（稽核 ANA）、0.3.6-W1-007（CLI 驗證修復）、0.3.6-W1-008（hook 存量掃描）
**嚴重度**: 中（防護表面存在但實質可穿透，比無防護更危險——產生已防護的錯覺）

---

## 症狀

`ticket track close` 的 `--resolved-by` 為必填參數（PC-090 C1 防護），語意是「解決此問題的 Ticket ID」，但實際接受任意字串：

- 0.4.0-W3-001 以 `--resolved-by 設計決策` 通過關閉，frontmatter 留下 `closed_by: 設計決策`
- 預設 note 模板產生「已在 設計決策 一併解決」——語法通順掩蓋了引用對象不存在的事實
- 追蹤鏈斷裂：該票宣稱被「某處」解決，但無任何可導航的 ticket ID

## 根因

**必填驗證只檢查「有沒有給值」，不檢查「值是否符合欄位語意」。** 欄位語意是 ticket ID，完整驗證應含三層：

| 層 | 檢查 | 現狀 |
|----|------|------|
| 存在性 | 參數有值 | 有（argparse required） |
| 格式 | 符合 `{version}-W{wave}-{seq}` pattern | **無** |
| 參照完整性 | 該 ticket 檔案真實存在 | **無** |

行為模式：操作者（AI agent 尤甚）在欄位語意不適用當下情境時（requirement_vanished 沒有「解決者」），傾向塞語意近似的自由文字讓命令通過，而非質疑欄位設計。防護只做第一層時，這條逃生路徑永遠敞開。

## 解決方案

0.3.6-W1-007 落地：close() 依 reason 分歧驗證——superseded_by/duplicate 的 resolved_by 必為合法且存在的 ticket ID；requirement_vanished/cancelled_by_user 允許顯式 `none` + 必填 note（消除「欄位語意不適用→塞自由文字」的設計逼迫）；note 含延後語意時 reject 引導 `ticket migrate`。

## 預防措施

設計或審查 CLI / schema 必填欄位時，逐欄自問三層清單：

1. 欄位有語意型別（ID / 路徑 / 枚舉 / 日期）嗎？有 → 必須驗格式，不能只驗非空
2. 欄位是參照（指向另一實體）嗎？是 → 必須驗存在性（dangling reference 即追蹤鏈斷裂）
3. 欄位語意在某些合法情境不適用嗎？是 → 提供顯式 `none` 通道 + 替代必填欄位，堵住自由文字逃生路徑

**檢驗法**：對每個必填欄位問「塞『設計決策』四個字能通過嗎？」能通過 = 防護是裝飾。

## 關聯

- [[PC-BAL-010]]（驗證管道對待驗現象不敏感）——上位家族模式；本模式屬其恆真側成員（必填檢查對「值是否有效」不敏感）
- PC-090（deferred-close 反模式）——本 pattern 補其 C1 防護的值驗證缺口
- [[PC-MON-001]]（防護落地於可繞過執行點）——姊妹模式：MON-001 是防護「位置」錯誤，MON-002 是防護「深度」不足
