# Phase 2：測試設計指引

## Phase 目標

根據 Phase 1 的功能規格和行為場景，設計完整的測試案例，確保測試覆蓋所有業務行為。

**核心問題**：「什麼樣的測試能充分驗證這個功能的正確性？」

---

## 產出（Layer 1）

Phase 2 完成後，必須產出：

- [ ] 測試案例清單（涵蓋正常/異常/邊界）
- [ ] GWT（Given-When-Then）格式的測試規格
- [ ] 測試檔案結構規劃（位置、命名）
- [ ] 測試策略說明（測試金字塔層次）
- [ ] 非功能性需求測試對照表（Q10/Q_new4 的答案）
- [ ] 測試資料純淨性確認（Q11 的答案）

---

## 讀者分層指引

本文件內容豐富，不同角色只需閱讀相關章節。請依角色選擇閱讀路徑：

### 測試設計者

負責「設計什麼樣的測試」。建議閱讀順序：

1. **產出（Layer 1）** — 確認 Phase 2 必須交付的六項產出
2. **Decision Questions** — Phase 2 設計時必答的判斷問題（Q7-Q11）
3. **測試策略設計** — 選擇測試金字塔的層次分配
4. **BDD / Given-When-Then 設計** — 場景設計原則、行為鏈式設計
5. **測試案例設計** — GWT 格式、邊界條件識別
6. **測試規格書（Phase 2 產出文件）** — 最終交付的文件模板
7. **轉換條件** — 確認退出 Phase 2 的條件

可跳過：測試檔案結構規劃（實作者決定）、測試實戰教訓（實作階段參考）、Fake/Mock 設計原則（實作細節）

### 實作者

負責「把測試案例變成程式碼」。建議閱讀順序：

1. **測試檔案結構規劃** — 檔案命名和目錄對應規則
2. **測試實戰教訓** — 六個坑的完整案例（最重要，避免重蹈覆轍）
3. **Fake/Mock 設計原則** — 設計測試替身的確認項目
4. **從呼叫路徑出發的測試規劃方法** — 整合測試與單元測試的分工
5. **try-catch 設計原則** — 避免錯誤被靜默吞掉
6. **檢查清單：避免「測試全過但有 Bug」** — 寫完測試後的自我檢查

可跳過：測試策略設計（設計師已決定）、BDD / GWT 設計（設計師已完成規格）、測試規格書模板（設計師的產出格式）

### 快速章節索引

| 章節 | 設計師 | 實作者 |
|------|--------|--------|
| 產出（Layer 1） | 必讀 | 參考 |
| Decision Questions | 必讀 | 參考 |
| 測試策略設計 | 必讀 | - |
| BDD / Given-When-Then 設計 | 必讀 | 參考 |
| 測試案例設計 | 必讀 | 參考 |
| 測試檔案結構規劃 | - | 必讀 |
| 測試規格書（Phase 2 產出文件） | 必讀 | - |
| 測試實戰教訓 | 參考 | 必讀 |
| Fake/Mock 設計原則 | - | 必讀 |
| 從呼叫路徑出發的測試規劃方法 | 參考 | 必讀 |
| try-catch 設計原則 | - | 必讀 |
| 檢查清單 | 參考 | 必讀 |
| 轉換條件 | 必讀 | - |

---

## Decision Questions

Phase 2 測試設計時必須回答的判斷問題。依觸發條件分組，非所有問題都需要回答——只回答觸發條件成立的組別。

### 組 1：中間步驟測試

**觸發條件**：功能涉及多步驟流程時（入口方法內部呼叫 2+ 個子方法）。

#### Q7: 測試驗證的是最終結果還是中間過程？

**行動路徑**：若只測最終結果 → 識別中間步驟清單，每個至少一個獨立測試。

Phase 2 時新建的私有方法可能還不存在（Phase 3 才建立）。因此：
- Phase 2 設計「方法契約測試」：根據規格定義的資料結構，預定義中間方法的輸入輸出
- Phase 3 實作時，新建方法必須符合 Phase 2 預定義的契約

範例：
- [v0.17.0] _buildTitleAuthorKey 是新寫的私有方法，讀取 book.author（單數）而非 book.authors（複數）。測試只驗「合併結果是否正確」，未測「key 生成函式」這個中間步驟。若 Phase 2 預定義了「key 生成的輸入輸出契約」，Phase 3 實作者就必須為此寫測試。
- [反面] getBookById(id) 內部只調 storage.get → 不需要中間步驟測試，直接測最終結果即可。

<!-- 擴充指引：Phase 4 檢討發現新案例時，在此追加。格式：[{版本號}] {案例描述} -->

#### Q8: 新建的私有方法是否被至少一個測試的斷言直接覆蓋？

透過公開入口呼叫，確認私有方法的邏輯被斷言驗證。

範例：
- [v0.17.0] _buildTitleAuthorKey 是私有方法，只被 _handleDuplicateBooks 呼叫。測試直接驗證去重結果，但 key 生成邏輯未被斷言覆蓋（即使去重結果「碰巧正確」）。
- [反面] 私有方法只是簡單委派（如 `_save(data) => storage.set(data)`），不需要額外測試。

<!-- 擴充指引：Phase 4 檢討發現新案例時，在此追加。格式：[{版本號}] {案例描述} -->

#### Q9: 如果實作讀錯了欄位名，這份測試資料會不會碰巧讓測試通過？

**行動路徑**：逐一檢查測試資料中的每個欄位，確認不存在「讀錯欄位名但結果相同」的情況。

範例：
- [v0.17.0] 測試資料同時有 author（v1 字串）和 authors（v2 陣列），實作讀取 book.author 而非 book.authors，測試碰巧通過。防護：測試資料只保留 v2 欄位 authors，完全移除 author → 測試會正確失敗。
- [反面] 測試資料的欄位名稱各自唯一（如 title, isbn, publishDate），無碰巧通過風險。

<!-- 擴充指引：Phase 4 檢討發現新案例時，在此追加。格式：[{版本號}] {案例描述} -->

---

### 組 2：非功能性測試

**觸發條件**：Phase 1 規格含非功能性需求時（日誌、可觀測性、錯誤處理路徑等）。

#### Q10: 列出規格中定義的每個 error handling 路徑，對照測試清單標記覆蓋狀態

**行動路徑**：建立對照表，未覆蓋的路徑必須補充測試案例。

範例：
- [v0.17.0] tag-storage-adapter 有 8 個 catch 區塊，規格（references/observability-rules.md）要求「catch 區塊必須有日誌」。W2 測試未驗證任何 catch 區塊是否調用 logger，導致 P1-3（零日誌可觀測性）。防護：Phase 2 增加 mock logger 驗證 call count。

<!-- 擴充指引：Phase 4 檢討發現新案例時，在此追加。格式：[{版本號}] {案例描述} -->

#### Q_new3: expect 語句驗證的是值本身還是存在性？逐一列舉確認

**行動路徑**：列出所有 expect 語句，標記「值驗證」或「存在性驗證」。存在性驗證（如 isNotNull, containsKey）需要評估是否足夠。

範例：
- [v0.17.0] `expect(result.kitchenResults.containsKey('kitchen-1'), isTrue)` 只檢查「有沒有這台印表機的結果」，不管結果是成功還是失敗。列印在 try-catch 裡失敗後回傳 false，但 key 存在，所以斷言通過。改為 `expect(result.kitchenResults['kitchen-1'], isTrue)` 後立刻暴露 Bug。（對應坑 3）

<!-- 擴充指引：Phase 4 檢討發現新案例時，在此追加。格式：[{版本號}] {案例描述} -->

#### Q_new4: Phase 1 定義的每個異常處理路徑，是否都有對應的失敗測試？列出對照表

**行動路徑**：建立「異常處理路徑 vs 失敗測試」對照表，空格代表測試盲點。

| Phase 1 異常路徑 | 對應失敗測試 | 覆蓋狀態 |
|-----------------|------------|---------|
| {路徑描述} | {測試名稱} | 已覆蓋 / 未覆蓋 |

範例：
- [v0.17.0] 規格定義「CRUD 操作失敗時應記錄 warning 日誌」，但無對應的失敗測試驗證 logger 是否被呼叫。

<!-- 擴充指引：Phase 4 檢討發現新案例時，在此追加。格式：[{版本號}] {案例描述} -->

#### 非功能性需求測試推導表

Phase 2 設計時，對照 Phase 1 的非功能性需求，推導對應測試：

| Phase 1 非功能性需求 | Phase 2 測試設計 | 驗證方式 |
|--------------------|----------------|---------|
| 異常時記錄日誌 | catch 區塊日誌測試 | Mock logger，驗證 call count 和參數 |
| 日誌含堆疊追蹤 | 日誌訊息結構測試 | 驗證日誌包含 error.stack |
| 操作逾時處理 | 逾時邊界測試 | 模擬延遲，驗證逾時行為 |
| 資料寫入原子性 | 中途失敗回滾測試 | 模擬寫入失敗，驗證狀態回滾 |
| 並發安全 | 競爭條件測試 | 模擬同時操作，驗證結果一致性 |

---

### 組 3：資料純淨性

**觸發條件**：涉及 schema 版本遷移時（v1 → v2、欄位重新命名等）。

#### Q11: 測試資料工廠是否只使用目標版本的欄位？有無殘留舊版？

**行動路徑**：檢查測試資料工廠（createBookV2、buildTestTag 等），確認不包含已棄用的舊版欄位。

範例：
- [v0.17.0] createBookV2() 工廠同時包含 author（v1）和 authors（v2）→ 殘留 v1 欄位掩蓋了 bug。修正後：createBookV2() 只包含 authors，完全移除 author。
- [反面] 測試遷移函式（migrateV1ToV2）時，輸入端應使用 v1 欄位，輸出端驗證 v2 欄位。這是正確使用，非殘留。

<!-- 擴充指引：Phase 4 檢討發現新案例時，在此追加。格式：[{版本號}] {案例描述} -->

---

## 測試策略設計

### 測試金字塔

```
              端對端測試（少量）
           /       UI 整合       \
         /   整合測試（中量）    \
       /    模組間互動 / API      \
     /  單元測試（大量）          \
   /   函式、類別、邏輯            \
```

| 層次 | 測試對象 | 數量 | 速度 |
|------|---------|------|------|
| 單元測試 | 函式、類別、業務邏輯 | 最多 | 最快 |
| 整合測試 | 模組間互動、資料層 | 中量 | 較慢 |
| 端對端測試 | 完整使用者流程 | 最少 | 最慢 |

**原則**：測試金字塔底部穩定，不要讓整合測試做單元測試的工作。

### 測試類型選擇

| 功能類型 | 建議測試層次 |
|---------|------------|
| 業務邏輯（Domain） | 單元測試（純函式，無外部依賴） |
| 應用服務（Use Case） | 單元測試（Mock 外部依賴） |
| 資料存取（Repository） | 整合測試（真實資料庫或 Mock） |
| UI 元件 | 元件測試 + 少量整合 |
| 完整功能流程 | 端對端測試（少量核心路徑） |

---

## BDD / Given-When-Then 設計

### GWT 格式

```
場景 {編號}：{業務行為描述}

Given: {系統的前置狀態}
  AND: {額外前置條件}（可選）
When: {觸發的操作}
  AND: {額外操作}（可選）
Then: {預期的可觀察結果}
  AND: {額外預期結果}（可選）
```

### 場景設計原則

| 原則 | 說明 |
|------|------|
| 每個場景獨立 | 不依賴其他場景的執行順序 |
| 業務語言 | 用業務語言描述，不用技術術語 |
| 可觀察結果 | Then 必須描述可驗證的外部行為 |
| 最小化 Given | 只設定測試所需的最少前置條件 |

### 場景類型覆蓋要求

| 類型 | 說明 | 必要性 |
|------|------|--------|
| 正常流程 | 標準成功路徑（Happy Path） | 必要 |
| 異常流程 | 輸入錯誤、業務規則拒絕 | 必要 |
| 邊界條件 | 最小值、最大值、邊界附近 | 必要 |
| 非功能性 | 日誌記錄、錯誤處理路徑、可觀測性（見 Q10/Q_new4） | 必要 |
| 並發/競爭 | 同時操作、狀態競爭（如適用） | 視情況 |
| 效能邊界 | 大量資料、超時（如有效能需求） | 視情況 |

### 行為鏈式設計

對於涉及多步驟的功能，測試應基於行為事件逐步推演：

```
A（初始狀態）→ B（操作後狀態）→ C（進一步操作後狀態）

測試 1：驗證 A → B 的轉換
測試 2：Given B 的前置狀態，驗證 B → C 的轉換
```

這樣每個測試都是獨立可執行的，同時能表達完整的業務流程。

---

## 測試案例設計

### 測試案例格式

```markdown
### 測試案例：{案例名稱}

**類型**：單元 / 整合 / 端對端
**對應場景**：場景 {編號}
**測試對象**：{被測試的函式/類別/流程}

**Given**：
- {前置條件設定}

**When**：
- {操作}

**Then**：
- {預期結果}
- {預期的副作用（如有）}

**測試資料**：
- 輸入：{具體測試資料}
- 預期輸出：{具體預期值}
```

### 邊界條件識別

對每個輸入欄位，考慮：

| 邊界類型 | 範例（字串欄位，長度 1-100） |
|---------|---------------------------|
| 最小有效值 | 長度 = 1 |
| 最大有效值 | 長度 = 100 |
| 低於最小值 | 長度 = 0（空字串）|
| 超過最大值 | 長度 = 101 |
| 特殊字元 | 包含 SQL 注入字元、Unicode |
| Null / 未提供 | 欄位為 null 或缺少 |

---

## 測試檔案結構規劃

### 命名規範

| 規則 | 說明 |
|------|------|
| 測試檔案名稱 | `{被測試模組名稱}_test.{副檔名}` |
| 測試函式名稱 | 描述被測試的行為，而非實作細節 |
| 避免 | `test1`, `testMethod`, `testHappy` 等無意義名稱 |

### 目錄結構

測試檔案位置應與被測試的原始碼對應：

```
src/
  features/
    search/
      domain/
        search_query.dart

test/
  features/
    search/
      domain/
        search_query_test.dart
```

---

## 測試規格書（Phase 2 產出文件）

```markdown
## Phase 2 測試規格書

### 功能：{功能名稱}
### 測試策略：{說明採用的測試層次和理由}

---

### 場景清單

| 場景 | 類型 | 描述 | 優先級 |
|------|------|------|--------|
| 場景 1 | 正常流程 | {描述} | 高 |
| 場景 2 | 異常流程 | {描述} | 高 |
| 場景 3 | 邊界條件 | {描述} | 中 |
| 場景 4 | 非功能性 | {描述} | 高 |

---

### 場景 1：{名稱}

Given: {前置條件}
When: {操作}
Then: {預期結果}

測試資料：
- 輸入：{值}
- 預期輸出：{值}

---

### 非功能性需求測試對照表

| Phase 1 非功能性需求 | 對應測試場景 | 驗證方式 |
|--------------------|------------|---------|
| {需求描述} | 場景 {N} | {Mock/Assert 策略} |

### 測試資料純淨性確認

| 資料工廠 | 目標版本 | 殘留舊版欄位 | 狀態 |
|---------|---------|------------|------|
| {工廠名稱} | {v2} | {無 / 列出} | 通過 / 待修正 |

---

### 測試檔案規劃

| 測試檔案 | 測試類型 | 場景覆蓋 |
|---------|---------|---------|
| {路徑} | 單元測試 | 場景 1、2、3 |
```

---

## 測試實戰教訓

完整案例見 [`../cases/printer-test-coverage-depth-failure.md`](../cases/printer-test-coverage-depth-failure.md)（印表機列印功能：28 個測試全過但上線後發現 4 個 Bug）。

核心教訓摘要：

| 坑 | 問題 | 對應 DQ |
|----|------|---------|
| 坑 1 | 手動構造結果，分派邏輯從未執行 | Q7, Q9 |
| 坑 2 | 只測子類別方法，父類別繼承方法未覆蓋 | - |
| 坑 3 | 斷言只驗存在性（`containsKey`），未驗值 | Q_new3 |
| 坑 4 | 模擬資料只觸發文字列印，表格列印分支未覆蓋 | - |
| 坑 5 | try-catch 吞掉錯誤，測試誤以為正常 | Q10 |
| 坑 6 | catch 範圍太大，程式碼 bug 和硬體故障混在一起 | - |

---

## Fake/Mock 設計原則

| | Fake（假實作） | Mock（模擬物件） |
|---|--------------|----------------|
| 適用場景 | 需要跑通完整路徑 | 只需驗證互動次數/參數 |

**設計 Fake 時的確認項目**：

- [ ] 繼承/實作的方法中，有哪些是上層呼叫者**實際會用到的**？
- [ ] 這些方法依賴哪些**內部狀態**（如 `late` 變數）？
- [ ] Fake 的初始化是否正確建立了這些內部狀態？
- [ ] Fake 回傳的資料是否足以讓下游**所有分支**都被觸發？

**常見錯誤**：

| 錯誤 | 後果 |
|------|------|
| 只覆寫子類別的方法，沒測繼承的方法 | 上層呼叫繼承方法時，內部狀態未初始化 |
| 回傳資料只覆蓋部分分支 | 某些程式碼路徑從未被執行 |
| 回傳資料缺少邊界值 | 空字串、空列表等邊界情況未測試 |
| 測試資料工廠殘留舊版欄位 | 讀錯欄位名的 bug 碰巧通過（Q11） |

---

## 從呼叫路徑出發的測試規劃方法

按「使用者操作觸發了什麼路徑」來規劃測試，而非按「這個 class 有哪些方法」來分配。按路徑規劃之後，每個測試案例都會走過完整的鏈路，中間環節的問題自然會被觸發。

### 整合測試與單元測試的分工

| | 單元測試 | 整合測試 |
|---|---------|---------|
| 測什麼 | 單一方法的輸入輸出 | 多個元件串接的結果 |
| 假設什麼 | 其他元件是正確的 | 驗證元件之間的銜接 |
| 能抓到什麼 Bug | 演算法邏輯錯誤 | 初始化遺漏、依賴缺失、介面不匹配、狀態傳遞錯誤 |

功能涉及多個元件協作時，只有單元測試是不夠的。整合測試才能抓到元件之間的銜接問題。

---

## try-catch 設計原則

- 區分「預期的執行期錯誤」和「程式碼 bug」，只攔截前者
- 定義專用的 exception 類型（如 `PrinterException`），在 IO 邊界包裝，上層只 catch 這個類型
- 資料準備、邏輯運算等步驟不要放在 try-catch 裡面，讓錯誤直接拋出

---

## 之後可以改善的方向

### 寫測試時

- 測試應從入口方法開始驅動，讓中間的邏輯實際執行，避免手動構造中間結果
- 使用模擬子類別時，確認上層實際呼叫到的繼承方法也有被測試覆蓋
- 斷言驗證值本身，而非只驗證存在性（`containsKey` → 直接檢查值）
- 設計模擬元件的回傳資料時，先確認下游有哪些分支，確認回傳資料能觸發這些分支
- 回傳資料中加入邊界值——空字串、空列表等

### 修 Bug 時

- 修完後確認有測試會實際走到修改的程式碼，否則測試通過不代表修改生效
- 沿著執行路徑往下看——之前被擋住的程式碼現在可以執行了，那些區段可能存在未發現的問題
- 如果修改讓新的資料流入第三方 library，檢查那些資料是否有 edge case

### 設計 try-catch 時

（見上方「try-catch 設計原則」章節）

---

## 檢查清單：避免「測試全過但有 Bug」

寫完測試案例後，用以下問題自我檢查：

1. 這個測試有走過真實的呼叫路徑嗎？還是只測了資料搬運？
2. 斷言是驗證「值」還是只驗證「存在」？
3. 所有依賴都有提供嗎？缺少的依賴會不會被 try-catch 吞掉？
4. 模擬子類別覆寫的方法之外，繼承的方法有被測到嗎？
5. 模擬元件回傳的資料有觸發下游的所有分支嗎？
6. 邊界值（空字串、空列表、null）有出現在測試資料中嗎？
7. try-catch 的範圍是否只包含 IO 操作？資料準備和邏輯運算是否在 try 外面？
8. 有寫反向測試（故意觸發錯誤）來確認理解了 Bug 的根因嗎？
9. 測試資料工廠是否只使用目標版本欄位？（Q11）
10. 非功能性需求（日誌、可觀測性）是否有對應測試？（Q10/Q_new4）

---

## 轉換條件

### 進入 Phase 2 的條件

- Phase 1 功能規格完整
- API 介面定義完成
- 驗收標準明確
- 行為場景（GWT）已提取

### 退出 Phase 2 的條件（進入 Phase 3a）

- [ ] 測試案例涵蓋正常/異常/邊界/非功能性情境
- [ ] 所有場景以 GWT 格式規格化
- [ ] 測試檔案結構規劃完成
- [ ] 測試策略說明完整
- [ ] 非功能性需求測試對照表已填寫
- [ ] 測試資料純淨性確認已完成
- [ ] 適用的 Decision Questions（Q7-Q11）已回答

> **框架整合**：Phase 2 由測試設計者角色執行，完成後自行提交（git commit），更新任務狀態，回報：「Phase 2 完成，測試規格書已提交」。

---

## 案例索引

| 案例 | 主要教訓 |
|------|---------|
| [測試資料與可觀測性盲點](../cases/test-data-and-observability-blind-spots.md) | 碰巧通過（Q9）、中間步驟未測（Q7）、零日誌可觀測性（Q10） |
| [印表機測試覆蓋深度不足](../cases/printer-test-coverage-depth-failure.md) | 路徑深度不足（Q7）、存在性驗證（Q_new3）、try-catch 吞錯誤（Q10） |
| [私有方法測試覆蓋缺口](../cases/private-method-test-coverage-gap.md) | 合併邏輯和快取鍵的私有方法無獨立斷言，邊界條件未覆蓋（Q8） |
| [異常路徑測試覆蓋缺口](../cases/error-path-test-coverage-gap.md) | 180 錯誤碼中 49% 生產路徑未測，引用 != 測試（Q_new4） |
| [全範圍重寫拆分策略](../cases/rewrite-scope-split-strategy.md) | 測試 Ticket 應按面向拆分（欄位/狀態/CRUD/邊界），與實作 1:1 對應 |

> 新案例追加方式：Phase 4 審查完成後，依流程改善回饋環（見 `references/phase4/rules.md`）追加。

---

## 相關文件

- .claude/references/observability-rules.md — 可觀測性規則（Q10/Q_new4 的規格來源）
- .claude/references/quality-common.md — 通用品質基線

---

**Last Updated**: 2026-04-04
**Version**: 2.1.0 - 新增案例索引章節（格式對齊 phase3/rules.md），完善相關文件引用
