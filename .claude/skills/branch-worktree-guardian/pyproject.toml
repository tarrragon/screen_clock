[project]
name = "branch-worktree-guardian"
version = "0.1.0"
description = "Create feature worktree utility"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=7.0", "pytest-mock>=3.10"]
