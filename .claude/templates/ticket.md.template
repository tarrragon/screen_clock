---
# === 識別資訊 ===
ticket_id: "${ticket_id}"
version: "${version}"
wave: ${wave}

# === 單一職責定義 ===
action: "${action}"
target: "${target}"

# === 執行資訊 ===
agent: "${agent}"

# === 5W1H 設計 ===
who: "${who}"
what: "${what}"
when: "${when}"
where: "${where}"
why: "${why}"
how: "${how}"

# === 驗收條件 ===
acceptance:
${acceptance}

# === 相關檔案 ===
files:
${files}

# === 依賴 ===
dependencies:
${dependencies}

# === 任務鏈追蹤 ===
chain:
  root: "${chain_root}"
  parent: ${chain_parent}
  depth: ${chain_depth}
  sequence: [${chain_sequence}]

# === 關聯追蹤 ===
spawned_tickets: []
source_ticket: null

# === 狀態追蹤 ===
status: "pending"
assigned: false
started_at: null
completed_at: null
---

## 決策樹路徑

### 進入點
- **層級**: ${entry_layer}
- **觸發條件**: ${trigger_condition}

### 決策節點

| 層級 | 決策問題 | 答案 | 下一步 |
|------|---------|------|--------|
${decision_nodes_table}

### 最終決策
- **決策結果**: ${final_decision}
- **決策理由**: ${rationale}

---

# 執行日誌

## 任務摘要

${task_summary}

---

## 問題分析

<!-- 待執行代理人填寫 -->

---

## 解決方案

<!-- 待執行代理人填寫 -->

---

## 測試結果

<!-- 待執行代理人填寫 -->

---

## 完成資訊

**完成時間**: (待定)
**執行代理人**: ${agent}
**Review 狀態**: 待定
