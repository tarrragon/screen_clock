---
id: 1.0.1-W1-001
title: 修正 透明遮罩黑底：MainFlutterWindow 補設 flutterViewController.backgroundColor=.clear
type: IMP
status: in_progress
version: 1.0.1
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: Phase 2 RED 驗證透明後 Phase 3b 補原生設定，Phase 4 評估
  rationale: quality-baseline-rule-5
who:
  current: parsley-flutter-developer
  history: {}
what: 修正 透明遮罩黑底：MainFlutterWindow 補設 flutterViewController.backgroundColor=.clear
when: v1.0.0 發布後 hotfix，優先於其他新功能
where:
  layer: 待定義
  files:
  - macos/Runner/MainFlutterWindow.swift
why: SPEC-001 FR-02 透明背景在本機 Flutter 3.38.10 顯示黑底，遮擋桌面內容違反遮罩時鐘核心需求；根因為原生端漏設 FlutterViewController
  透明層（Flutter 3.7+ macOS regression）
how:
  task_type: Implementation
  strategy: Phase 2 撰寫驗證透明屬性的測試；Phase 3b 於 MainFlutterWindow.swift 建立 flutterViewController
    後補 backgroundColor = .clear；手動 flutter run 驗收透明效果
acceptance:
- '[x] MainFlutterWindow.swift 在建立 flutterViewController 後設定 backgroundColor = .clear'
- '[x] flutter run -d macos 後桌面底下內容可透視（透明生效，非黑底）'
- '[x] click-through 與 always-on-top 等既有遮罩行為不受影響'
- '[x] 既有 widget/unit 測試全數通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-29T11:20:46'
completed_at: null
created: '2026-05-29'
updated: '2026-05-29'
---

# Execution Log

## Task Summary

修正 透明遮罩黑底：MainFlutterWindow 補設 flutterViewController.backgroundColor=.clear

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 症狀
flutter run -d macos 啟動後，透明遮罩視窗顯示為黑色背景，遮擋桌面底下內容，違反 SPEC-001 FR-02「背景真透明」與遮罩時鐘核心需求。

### 根因
macOS 原生端只把 NSWindow（self）設為透明，漏設 FlutterViewController 透明層。

現況 macos/Runner/MainFlutterWindow.swift（已正確）：
- self.isOpaque = false（line 13）
- self.backgroundColor = .clear（line 14）

Dart 端三處透明（已正確）：
- lib/main.dart:55 windowManager.setBackgroundColor(overlayBackground)
- lib/main.dart:173 Scaffold backgroundColor: overlayBackground
- lib/app_constants.dart:48 overlayBackground = Colors.transparent

缺失：FlutterViewController 本身的 backgroundColor 未設。Flutter 3.7+ macOS embedder 的 FlutterView backing layer 預設 opaque，會自繪黑底蓋在透明 NSWindow 上。只設 NSWindow 透明不足，必須同時設 flutterViewController.backgroundColor = .clear。

### 證據
- flutter/flutter #119132（Flutter 3.7.0 renders black background on macOS）
- leanflutter/window_manager #293（macOS transparency broken in Flutter 3.7.0）
- flutter/flutter #59969（Transparent FlutterViewController）
- 本機環境：Flutter 3.38.10 / Dart 3.10.9

### 關鍵結論
此黑底在 Flutter 3.7+ 持續存在，升級 Flutter 不會自動修復，必須補原生設定。


### WRAP 行前三問
- **W（其他做法）**：(1) 補設 `flutterViewController.backgroundColor = .clear`【選此，flutter/flutter #119132 等官方 issue 證實的標準解】；(2) 調整 `setBackgroundColor` 色值【無效，根因在 FlutterView backing layer 非 NSWindow】；(3) 升級 Flutter【無效，regression 持續存在於 3.7+】。
- **A（機會成本）**：單檔一行原生設定，不擠壓其他 v1.0.x 目標；屬遮罩核心需求 hotfix。
- **P（最可能失敗）**：原生改對但本機 build 快取未清導致視覺仍黑底；防護：`flutter clean` 後重 build 再目視驗收。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 變更摘要

macos/Runner/MainFlutterWindow.swift 於既有透明設定區塊（self.isOpaque / self.backgroundColor）後補一行：

```swift
flutterViewController.backgroundColor = .clear
```

僅動此 Swift 檔，未改 Dart 端（lib/ 透明設定已正確）。

### 註解設計

於新增行上方補兩行業務情境註解，說明根因與必要性：FlutterView backing layer 在 Flutter 3.7+ macOS 預設不透明，會自繪黑底蓋住透明 NSWindow，必須與 NSWindow 同步清背景，否則透明遮罩顯示為黑底（引用 flutter/flutter #119132）。註解聚焦「為何需要」而非「做了什麼」，與既有 SPEC-001 FR-02 註解同群組。

### Phase 4 評估

- 重構需要：否
- 技術債務：無
- 程式碼品質：A+（單行原生設定，無分支、無新依賴，認知負擔指數 1）
- 結論：單檔一行 hotfix，無重構空間，維持現狀。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 測試執行（fix 分支實跑，2026-05-29）

指令：`flutter clean && flutter pub get && flutter test`

結果：**All tests passed!**（29 passed / 0 failed）

涵蓋：platform/screen_arg_test、models/settings_model_test、widget_test（含 transparent scaffold）、services/settings_service_test、widgets/center_clock_test。SettingsService 測試中的兩行 fallback log 為 corrupt-JSON 容錯用例的預期輸出，非失敗。

### 備註：parsley worktree 的 BLOCKED 為基底錯誤造成的假阻擋

parsley 在 worktree 回報 flutter test 被 `^3.11.1` SDK 閘擋為 unmet。經查 worktree 從舊 main（47c06ee，SDK 放寬前）fork，未含本分支 `^3.10.0` 放寬，屬 worktree 基底錯誤造成的假阻擋。fix 分支（含 SDK 放寬）實跑全綠，#4 達成。

### 手動 GUI 驗收（fix 分支，2026-05-29）

`flutter run -d macos`（flutter clean 後重 build）目視確認：
- #2 背景透明：時鐘底下桌面內容可透視，非黑底 — 通過
- #3 click-through：滑鼠點時鐘區域底下程式仍可操作 — 通過
- #3 always-on-top：切換其他視窗時時鐘仍置頂 — 通過

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending