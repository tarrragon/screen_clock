---
version: 1.3.0
codename: mouse-button-binding
status: active
proposal: PROP-002
specs:
  - SPEC-007
created: "2026-06-15"
---

# v1.3.0 工作日誌：滑鼠按鍵綁定

## 版本目標

可擴充的「滑鼠按鍵綁定」domain：一筆綁定 = 實體滑鼠按鍵 → 動作。本版實作兩種動作：
拖曳滾動（按住側鍵以垂直位移合成滾輪）與綁定快捷鍵（合成鍵盤組合鍵）。一律吃掉
原本的上一頁/下一頁，設定面板以偵測捕捉指定按鍵、參數可調。底層原生 CGEventTap，
需移除 App Sandbox + 輔助使用權限。macOS only。

來源：PROP-002 / SPEC-007（FR-01~08）。

## Wave 結構

| Wave | 範圍 | 票 |
|------|------|-----|
| W1 可行性 | CGEventTap 四假設 spike（已驗證全過） | W1-001（completed） |
| W2 基礎建設 | 移除 sandbox + 權限 + channel；Dart 綁定 domain + schema v3；tap 整合 | W2-001 / W2-002 / W2-003 |
| W3 動作 | ①拖曳滾動 ②綁定快捷鍵 | 待建 |
| W4 面板 | 偵測捕捉 + 綁定清單 UI + 持久化 | 待建 |
| W5 收尾 | runtime 驗收 + 非 sandbox 回歸；CHANGELOG/README + 打包 | 待建 |

依賴：W2-003 blockedBy W2-001 + W2-002（W2-001、W2-002 可並行）。

## 進度追蹤

- 2026-06-15: [建立] PROP-002 + SPEC-007 + 版本索引；1.2.1 關閉（假全螢幕讓位非用戶目標）、1.3.0 啟用 -- commit 80c4c29
- 2026-06-15: [完成] W1-001 spike：四假設經用戶實機驗證全數成立，PROP-002 可行性確認
- 2026-06-15: [拆分] W2 三張實作票建立（W2-001 原生基礎 / W2-002 Dart domain / W2-003 tap 整合）
- 2026-06-15: [完成] W2-002 Dart 綁定 domain + schema v3，merge 入 main（80 tests 全過 / analyze 乾淨），PM 驗收通過
- 2026-06-15: [完成] W2-001 原生基礎（移除 App Sandbox + 輔助使用權限 + channel 骨架），用戶 runtime 回歸四項全正常，merge 入 main。acceptance #2 live 授權提示折入 W2-003
- 2026-06-15: [備註] harness worktree 隔離從 origin/main 取基底，dispatch 前須先 push 本地 main，否則依賴前票的 worktree 會拿到落後基底
- 2026-06-15: [完成] W2-003 CGEventTap 整合 + 拖曳滾動 + 預設綁定。用戶 runtime 驗證通過：按住 button 4 側鍵拖動即捲動（壞滾輪替代品上線）。merge 入 main、86 tests
- 2026-06-15: [事件] W2-003 runtime 排查：disk 滿載一度阻斷（用戶清空間後接續）；預設綁定因既有存檔空 bindings 覆蓋 defaults 未生效（手動刪正確位置存檔後解，過程踩 sandbox 容器誤導 defaults）
- 2026-06-15: [發現→spawn] W3-001(ANA：debug 設定面板透明穿透調查) / W3-002(IMP：既有使用者預設綁定 seed migration)，皆 source W2-003
- 2026-06-15: [備註] W2 三票全完成；拖曳滾動可日常使用。原 roadmap 的「快捷鍵動作(W3)/設定面板(W4)」順延至 W3-001/002 之後另編號
- 2026-06-16: [完成] W3-001 設定面板透明調查：用戶乾淨重跑後面板正常（暗遮罩 + 不透明框 + 按鈕滑桿可點），先前回報不重現，研判 transient debug 啟動時序 artifact、非程式碼 bug。結案（無需後續修復 ticket）
- 2026-06-16: [handoff] 用戶指定下一 session 接「快捷鍵動作 + W4 設定面板」。詳見下方接手指引
- 2026-06-16: [完成] W3-002 既有使用者預設綁定 seed migration：新增 `bindingsSeeded` 一次性旗標，舊存檔升級時僅空綁定才補 defaults（不覆蓋既有非空綁定），一律標記 seeded 並持久化。RED `0848989` → GREEN `10284df`，flutter test 96/96、Phase 4 無需重構，merge + push 入 main（`d483661`）。v1.3.0 現有 ticket 全完成（6/6）

## 接手指引（下一 session）

**現狀**：v1.3.0 拖曳滾動已上線可用（W2 三票 + W3-001 + W3-002 完成，push 至 `d483661`，現有 ticket 6/6）。按住 button 4 側鍵拖動即捲動；既有使用者升級自動取得預設綁定。

**下一步工作（用戶指定）**：快捷鍵動作 + W4 設定面板。建議先 W4 面板（提供建立綁定的途徑），再 hotkey 執行（需面板能建 hotkey 綁定才可測）。

| 待做項目 | SPEC | 說明 |
|---------|------|------|
| 設定面板綁定管理（W4） | FR-06 / FR-08 | 綁定清單（新增 / 刪除）+ 偵測捕捉（滑鼠鍵 via channel；快捷鍵 via Flutter HardwareKeyboard）+ 動作參數（方向 / 靈敏度 / 組合鍵）。可能需拆多票 |
| HotkeyAction 執行 | FR-05 | 原生 `InputBindingBridge.handleButtonDown` 目前 hotkey 分支僅「消費 + TODO log」，需實作合成鍵盤組合鍵。blockedBy 面板能建 hotkey 綁定 |

**關鍵 context**：
- domain 規格：`docs/spec/input/mouse-button-binding.md`（SPEC-007）
- 原生分派：`macos/Runner/MainFlutterWindow.swift` 的 `InputBindingBridge`（hotkey 分支留 TODO）
- 模型：`lib/input/`（`MouseBinding` / `MouseAction` / `HotkeyAction` 已有 keyCode + modifiers 欄位）+ `lib/input/input_binding_channel.dart` + `input_binding_controller.dart`

**踩雷備忘**：
- worktree 隔離從 origin/main 取基底 → 派發依賴前票成果的 worktree 前先 `git push`（見 memory `worktree-isolation-bases-on-origin-main`）
- 非 sandbox app prefs 在 `~/Library/Preferences/com.example.screenClock.plist`；舊 sandbox 容器會誤導 `defaults` 指令
- debug build TCC 自動授權提示常不顯示，需手動把 app 加入「輔助使用」清單；日常用需打包簽章 release（debug 會停）
- 預設綁定僅 `defaults()` 提供（無存檔時生效）；既有空 bindings 存檔會覆蓋 → W3-002 處理
