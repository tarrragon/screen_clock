---
id: 1.3.0-W1-001
title: spike 驗證 CGEventTap 攔截側鍵/合成滾輪/消費事件/輔助使用權限四假設
type: ANA
status: in_progress
version: 1.3.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 1.3.0-W2-001
- 1.3.0-W2-002
- 1.3.0-W2-003
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:需求分析
  final_decision: 先 spike 驗證 CGEventTap 四假設再進 TDD 拆票
  rationale: quality-baseline-rule-1-runtime-correctness
who:
  current: parsley-flutter-developer
  history: {}
what: 驗證 CGEventTap 拖曳滾動與按鍵綁定可行性
when: v1.3.0 W1，優先於所有 TDD 實作
where:
  layer: Infrastructure
  files:
  - scripts/experiments/
  - macos/Runner/MainFlutterWindow.swift
  - docs/spec/input/mouse-button-binding.md
why: PROP-002 成敗幾乎全在原生 runtime 行為（CGEventTap 攔截側鍵、合成滾輪被接受、消費事件不卡系統、移除 sandbox 後取得輔助使用授權），此四項
  unit test 驗不出，屬 quality-baseline 規則 1『測試綠燈≠runtime 正確』陷阱；須先 spike 驗證再展開 TDD，避免 false
  positive 與設計到一半回頭
how:
  task_type: Analysis
  strategy: 建立最小 standalone Swift 實驗於 scripts/experiments/：建 CGEventTap 監聽 otherMouseDown/Up/mouseMoved/otherMouseDragged，印出側鍵
    buttonNumber；按住側鍵+垂直位移合成 CGScrollWheelEvent；回呼回傳 nil 消費側鍵事件；以 AXIsProcessTrusted
    檢查授權。由用戶實機執行（Terminal 需授予輔助使用）觀察四假設，回填結論與 TDD spawn 規劃
acceptance:
- '[x] 驗證 CGEventTap 能攔截滑鼠側鍵(otherMouseDown/Up,buttonNumber>=3)與移動事件,記錄實機印出的 buttonNumber
  對照(後側鍵/前側鍵=哪個編號)'
- '[x] 驗證按住側鍵+垂直移動合成 CGScrollWheelEvent 能讓瀏覽器/文件實機捲動,記錄方向(往下拖=往下捲)與靈敏度觀察'
- '[x] 驗證移除 sandbox+授予輔助使用後 event tap 可建立,且回呼回傳 nil 能消費側鍵事件使上一頁/下一頁不觸發,實機操作無滑鼠卡頓/異常'
- '[x] 產出四假設結論(成立/不成立/部分成立)與後續 TDD spawn 規劃(IMP 拆解/SPEC-007 細化點),回填 spawned_tickets
  或標註豁免理由'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-15T20:59:15'
completed_at: null
created: '2026-06-15'
updated: '2026-06-15'
---

# Execution Log

## Task Summary

驗證 CGEventTap 拖曳滾動與按鍵綁定可行性

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### Spike 實作參考（給執行代理人）

目標：建立 `scripts/experiments/cgeventtap_scroll_spike.swift`（throwaway 實驗，非產品碼；scripts/experiments/ 屬 branch-verify 豁免，可於 main 建立），驗證 PROP-002 四假設。

代理人職責：
1. 依下方參考碼建立檔案（可微調，但保留四假設的觀測點與註解）。
2. 編譯檢查：`swiftc -typecheck scripts/experiments/cgeventtap_scroll_spike.swift`（或 `swift -frontend -typecheck`），修正型別錯誤（如 kAXTrustedCheckOptionPrompt 需 import、CFMachPort? 解包）。
3. **不要實機執行**（需互動式輔助使用授權 + 人工觀察，由用戶執行）。
4. 回填 ticket Test Results（編譯結果），**不要 complete ticket**（需用戶實機回報後 PM 才結案）。

參考實作：

```swift
import Cocoa
import CoreGraphics

let sensitivity: Double = 1.0
let invertDirection = false

var dragging = false
var lastY: CGFloat = 0
let scrollSource = CGEventSource(stateID: .hidSystemState)
var tapRef: CFMachPort?

func handle(proxy: CGEventTapProxy, type: CGEventType, event: CGEvent, userInfo: UnsafeMutableRawPointer?) -> Unmanaged<CGEvent>? {
  if type == .tapDisabledByTimeout || type == .tapDisabledByUserInput {
    if let t = tapRef { CGEvent.tapEnable(tap: t, enable: true) }
    return Unmanaged.passUnretained(event)
  }
  switch type {
  case .otherMouseDown:
    let button = event.getIntegerValueField(.mouseEventButtonNumber)
    print("[down] 側鍵 buttonNumber=\(button) → 進入拖曳滾動，消費此事件")
    dragging = true
    lastY = event.location.y
    return nil
  case .otherMouseUp:
    if dragging { dragging = false; print("[up] 離開拖曳滾動，消費此事件"); return nil }
    return Unmanaged.passUnretained(event)
  case .mouseMoved, .otherMouseDragged, .leftMouseDragged, .rightMouseDragged:
    guard dragging else { return Unmanaged.passUnretained(event) }
    let y = event.location.y
    let dy = y - lastY
    lastY = y
    if dy != 0 {
      var amount = Int32((invertDirection ? dy : -dy) * sensitivity)
      if amount == 0 { amount = dy > 0 ? -1 : 1 }
      let scroll = CGEvent(scrollWheelEvent2Source: scrollSource, units: .pixel, wheelCount: 1, wheel1: amount, wheel2: 0, wheel3: 0)
      scroll?.post(tap: .cghidEventTap)
    }
    return nil
  default:
    return Unmanaged.passUnretained(event)
  }
}

let trusted = AXIsProcessTrustedWithOptions([kAXTrustedCheckOptionPrompt.takeUnretainedValue() as String: true] as CFDictionary)
if !trusted {
  print("[權限] 尚未取得輔助使用授權。系統設定 → 隱私權與安全性 → 輔助使用，勾選目前終端機 app 後重跑。")
  exit(1)
}
print("[權限] 輔助使用已授權（假設 3 成立）。")

let mask: CGEventMask =
  (1 << CGEventType.otherMouseDown.rawValue) |
  (1 << CGEventType.otherMouseUp.rawValue) |
  (1 << CGEventType.mouseMoved.rawValue) |
  (1 << CGEventType.otherMouseDragged.rawValue) |
  (1 << CGEventType.leftMouseDragged.rawValue) |
  (1 << CGEventType.rightMouseDragged.rawValue)

guard let tap = CGEvent.tapCreate(tap: .cgSessionEventTap, place: .headInsertEventTap, options: .defaultTap, eventsOfInterest: mask, callback: handle, userInfo: nil) else {
  print("[失敗] event tap 建立失敗（假設 1/3）。")
  exit(1)
}
tapRef = tap
let src = CFMachPortCreateRunLoopSource(kCFAllocatorDefault, tap, 0)
CFRunLoopAddSource(CFRunLoopGetCurrent(), src, .commonModes)
CGEvent.tapEnable(tap: tap, enable: true)
print("[就緒] event tap 已啟用。按住側鍵上下移動觀察捲動；Ctrl+C 結束。")
CFRunLoopRun()
```

### 用戶實機觀察協議（PM 於用戶回報後回填四假設結論）

執行：`swift scripts/experiments/cgeventtap_scroll_spike.swift`
（首次需到 系統設定 → 隱私權與安全性 → 輔助使用 勾選執行用的終端機 app，再重跑）

觀察並回報：
1. 各側鍵 buttonNumber（後側鍵/前側鍵分別印出幾號）→ 假設 1
2. 按住側鍵上下拖動，瀏覽器/文件是否捲動、方向對不對 → 假設 2
3. 按住側鍵期間有沒有誤觸上一頁/下一頁 → 假設 4
4. 滑鼠整體有無卡頓/異常 → 假設 4

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### Spike 實機結果（用戶執行，2026-06-15）

原始觀察：側鍵連續 down/up 印出 buttonNumber=4；按住側鍵垂直拖動頁面捲動、方向正確；無換頁；無卡頓。

### 四假設結論

| 假設 | 結論 | 證據 |
|------|------|------|
| 1 攔截側鍵 + 讀 buttonNumber | 成立 | 實機印出 buttonNumber=4（前側鍵/下一頁鍵），down/up 皆攔到 |
| 2 合成滾輪 + 方向 | 成立 | 按住側鍵垂直拖動，瀏覽器/文件捲動；invertDirection=false 即「往下拖=往下捲」，方向正確 |
| 3 輔助使用授權 + tap 建立 | 成立 | 授權後 spike 正常執行，event tap 啟用 |
| 4 消費事件不卡系統 | 成立 | 按住期間無誤觸上一頁/下一頁（nil 消費生效），滑鼠操作無卡頓/異常 |

結論：PROP-002 核心可行性四假設全數成立，無須走任何退路。可進入完整 TDD 實作。
備註：用戶實機側鍵為 button 4（前側鍵）；另一顆（後側鍵，推測 button 3）未測，不影響可行性——正式版以偵測捕捉模式抓任意 buttonNumber，與固定編號無關。

### 後續 TDD spawn 規劃（roadmap）

| Wave | Ticket（待建） | 內容 |
|------|---------------|------|
| W2 基礎建設 | IMP 移除 sandbox + entitlements/Info.plist + 輔助使用權限與 method channel（原生） | FR-07 |
| W2 | IMP Dart input domain（MouseBinding/MouseAction/ScrollDirection）+ 序列化 + SettingsModel schema v3（TDD） | FR-01/02 |
| W2 | IMP CGEventTap 整合進 app 本體（沿用 spike 邏輯），接收綁定清單並分派 | FR-03 |
| W3 動作 | IMP 動作①拖曳滾動（合成滾輪 + 吃原動作 + 方向/靈敏度） | FR-04 |
| W3 | IMP 動作②綁定快捷鍵（合成鍵盤組合鍵） | FR-05 |
| W4 面板 | IMP 偵測捕捉（滑鼠鍵 via channel + 快捷鍵 via Flutter） | FR-06 |
| W4 | IMP 綁定清單 UI（新增/刪除）+ 參數 + 持久化串接 | FR-08 |
| W5 收尾 | ANA runtime 實機驗收（4 AC）+ 非 sandbox 既有功能回歸；DOC CHANGELOG/README + 打包 | 驗收 |

spawned_tickets 將於用戶確認 roadmap 後，先建 W2 三張實作票回填本 ANA，後續 Wave 依序建立。

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->
### 編譯檢查（PM 前置，2026-06-15）

- spike 檔位置改放 ticket worklog 目錄（PM 可寫 docs/，免 worktree 來回）：
  `docs/work-logs/v1/v1.3/v1.3.0/spike/cgeventtap_scroll_spike.swift`
- 編譯檢查：`swiftc -typecheck <file>` → exit 0，無錯誤/警告。
- 狀態：靜態 typecheck 通過；四假設待用戶實機執行觀察後回填結論（ticket 維持 in_progress，不結案）。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
W1 spike ANA 完成。四假設經用戶實機驗證全數成立（攔截側鍵 button4 / 合成滾輪方向正確 / 輔助使用授權 + tap 建立 / 消費事件不換頁不卡頓），PROP-002 核心可行性確認，無須走退路。
Spawn 落地：W2-001（原生基礎：移除 sandbox+權限+channel）、W2-002（Dart 綁定 domain+schema v3）、W2-003（CGEventTap 整合，blockedBy W2-001/W2-002）已建並雙向關聯。W3-W5 roadmap 記於 Solution，依 Wave 推進時建立。
