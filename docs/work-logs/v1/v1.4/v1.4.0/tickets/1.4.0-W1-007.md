---
id: 1.4.0-W1-007
title: 建立 使用者設定持久化的 data-contract 文件
type: DOC
status: in_progress
version: 1.4.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: sassafras-data-administrator
  history: {}
what: 建立 使用者設定持久化的 data-contract 文件
when: doc skill 將 DataContract 升為 first-class 文件類型；W1-001 將把 SettingsModel schema
  由 3 升至 4
where:
  layer: Infrastructure
  files:
  - docs/spec/data-management/
  - lib/models/settings_model.dart
why: 待定義
how:
  task_type: Implementation
  strategy: 以 doc create data-contract --domain data-management 建立（沿用 SPEC-NNN 編號空間，subdomain
    固定 data-contract）；依 data-layer-contract-methodology.md 的可攜性兩區結構撰寫：A 區邏輯契約（DB-agnostic：schema
    各版本欄位語意、不變式 INV-xx、預設值來源、migration 保證）、B 區實作綁定（DB-specific：JSON 檔位置、序列化格式、讀寫時機）；記錄
    schema 1 到 4 的演進與各版 migration 規則；以 doc validate SPEC-NNN 驗證章節 schema
acceptance:
- '[x] data-contract 文件建立於 docs/spec/data-management/ 且 frontmatter subdomain 為 data-contract'
- '[x] 含可攜性邊界原則 + A 區（A.1-A.6）+ B 區（B.1-B.3）章節'
- '[x] schema 1 至 4 各版欄位與 migration 規則完整記錄'
- '[x] doc validate <SPEC-ID> 通過（exit 0）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-07-29T15:15:03'
completed_at: null
created: '2026-07-29'
updated: '2026-07-29'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

建立 使用者設定持久化的 data-contract 文件

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型以 Completion Info 變更摘要取代）

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

（免填：DOC 類型無需測試）

---

### 驗收驗證命令實際輸出（2026-07-29）

目標檔案：docs/spec/data-management/SPEC-009-settings-storage-data-contract.md

**[1] 文件建立且 subdomain 正確** — grep -n "subdomain" <file>

```
13:subdomain: "data-contract"
```

**[2] 章節 schema 合規** — doc validate SPEC-009

```
通過: SPEC-009 符合 data-contract 章節 schema
exit=0
```

**[3] 編號未重複** — doc validate-filenames

```
通過: 所有文件檔名符合配號器慣例
exit=0
```

**[4] A/B 兩區齊備** — grep -c "^## " <file> = 7

```
21:## 概述
29:## 可攜性邊界原則
40:## A 區：邏輯契約（DB-agnostic）
136:## B 區：實作綁定（DB-specific）
185:## 欄位 × 既有載體對照表
202:## 適用判準（本文件是否需要撰寫）
210:## 變更歷史
```

子章節 grep -n "^### [AB]\." 命中 9 節：A.1 表/欄位語意、A.2 狀態責任分層、A.3 不變式清單、A.4 交易邊界、A.5 錯誤語意契約、A.6 恢復模型、B.1 保證層歸屬、B.2 邊界行為的引擎機制、B.3 Schema 演進策略與 Seed 資料政策。

四條驗收驗證命令全數通過，無放寬條件。

### schema 版本溯源證據

逐 commit 比對 lib/models/settings_model.dart 的 schemaVersion 常數與 toJson() 欄位集：

```
10284df | schemaVersion = 3 | feat(1.3.0-W3-002): 既有使用者預設綁定 seed migration
e5cc4b9 | schemaVersion = 3 | feat(1.3.0-W2-003): CGEventTap 整合 + 拖曳滾動 + 預設綁定
d05724a | schemaVersion = 3 | feat(1.3.0-W2-002): Dart 滑鼠綁定 domain 與 schema v3
afa3d67 | schemaVersion = 2 | feat: 生命計時模式（即時年齡跑數顯示）
bd83aea | schemaVersion = 1 | feat(1.0.0-W1-002): SettingsModel data class with JSON round-trip
```

v1/v2 差異非「未記載」——由 git 歷史 toJson() 欄位集直接取得：v2 相對 v1 新增 lifeTimerMode 與 birthDate。v3 新增 bindings（d05724a）；bindingsSeeded 於 10284df 加入但未提升 schemaVersion，已在 A.1 演進表明列為「同版擴充」。
## Context Bundle

### 規範依據

- .claude/skills/doc/templates/data-contract-template.md — 模板（A 區 / B 區兩區結構）
- .claude/methodologies/data-layer-contract-methodology.md — 三層保護模型與可攜性邊界原則
- .claude/skills/doc/SKILL.md — DataContract 為 first-class 文件類型；沿用 SPEC-NNN 編號空間，frontmatter subdomain 固定為 data-contract
- .claude/skills/spec/SKILL.md「適用範圍限制」— data-contract 文件不適用 /spec validate，機械驗證改由 doc validate 承接

### 建立方式

用 CLI 建立以取得自動編號與日期（現有 spec 已用到 SPEC-008，本文件應配到 SPEC-009）：

doc create data-contract --title "settings-storage-data-contract" --domain data-management

不要用 cp 手動複製模板（會失去自動編號與 tracking entry）。

### 資料來源（實作即現況，須逐項讀取後書寫）

| 檔案 | 提供什麼 |
|------|---------|
| lib/models/settings_model.dart | schemaVersion 常數（現為 3）、欄位定義、fromJson 的舊版相容處理、toJson 的序列化格式 |
| lib/services/settings_service.dart | 持久化位置、讀寫時機、失敗處理 |
| lib/state/settings_controller.dart | 預設值來源與變更流程 |
| docs/spec/data-management/SPEC-004-settings-storage.md | 既有規格描述，避免與契約文件重複或矛盾 |

已知演進線索（須以實際程式碼驗證後才寫入，禁止照抄本段推測）：schemaVersion 3 的註解記載「新增 bindings（滑鼠按鍵綁定清單）；舊版 v2 資料缺 bindings 欄，fromJson 解析為空清單，向後相容」。v1 與 v2 的差異須從 git log 或既有 spec 追溯，追不到者標記為未記載，不虛構。

### A 區與 B 區的分界（可攜性邊界原則）

| 區 | 性質 | 本專案對應內容 |
|----|------|--------------|
| A 區（邏輯契約，DB-agnostic） | 換一種儲存後端仍成立的約定 | 各 schema 版本的欄位語意、型別、值域；不變式（如 schemaVersion 單調遞增、欄位一旦加入不再移除）；預設值來源；migration 保證（舊版讀入不丟資料） |
| B 區（實作綁定，DB-specific） | 綁定當前儲存實作的細節 | JSON 檔實際路徑、序列化格式細節、讀寫觸發時機、檔案損毀或不存在時的降級行為 |

判準：若把 JSON 檔換成 SQLite 後仍需遵守，屬 A 區；只在 JSON 實作下成立，屬 B 區。

### schema 4 的處理

1.4.0-W1-001 將把 schemaVersion 由 3 升至 4（新增滑鼠定位器設定：啟用開關、特效時長、主色調）。本 ticket 先記錄 1 至 3 的既成事實，並為 4 預留條目，標記狀態為規劃中，欄位定義引用 docs/spec/user-experience/SPEC-008-cursor-locator.md 的設定章節。不預先斷言尚未實作的欄位名稱與型別，標註來源為 spec 而非 code。

### 驗收驗證命令

| 驗收條件 | 驗證命令 | 期望 |
|---------|---------|------|
| 文件建立且 subdomain 正確 | grep -n "subdomain" 新建檔案 | subdomain: data-contract |
| 章節 schema 合規 | doc validate SPEC-009 | exit 0 |
| 編號未重複 | doc validate-filenames | exit 0（本 ticket 建立的檔案須自帶 SPEC-NNN 前綴） |
| A/B 兩區齊備 | grep -c "^## " 新建檔案 | 含可攜性邊界原則 + A 區 + B 區 |

### 執行約束

- 不修改 lib/ 下任何程式碼（本 ticket 是記錄契約，非改實作）
- 不修改 SPEC-004（settings-storage）內容；若發現兩者矛盾，於契約文件標註並回報 PM，不自行改 spec
- 欄位資訊一律以實際讀取 lib/models/settings_model.dart 為準，禁止依 spec 描述推測

### Commit Policy

單一 commit，訊息格式：docs(1.4.0-W1-007): 建立使用者設定持久化 data-contract

注意：1.4.0-W1-002 會重命名 docs/spec/ 下既有檔案。本 ticket 新建檔案自帶 SPEC-NNN 前綴，與該重命名無衝突；但若執行時 SPEC-004 已改名，引用時須用新路徑。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "SPEC-009 data-contract 建立完成，四條驗收驗證命令全數通過（doc validate exit 0、doc validate-filenames exit 0）"
confidence: 0.95
acceptance_met: [1, 2, 3, 4]
acceptance_unmet: []
artifacts:
  - docs/spec/data-management/SPEC-009-settings-storage-data-contract.md
context_dependencies:
  - lib/models/settings_model.dart
  - lib/services/settings_service.dart
  - lib/state/settings_controller.dart
  - lib/app_constants.dart
  - docs/spec/data-management/SPEC-004-settings-storage.md
  - docs/spec/user-experience/SPEC-008-cursor-locator.md
  - git log 於 lib/models/settings_model.dart（v1/v2/v3 欄位集溯源）
estimated_recovery_effort: ""
```

### 需 PM 裁決的發現（未自行處理）

1. **SPEC-004 內容停留在 schema v1**：FR-02 記載「schemaVersion 目前為 1」、NFR-02 記載「MVP 階段只實作 v1 schema」、FR-01 欄位表僅 7 欄，未涵蓋 v2/v3 新增的 birthDate / lifeTimerMode / bindings / bindingsSeeded。依 Context Bundle 執行約束「不修改 SPEC-004」，已於 SPEC-009 A.1 以「已知落差」標註並以 code 為準，未改動 SPEC-004。是否回補由 PM 決定。

2. **bindingsSeeded 加欄未提升 schemaVersion**（commit 10284df）：造成 schema v3 內部存在「有無 bindingsSeeded」兩種資料形態，且該欄的 migration 觸發條件為旗標值而非版本號比較。已於 A.1 演進表與 B.3 明列，屬既成事實記錄，非本 ticket 修正範圍。

3. **契約 ↔ 測試對應軸缺位**：本專案無 docs/traceability.yaml，INV-01 至 INV-09 目前無對應測試索引。已於「欄位 × 既有載體對照表」標為「未承載」。

4. **INV-03（只加欄、不改型別）無機械強制**：已於 B.1 明列為「本契約最脆弱的一條」——違反不會有自動化訊號，僅在使用者升級後資料異常時暴露。若 PM 認為需要防護，可考慮建立 schema 欄位快照測試。

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->
**Completion Time**: 2026-07-29
**Executing Agent**: sassafras-data-administrator
**Review Status**: pending

### 變更摘要

**新增** `docs/spec/data-management/SPEC-009-settings-storage-data-contract.md`（1 檔，經 doc create data-contract CLI 建立，自動配號 SPEC-009）：

| 章節 | 內容 |
|------|------|
| 概述 | 涵蓋範圍 + 資料來源聲明（以 code 為準） |
| 可攜性邊界原則 | A/B 兩區判準：換掉 shared_preferences 後是否仍成立 |
| A.1 表/欄位語意 | v3 現行 12 欄完整定義（型別/單位/值域）+ schema v1→v4 演進表（含引入 commit 與舊版讀入保證）+ SPEC-004 落差標註 |
| A.2 狀態責任分層 | canonical（含 autoLaunch 的 OS 對帳例外）/ 追蹤欄位（schemaVersion、bindingsSeeded） |
| A.3 不變式清單 | INV-01 至 INV-09 + schemaVersion 實際不參與解析的重要註記 |
| A.4 交易邊界 | 整份設定原子寫入；autoLaunch OS 對帳與 seed 遷移為非原子 |
| A.5 錯誤語意契約 | 不定義型別化例外，全數降級；含「無法區分首次啟動與資料損毀」的契約後果 |
| A.6 恢復模型 | 無自動備份；跨版本降級明確標為不保證（未知欄位不保留） |
| B.1 保證層歸屬 | 九條不變式全由應用層保證；INV-03 標為最脆弱條目 |
| B.2 邊界行為的引擎機制 | SharedPreferences 單鍵 screen_clock.settings.v1、JSON 字串序列化、Color ARGB32、DateTime epoch ms、讀寫時機、降級行為 |
| B.3 Schema 演進策略與 Seed 資料政策 | 加欄 + 預設值 fallback、無 onUpgrade；唯一命令式 migration _migrateBindingSeed；defaults() 內建一筆側鍵拖曳滾動綁定 |
| 欄位 × 既有載體對照表 | 逐項標新建 / 部分承載（引用 SPEC-004 對應章節） |
| 適用判準 | 契約文件「要」、migration 治理「要」、dormant 豁免「不適用」 |

**未變更**：lib/ 下所有程式碼、SPEC-004、SPEC-008（依 Context Bundle 執行約束）。

v4 條目來源標註為 SPEC-008 FR-06 規格而非 code，不預先斷言尚未實作的 JSON 鍵名與 Dart 欄位名。
