---
id: 1.4.0-W2-006
title: 實作 Swift 特效層的螢幕判定與視窗管理
type: IMP
status: in_progress
version: 1.4.0
wave: 2
priority: P0
parent_id: null
children:
- 1.4.0-W2-006.1
- 1.4.0-W2-006.2
blockedBy: []
relatedTo: []
spawned_tickets:
- 1.4.0-W2-026
- 1.4.0-W2-027
- 1.4.0-W2-028
source_ticket: 1.4.0-W1-009
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: 特效層拆為視窗管理與視覺繪製兩票
  rationale: 1.4.0-W1-009 建議合併四個 FR 為一票但備註可再拆；依 cognitive-load 規則，視窗生命週期與三種特效繪製屬不同職責，分票降低單票認知負擔
who:
  current: pepper-test-implementer
  history: {}
what: 實作 Swift 特效層的螢幕判定與視窗管理
when: 1.4.0-W1-001（channel 骨架）與熱鍵票（觸發入口）完成後
where:
  layer: Infrastructure
  files:
  - macos/Runner/
  - lib/platform/
why: SPEC-008 FR-02 目標螢幕判定與跨螢幕跟隨；UC-06 主成功步驟 2/3/5、替代場景 06a 跨螢幕跟隨、06b 播放期間重複觸發、06d
  覆蓋全螢幕應用、例外 EX-06-02 螢幕解析失敗、EX-06-03 視窗建立失敗
how:
  task_type: Implementation
  strategy: Swift 側建立特效專用 NSWindow：依 NSEvent.mouseLocation 判定游標所在 NSScreen；視窗 level
    須高於假全螢幕應用（與時鐘遮罩的讓位策略相反，見 SPEC-008 架構決策）；播放期間以 CVDisplayLink 驅動並跟隨游標跨螢幕移動；重複觸發時重置既有播放而非疊加視窗；結束時淡出並確實釋放視窗避免洩漏；螢幕解析失敗與視窗建立失敗皆須
    NSLog 並經 channel 回報 Dart 側；本票只做螢幕判定與視窗生命週期，三種視覺特效的繪製由後續票承接
acceptance:
- '[ ] 觸發時於游標所在螢幕建立特效視窗，多螢幕環境判定正確'
- '[ ] 播放期間游標移至另一螢幕時特效跟隨'
- '[ ] 特效視窗顯示於假全螢幕應用之上'
- '[ ] 播放期間重複觸發為重置而非疊加視窗'
- '[ ] 播放結束後視窗確實釋放，重複觸發十次無視窗洩漏'
- '[ ] 螢幕解析失敗與視窗建立失敗皆有日誌並回報 Dart 側'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-31T15:29:46'
completed_at: null
created: '2026-07-31'
updated: '2026-07-31'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實作 Swift 特效層的螢幕判定與視窗管理

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 認領時 WRAP 三問（0.18.0-W10-027 checkpoint）

**W（Widen）—— 有其他做法嗎？**

Swift 側驗證策略列三個候選：(a) 啟用 XCTest 測純邏輯層（螢幕判定抽純函式 + 視窗生命週期以 controller 狀態機測）；(b) 純人工實機驗證，對齊專案現況（至今零 Swift 測試，RunnerTests 僅 scaffold 空殼）；(c) 只測螢幕判定純函式，生命週期全歸人工。用戶裁示採 (a)。理由：acceptance 5「重複觸發十次無視窗洩漏」在 (b)(c) 下無自動訊號，回歸防護完全依賴人工重跑；且 XCTest 基礎設施建立後 1.4.0-W3-001（三種視覺特效繪製）可直接複用。

**A（Attain distance）—— 機會成本是什麼？**

本票是 1.4.0-W2 唯一 P0，其餘 20+ 張 W2 票皆 P2（多為重構與文件同步）。執行本票擠壓的是 W2-015（拆分 MainFlutterWindow.swift 多 bridge 單檔累積）——但兩者實為互補而非競爭：本票依 cognitive-load 規則將特效視窗落於新檔 `macos/Runner/CursorLocatorEffectWindow.swift`，不再往 836 行的 MainFlutterWindow.swift 堆疊，等於減少 W2-015 屆時的拆分面。

**P（Prepare to be wrong）—— 最可能失敗的原因是什麼？**

最可能失敗點：XCTest target 接線。macos/RunnerTests 雖存在且 project.pbxproj 有 33 處 XCTest 引用，但從未實際執行過任何測試，`flutter test` 不涵蓋 Swift target，需經 xcodebuild 或 Xcode scheme 執行。若接線耗時超出預期，防護措施為：螢幕判定的純函式抽取與 NSWindow 生命週期實作本身不依賴測試框架就緒，可先落地實作再補測試執行路徑，避免 P0 票卡在基礎設施。

---

### 派發前拆分決策（dispatch-readiness FAIL 覆核）

`ticket track dispatch-readiness 1.4.0-W2-006` 回報閾值 1 FAIL（acceptance 條目 6 > 4，強制拆分），閾值 2、3 PASS。CLI 以 acceptance 條目數近似功能職責數，含驗證類條目時會高估，故依規定手動覆核。

覆核結果：CLI 判定成立，非高估。六項 acceptance 依實作依賴分為兩組互不重疊的職責：

| 職責組 | 涵蓋 acceptance | 技術面 | 可測性 |
|--------|----------------|--------|--------|
| A 螢幕判定 | 1（判定部分）、2（判定部分）、6（E_CL_SCREEN） | NSEvent.mouseLocation 對 NSScreen.screens 的 frame 涵蓋判定 | 純函式，可注入 screens 陣列，XCTest 全覆蓋 |
| B 視窗生命週期 | 3、4、5、2（跟隨部分）、6（E_CL_WINDOW） | NSWindow 建立/level/collectionBehavior/ignoresMouseEvents、CVDisplayLink 驅動、淡出釋放 | controller 狀態機可測；視窗屬性需實機 |

依此拆為 1.4.0-W2-006.1（螢幕判定 + XCTest 基礎設施）與 1.4.0-W2-006.2（特效視窗生命週期），母票保留為 group。

---

### 規格落差澄清：acceptance 6「回報 Dart 側」的語意

本票 acceptance 6 措辭為「螢幕解析失敗與視窗建立失敗皆有日誌並回報 Dart 側」，而 SPEC-008 介面規格節明載「原生層不主動回呼 Dart」。兩者不矛盾，但措辭易誤導，於此定義權威解讀：

- 「回報 Dart 側」指 `play` 這次 method channel 呼叫以 `result(FlutterError(code:message:details:))` 回傳錯誤，屬呼叫的同步回應，既有 CursorLocatorBridge 已具此路徑（MainFlutterWindow.swift:805 的參數缺失分支即為先例）
- **禁止**新增原生 → Dart 的反向 channel 或 EventChannel，該做法違反 SPEC-008 介面規格
- E_CL_SCREEN 涵蓋兩條語意不同的路徑，須分別處理：**退回 main**（游標不落於任何螢幕 frame，SPEC-008 錯誤處理表定義的復原路徑）僅記 NSLog，播放照常進行、`play` 回傳成功；**無任何可用螢幕**（連 `NSScreen.main` 亦不可得，SPEC-008 未列舉的殘餘情形）為不可播放，以 FlutterError 回傳並歸入同一錯誤碼，不新增碼以免擴張 SPEC 定義的錯誤碼集合
- E_CL_WINDOW（視窗建立失敗、略過本次播放）走 FlutterError 回傳

> **PM 修訂（Phase 1.5，2026-07-31）**：本段原寫「僅 E_CL_WINDOW 走 FlutterError 回傳」，該絕對敘述在 Phase 1 引入「無可用螢幕」情形後不再成立，而 Phase 1 產出以「依 Problem Analysis」措辭承接，使兩處互斥卻看似一致（basil C-1）。錯在本段的絕對化，非 Phase 1 的擴充；已改為分岔敘述。判準仍不變：**復原路徑不以 FlutterError 中斷播放**，會到達 Dart 的 E_CL_SCREEN 語意唯一（無可用螢幕）。

---

### 檔案落點決策

新增 `macos/Runner/CursorLocatorEffectWindow.swift`，不將特效視窗程式碼加入 `MainFlutterWindow.swift`。

理由：MainFlutterWindow.swift 現為 836 行，超出 cognitive-load 規則的 300 行閾值 2.8 倍，且已承載時鐘遮罩視窗、CGEventTap、settings bridge、cursor locator bridge 等多個 domain，觸發「單一檔案承載 domain 數 > 2 必須拆分」條款。特效視窗的生命週期與上述任一 domain 皆無共用狀態，屬獨立 domain。MainFlutterWindow.swift 於本票僅改動一處：CursorLocatorBridge 的 play handler 由 NSLog 骨架改為委派至新類別。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

### TDD 階段規劃與子票定位

本票為 group，依 tdd-flow「3b 拆分」機制運作：TDD Phase 1 / 1.5 / 2 / 3a / 4 於**母票**執行一次，僅 Phase 3b 實作拆為子票落地。

| Phase | 執行位置 | 負責 | 產出 |
|-------|---------|------|------|
| Phase 1 功能設計 | 母票 | lavender-interface-designer | Swift 側介面規格：螢幕判定函式簽章、特效視窗控制器的公開介面與狀態模型 |
| Phase 1.5 規格審查 | 母票 | /parallel-evaluation G | 跨 SPEC-008 / UC-06 / 既有 CursorLocatorBridge 的一致性與邊界完整性（強制，無豁免） |
| Phase 2 測試設計 | 母票 | sage-test-architect | XCTest 案例設計（含 EX-06-02 間隙退回、十次觸發無洩漏的計數斷言設計） |
| Phase 3a 策略規劃 | 母票 | pepper-test-implementer | 語言無關實作策略 |
| Phase 3b 實作 | **子票** | parsley-flutter-developer | 1.4.0-W2-006.1（螢幕判定純函式 + XCTest 基礎設施）→ 1.4.0-W2-006.2（視窗生命週期，blockedBy .1） |
| Phase 4 重構評估 | 母票 | 依 3b 結果裁定 | 技術債務與重構結論 |

**Why 不讓子票各走完整 TDD**：兩張子票的功能規格同源於 SPEC-008 FR-02 與 UC-06，各走一次 Phase 1 + Phase 1.5 會對同一份規格重複設計與重複多視角審查，屬 tdd-flow 明確以 3b 拆分機制避免的重工。子票的 `tdd_stage` 應理解為僅涵蓋 phase3b。

**子票執行順序**：.1 → .2 串行（.2 的 blockedBy 已設 .1）。CLI 並行衝突偵測亦回報「1.4.0-W2-006.1 <-> 1.4.0-W2-006.2 有依賴關係，無法並行」，與此一致。

### 驗證策略裁示（用戶 2026-07-31 裁示）

採「啟用 XCTest 測純邏輯層」。macos/RunnerTests 現為 Flutter scaffold 空殼（僅 testExample()），專案至今零 Swift 測試，本票同時承擔測試基礎設施建立。覆蓋分工：螢幕判定三種情形與視窗計數歸零由 XCTest 斷言；視窗 level 高於假全螢幕、跨螢幕跟隨的視覺確認由實機驗證，結果記錄於各子票 Test Results。

### 派發位置決策

主 repo feat 分支 `feat/1.4.0-W2-006-cursor-locator-effect-window`（已建立），不使用 isolation:worktree。理由：單一代理人串行執行，worktree 的並行隔離收益不存在，卻須付出 settings.local.json additionalDirectories 設定成本，且代理人無法自行 `ticket track complete` 收尾需 PM 代跑（PC-SCLK-002，1.4.0-W1-023 探針證實）。
### TDD Phase 1 功能設計：Swift 側介面規格（lavender-interface-designer，2026-07-31 初版；2026-07-31 依 Phase 1.5 修訂）

本節定義 `macos/Runner/CursorLocatorEffectWindow.swift` 的公開介面契約，供 Phase 2 設計 XCTest 案例、Phase 3b 兩張子票實作。**不重寫 SPEC-008**；SPEC-008 FR-02 與 UC-06 為需求權威，本節只做原生層介面落地。不含 FR-03/04/05 視覺繪製（留 1.4.0-W3-001），但預留擴充點。

#### 一、目的與範圍

讓 `play` 呼叫能在「游標當下所在的螢幕」建立唯一一個特效視窗、播放使用者設定的時長後淡出並確實釋放，且整條路徑的判定邏輯、生命週期狀態與視窗屬性皆可被 XCTest 斷言。核心設計原則：**把「決定在哪播」抽成純資料運算，把「真的開視窗」隔在協定後面，把「時間怎麼流」交由呼叫端注入**，使 acceptance 1/2/3/4/5 皆有自動訊號，只留視覺外觀走實機驗證。

RunnerTests 為 app-hosted（`BUNDLE_LOADER=$(TEST_HOST)` + `ENABLE_TESTABILITY=YES`，PM 於 Phase 1.5 查證），故視窗屬性可於 XCTest 中對真實 `NSWindow` 直接斷言，不必只靠實機肉眼。

#### 二、介面契約

##### 2.1 螢幕幾何快照與座標系

判定所需的螢幕資訊由呼叫端在「取樣當下」建構為值型別快照傳入，判定層不接觸 `NSScreen`：

```swift
/// 一次判定所需的螢幕幾何快照。
/// 播放期間每幀重新建構，顯示器熱插拔因此即時反映，不需另訂閱
/// didChangeScreenParametersNotification。
struct CursorScreenSnapshot: Equatable {
  struct Entry: Equatable {
    /// 全域座標（AppKit 左下原點）。第二螢幕位於主螢幕左方或上方時
    /// origin 為負值，判定不得假設非負。
    let frame: NSRect
    /// 供 frame driver 綁定螢幕更新率用（SPEC-008 FR-03）。
    let displayID: CGDirectDisplayID
  }

  /// 依 `NSScreen.screens` 原始順序；命中取「第一個」依賴此順序。
  let entries: [Entry]
  /// `NSScreen.main` 在 `entries` 中的索引；取不到 main 時為 nil。
  /// entries 為空時必為 nil。
  let mainIndex: Int?
}
```

**座標系契約**：本節所有 `NSPoint` / `NSRect` 一律為 AppKit 全域座標，原點在主螢幕左下、y 軸向上。與 `MainFlutterWindow.swift` 內 `FullscreenCoverageDetector` 使用的 CGWindowList 左上原點座標系不同，兩者不可混用。Phase 2 須有負座標排列的案例。

##### 2.2 螢幕判定（子票 .1）

```swift
/// 判定結果。以型別區分三條語意不同的路徑，呼叫端無須再比對回傳值
/// 即可決定是否記 E_CL_SCREEN、是否可播放。
/// index 為 CursorScreenSnapshot.entries 的索引。
enum CursorScreenResolution: Equatable {
  /// 找到 frame 涵蓋游標的螢幕（SPEC-008 FR-02 正常路徑）。
  case matched(index: Int)
  /// 無涵蓋螢幕，退回 main（EX-06-02 復原路徑，呼叫端須記 E_CL_SCREEN）。
  case fellBackToMain(index: Int)
  /// 連 main 都取不到（無可用螢幕）。呼叫端視為不可播放。
  case unavailable
}

enum CursorScreenLocator {
  /// 純函式：不讀取任何全域狀態，輸出只由兩個參數決定。
  static func resolve(
    cursorLocation: NSPoint,
    in snapshot: CursorScreenSnapshot
  ) -> CursorScreenResolution
}
```

判定語意（Phase 2 須逐項出案例）：

| 條件 | 回傳 |
|------|------|
| 存在 `frame` 涵蓋 `cursorLocation` 的 entry | `.matched(index:)`，取索引最小的命中者 |
| 無涵蓋者且 `mainIndex != nil` | `.fellBackToMain(index: mainIndex!)` |
| 無涵蓋者且 `mainIndex == nil`（含 `entries` 為空） | `.unavailable` |
| 多個 entry 同時涵蓋（重疊排列） | `.matched` 取索引最小者，不做面積或距離仲裁 |

涵蓋判定採 `NSRect.contains(_:)` 的半開區間語意：`minX <= x < maxX`、`minY <= y < maxY`。故相鄰螢幕的共用邊界座標歸屬**該座標等於其 `minX`／`minY` 的那個螢幕**，不會同時命中兩個，也不會兩個都不命中。此語意為契約的一部分，Phase 2 須有邊界座標案例鎖定（對應「場景 3 邊界座標」）。

> **PM 修訂（Phase 1.5，2026-07-31）**：本段原句為「故相鄰螢幕的共用邊界座標歸屬左／下側螢幕」，與自身的半開區間定義及 §4 場景 3 相反——代入 `[0,1920)` 與 `[1920,3840)`，x=1920 對前者為 `0 <= 1920 < 1920` 即 false、對後者為 `1920 <= 1920 < 3840` 即 true，故歸屬後者。原句由 linux 與 basil 兩個視角獨立指出，一致性視角的交叉檢查表則將此格誤判為「一致」。修訂改以 API 語意描述取代方向詞：方向隨座標系而變，`NSRect.contains(_:)` 的語意不變。教訓已記錄為 `PC-SCLK-006`。（本段原亦寫作 `NSPointInRect`，與同節形式定義所用符號不一致，屬該 pattern 的弱化版，已於 Layer 2 審查後統一為 `NSRect.contains(_:)`。）

##### 2.3 特效視窗控制器（子票 .2）

```swift
/// 一次播放的請求參數。由 bridge 從 channel 參數轉換而來。
struct CursorLocatorPlayRequest: Equatable {
  let duration: TimeInterval   // 由 durationMs 轉秒
  let tint: NSColor            // 由 tintArgb 轉色
}

/// 控制器對外可觀測的播放狀態。surface 是否存活由本型別唯一表示：
/// .idle 等價於「無 surface」，.playing 等價於「恰有一個 surface」。
enum CursorLocatorPlaybackState: Equatable {
  case idle
  /// screenFrame 為 surface 當下所在螢幕的 frame，使「判定結果是否真的
  /// 被拿去開視窗」可被斷言（Phase 1.5 P0-5）。
  case playing(elapsed: TimeInterval, duration: TimeInterval, screenFrame: NSRect)
}

enum CursorLocatorError: Error, Equatable {
  /// 特效視窗建立失敗（EX-06-03）。錯誤碼字面 E_CL_WINDOW。
  case windowCreationFailed(underlying: String)
  /// 無任何可用螢幕（CursorScreenResolution.unavailable）。錯誤碼字面 E_CL_SCREEN。
  case noAvailableScreen
}

final class CursorLocatorEffectController {
  var state: CursorLocatorPlaybackState { get }

  /// 觸發或重置播放。
  /// - .idle 時建立 surface 並開始播放
  /// - .playing 時「重置」既有 surface：elapsed 歸零、tint/duration 換為新
  ///   請求、alpha 復位為 1、必要時搬遷至新目標螢幕；不建立第二個
  ///   surface（UC-06 替代場景 06b）
  /// - throws CursorLocatorError.windowCreationFailed / .noAvailableScreen
  func play(_ request: CursorLocatorPlayRequest) throws

  /// 立即結束播放並釋放 surface（app 結束或測試 teardown 用）。
  /// 後置條件：state == .idle、surface 已 close 且控制器不再持有、
  /// 後續在途幀一律被忽略（見「時間與執行緒契約」的 generation token）。
  /// .idle 時為 no-op，不 throw。
  func stop()
}
```

> **移除 `activeSurfaceCount`**（Phase 1.5 P0-2，採 linux 裁定）。該屬性與 `state` 為同一位元存兩份，且計數歸零只證明控制器不再持有、證不了 `NSWindow` 已 dealloc，留著會讓人誤以為 acceptance 5 已被測到。改由測試端斷言：spy surface 的 `close()` 呼叫次數，加上對 spy 的 `weak var` 參照在控制器釋放後為 nil。

##### 2.4 注入依賴

四個外部依賴一律經建構子注入，production 傳 AppKit 實作、XCTest 傳定樁。單一方法的依賴降級為 closure，不開具名協定（Phase 1.5 次要修訂，cinnamon + linux 一致）：

```swift
/// 取得當下螢幕幾何快照。production 讀 NSScreen.screens / NSScreen.main。
typealias CursorScreenSnapshotProviding = () -> CursorScreenSnapshot

/// 取得當下游標位置。production 讀 NSEvent.mouseLocation。
typealias CursorLocationSampling = () -> NSPoint

/// 建立 surface。production 實作建立 NSWindow 並套用「視窗屬性契約」；
/// 建立失敗時 throw，觸發 E_CL_WINDOW 路徑（採 throw 而非回傳 nil：
/// 失敗原因需帶字串進 FlutterError.details）。
typealias CursorLocatorSurfaceMaking = (NSRect) throws -> CursorLocatorSurface

/// 逾時保險：安排一次性回呼，回傳取消用的 closure。
/// production 為 DispatchQueue.main.asyncAfter，測試為手動觸發樁。
typealias CursorLocatorDeadlineScheduling =
  (TimeInterval, @escaping () -> Void) -> () -> Void
```

```swift
/// 特效視窗的最小抽象。隔開 AppKit 視窗伺服器，使生命週期可在無視窗環境下測。
protocol CursorLocatorSurface: AnyObject {
  /// renderer 繪製的目標圖層。renderer 只碰這一層。
  var contentLayer: CALayer { get }
  func move(toScreenFrame frame: NSRect)
  /// 整個視窗的不透明度（production 對應 NSWindow.alphaValue，非圖層屬性）。
  /// 職責邊界見「淡出契約」。
  func setAlpha(_ value: CGFloat)
  func close()
}

/// 逐幀驅動。production 以 CVDisplayLink 實作，綁定指定 displayID 以對齊
/// 該螢幕更新率（SPEC-008 FR-03）。測試以手動 driver 推進時間，
/// 不使用 wall clock、不使用 expectation 等待（避免 flaky）。
protocol CursorLocatorFrameDriving: AnyObject {
  /// onFrame 保證於 main thread 呼叫；production 實作自行從 display link
  /// 執行緒派回，呼叫端不需知道底層是 CVDisplayLink。
  /// 參數為單調遞增的絕對時間戳（production 用 CACurrentMediaTime()）。
  func start(displayID: CGDirectDisplayID, onFrame: @escaping (CFTimeInterval) -> Void)
  /// 跨螢幕搬遷後重綁 CGDirectDisplayID。未重綁則更新率仍對齊舊螢幕，
  /// 違反 FR-03（Phase 1.5 P0-12）。播放中可多次呼叫。
  func retarget(displayID: CGDirectDisplayID)
  /// 盡力停止。返回後仍可能有一幀在途，由控制器的 generation token 攔下。
  func stop()
}
```

##### 2.5 視窗屬性契約（production `CursorLocatorSurfaceMaking` 須逐項滿足）

| 屬性 | 契約值 | 理由 |
|------|--------|------|
| `styleMask` | `.borderless` | 無標題列、無邊框 |
| `level` | `.screenSaver` | 高於全螢幕內容（UC-06 06d、SPEC-008 決策 1） |
| `collectionBehavior` | `[.canJoinAllSpaces, .fullScreenAuxiliary, .stationary, .ignoresCycle]` | 跨 Space 可見、可疊在全螢幕 Space 之上、不隨 Space 動畫位移、不進 Cmd+Tab 循環 |
| `ignoresMouseEvents` | `true` | 底下應用可持續操作（UC-06 成功保證） |
| `canBecomeKey` | `false`（覆寫回傳 false） | 不搶焦點 |
| 顯示方式 | `orderFrontRegardless()` | **不得用 `makeKeyAndOrderFront(_:)`**：後者會使全螢幕播放中觸發熱鍵導致退出全螢幕，違反 UC-06 成功保證與 06d（Phase 1.5 P0-8） |
| `isOpaque` | `false` | 透明底 |
| `backgroundColor` | `.clear` | 同上 |
| `hasShadow` | `false` | 無視覺干擾 |
| `alphaValue` | 建立時 `1.0` | 淡出起點 |
| `releasedWhenClosed` | `false` | ARC 下若為 true，`close()` 後的參照為 over-release；且 weak 斷言需物件由 ARC 管理 |
| `frame` | 等於目標螢幕 `frame` | 覆蓋整個目標螢幕 |

acceptance 3 的驗證方式因此由「僅實機」改為 **XCTest 斷言上表各值 + 實機僅驗視覺覆蓋**（Phase 1.5 P0-6；原稿「無法單元測」的自陳不成立）。

##### 2.6 淡出契約

淡出**含在 `duration` 之內**，最後 `fadeDuration = min(0.2, duration * 0.4)` 秒為淡出窗：`alpha` 由 1.0 線性降至 0.0，`elapsed >= duration` 時 surface 關閉。故單次播放的總時長恆等於使用者設定值。

擇「含在內」而非「時長之後再淡出」的理由：FR-06 的時長範圍下限為 0.5 秒，外加固定 200 ms 淡出等於在最短設定上超出 40%，使用者設定的數字與實際觀察不符；且「總時長 == 設定值」是可直接斷言的不變量，「設定值 + 未載入 SPEC 的常數」不是。

> **與 UC-06 主場景 5 字面的偏離**：UC-06 寫「依使用者設定的特效時長**後**整層淡出」，字面讀為淡出在時長之外。本節取「含在內」，屬規格字面偏離，須由 PM 決定是修 UC-06／SPEC-008 措辭或推翻本裁定。Phase 2 應以本節契約設計案例，若 PM 裁定相反，僅需改 `fadeDuration` 的起算基準，介面不變。

淡出的執行歸屬見下方「衝突裁定：`CursorLocatorSurface` 的淡出方法存廢」。

##### 2.7 時間與執行緒契約

| 項目 | 契約 |
|------|------|
| `onFrame` 參數語意 | **絕對時間戳**（單調遞增，production 為 `CACurrentMediaTime()`），非幀間隔 |
| `elapsed` 計算 | `timestamp - startTimestamp`，`play` 當下記錄起點 |
| 結束判定 | `elapsed >= duration` 即結束。睡眠喚醒後時間戳大幅前進只會**提早**結束，不會停滯 |
| 逾時保險 | `play` 時經 `CursorLocatorDeadlineScheduling` 安排 `duration + 0.5` 秒的一次性回呼；到期時若仍在播放則強制結束並釋放。涵蓋顯示器休眠導致 CVDisplayLink 停擺、幀完全不再送達的情形——此為 acceptance 5 的真實洩漏向量，手動 driver 測不到，故以獨立注入點覆蓋 |
| 執行緒 | 所有公開方法與 `onFrame` 皆於 main thread；控制器不做鎖 |
| 過期幀 | 控制器持有 `generation: UInt64`，`play` / `stop` 各遞增一次；`onFrame` 閉包捕捉當下 generation，不符即直接返回 |

`stop()` 的後置條件以 generation token 保證，而非要求 driver 的 `stop()` 同步「返回後不再有回呼」——後者在 CVDisplayLink 的獨立執行緒模型下需阻塞等待，而 `stop()` 可能由 `onFrame` 內部（播放自然結束）呼叫，阻塞即自我死結。token 攔截讓在途幀對已 `close()` 的 surface 呼叫 `move` / `setAlpha` 這條 CVDisplayLink 典型崩潰路徑結構性消失（Phase 1.5 P0-11 的第二個選項）。

##### 2.8 每幀行為

`onFrame` 回呼內依序：

1. generation 不符 → 返回
2. 重建螢幕快照（`CursorScreenSnapshotProviding`）、取樣游標（`CursorLocationSampling`）
3. `CursorScreenLocator.resolve` 重新判定目標螢幕
   - `.matched` / `.fellBackToMain` 且 frame 與當前 surface 所在不同 → `surface.move(toScreenFrame:)` + `driver.retarget(displayID:)`；`.fellBackToMain` 另記 E_CL_SCREEN log，不中斷播放
   - 同一螢幕 → 不呼叫 `move`，避免每幀搬遷
   - `.unavailable`（播放中螢幕數歸零，如闔蓋）→ 立即結束：停 driver、`close()`、狀態回 `.idle`，記 E_CL_SCREEN log
4. 計算 `elapsed`；落在淡出窗內則 `surface.setAlpha(...)`
5. `elapsed >= duration` → 停 driver、取消逾時保險、`close()` surface、狀態回 `.idle`

顯示器熱插拔因此不需要額外的 `didChangeScreenParameters` 訂閱：每幀重建快照已涵蓋「目標螢幕被拔除」（重判定後搬遷）與「全部螢幕消失」（`.unavailable` 結束）兩條路徑，且完全以注入依賴可測（Phase 1.5 P0-9）。

##### 2.9 視覺特效的擴充點（本票不實作）

FR-03/04/05 三種特效於 1.4.0-W3-001 各自實作 renderer 協定，由控制器建構子以陣列注入；本票注入空陣列即可讓生命週期完整運作。

**本票只宣告擴充點存在與注入形狀，不鎖定方法簽章**，Phase 2 亦不為其撰寫呼叫序列的 spy 測試（Phase 1.5 次要修訂，linux P2）：為尚未實作的功能預先鎖定呼叫順序，W3-001 動手時多半得先拆掉那些測試。控制器對 renderer 的呼叫時機屬 W3-001 的 Phase 1 職責。

因此原稿「場景 5 以 spy renderer 斷言 `reset()` 與未收到第二次 `attach()`」改為以控制器自身可觀測狀態斷言（見場景 7）。

##### 2.10 MainFlutterWindow.swift 的唯一改動

`CursorLocatorBridge` 持有一個 `CursorLocatorEffectController`，`handlePlay` 在既有參數解析成功之後，把 `NSLog + result(nil)` 換成委派：

```swift
do {
  try controller.play(CursorLocatorPlayRequest(duration: ..., tint: ...))
  result(nil)
} catch { /* 依「錯誤路徑的介面歸屬」決定回傳 */ }
```

該檔不新增任何其他型別與職責（現 836 行，已超 cognitive-load 300 行閾值 2.8 倍）。channel 名／方法名／參數鍵沿用既有 `CursorLocatorChannel` enum，不重複定義。

#### 三、衝突裁定：`CursorLocatorSurface` 的淡出方法存廢

Phase 1.5 兩份審查互斥：linux 主張刪除（surface 已交出 `contentLayer`，淡出是 renderer 對圖層做的事，留著等於兩處都能改透明度）；Plan 主張保留（UC-06 主場景 5 要求「整層淡出」，而本票 renderer 為 no-op，刪除則無人執行淡出）。

**裁定：保留，但改名為 `setAlpha(_:)` 並重新定義為視窗層級屬性（`NSWindow.alphaValue`），非圖層 opacity。**

判準與理由：

1. **「整層淡出」在語意上就不是單一特效的職責**。三種特效各有自己的動畫曲線（聚光燈壓暗、邊框閃三次、波紋擴散），若淡出歸 renderer，等於同一份淡出排程要在三個 renderer 內各實作一次，且日後新增特效必須記得補；而它們共同的收尾行為只有一種可能結果——整個視窗消失。這屬 surface 生命週期的收尾階段，與 `close()` 同一層。
2. **linux 指出的職責重疊確實存在，但根因是原命名而非該能力存在**。原 `setOpacity` 是圖層語彙，與 renderer 能碰的 `contentLayer.opacity` 同名同層，故看起來「兩處都能改」。改為 `setAlpha` 並綁定 `NSWindow.alphaValue` 後，職責邊界是型別層級的：renderer 只拿得到 `contentLayer`，碰不到視窗；surface 只動視窗整體 alpha，不動內容。重疊消失，能力保留。
3. 附帶效益：`NSWindow.alphaValue` 對整個視窗合成一次，比讓三個 renderer 各自對圖層做透明度動畫更省，且不會出現三層淡出速率不一致的視覺瑕疵。

本裁定使 UC-06 主場景 5 由本票交付，不改掛 W3-001；驗收對應表已據此列入「場景 12 淡出」。

#### 四、錯誤路徑的介面歸屬

| 場景 | 錯誤碼 | 介面表現 | play 的 channel 回傳 |
|------|--------|---------|---------------------|
| 目標螢幕解析失敗（EX-06-02） | E_CL_SCREEN | `.fellBackToMain`，控制器照常播放 | `result(nil)`，視為成功；僅 NSLog |
| 無任何可用螢幕 | E_CL_SCREEN | `.unavailable` → `throw .noAvailableScreen` | `result(FlutterError(code: "E_CL_SCREEN", ...))` |
| 特效視窗建立失敗（EX-06-03） | E_CL_WINDOW | 工廠 throw → `throw .windowCreationFailed` | `result(FlutterError(code: "E_CL_WINDOW", ...))` |
| 播放中每幀重判定退回 main | E_CL_SCREEN | 僅 NSLog，不中斷、不改變 state | 該次 play 早已回傳，無影響 |
| 播放中螢幕數歸零 | E_CL_SCREEN | 立即結束並釋放，僅 NSLog | 該次 play 早已回傳，無影響 |

判準（與 Problem Analysis「規格落差澄清」一致）：**復原路徑不以 FlutterError 中斷播放**；所有「回報 Dart 側」一律為本次 `play` 呼叫的同步 `FlutterError` 回應，不新增原生至 Dart 的反向 channel 或 EventChannel。會到達 Dart 的 E_CL_SCREEN 語意唯一——無可用螢幕。

`noAvailableScreen` 屬 SPEC-008 未列舉的殘餘情形（螢幕數為 0），歸入 E_CL_SCREEN 而非新增錯誤碼，避免擴張 SPEC 定義的錯誤碼集合。

#### 五、GWT 行為場景

場景編號沿用初版以保持既有引用可解析，另附語意名稱供錨定。

**判定層（子票 .1，純函式）**

- **場景 1（正常判定）**：Given 兩個不重疊 entry 的快照、游標落在第二個的 frame 內；When `resolve`；Then `.matched(index: 1)`。
- **場景 2（間隙退回）**：Given 兩 entry 間存在未涵蓋座標、`mainIndex == 0`；When 以間隙座標 `resolve`；Then `.fellBackToMain(index: 0)`。
- **場景 3（邊界座標）**：Given 兩螢幕於 x=1920 相鄰；When 游標恰在 x=1920；Then 命中右側螢幕（半開區間語意），且僅命中一個。
- **場景 3b（負座標排列）**：Given 第二螢幕位於主螢幕左方（frame origin.x 為負）或上方；When 游標落於第二螢幕；Then `.matched` 指向第二螢幕。
- **場景 3c（無可用螢幕）**：Given `entries` 為空且 `mainIndex == nil`；When `resolve`；Then `.unavailable`。
- **場景 3d（重疊排列）**：Given 兩 entry 的 frame 互相重疊且游標落於重疊區；When `resolve`；Then `.matched` 取索引較小者。

**生命週期層（子票 .2，注入依賴）**

- **場景 4（接線：目標螢幕真的被拿去開視窗）**：Given 游標位於螢幕 B；When `play`；Then `CursorLocatorSurfaceMaking` 收到的引數恰為 B 的 frame 且僅被呼叫一次，`state.screenFrame == B.frame`。（缺此場景時，判定正確但接線永遠傳 main 仍會全綠。）
- **場景 5（單次播放釋放）**：Given `.idle` 與手動 driver；When `play(duration: 1.5)` 後推進時間戳至 +1.5；Then 過程中 `state` 為 `.playing`，結束後 `state == .idle`、spy surface 的 `close()` 恰被呼叫一次。
- **場景 6（釋放後不再持有）**：Given 場景 5 的 spy surface 以 `weak var` 額外持有；When 播放結束並釋放測試端強參照；Then weak 參照為 nil（證 dealloc，非僅計數歸零）。
- **場景 7（06b 重複觸發為重置）**：Given 播放進行至 elapsed 0.8；When 再次 `play`；Then `CursorLocatorSurfaceMaking` 仍只被呼叫過一次、`state.elapsed` 歸零、`state.duration` 為新請求值、surface 的 `close()` 未被呼叫、`setAlpha(1.0)` 復位。
- **場景 8（播放中連按十次）**：Given 播放中；When 於同一次播放期間連續 `play` 十次且期間不推進至結束；Then `CursorLocatorSurfaceMaking` 呼叫次數恆為 1、`close()` 呼叫次數恆為 0、`state` 始終為 `.playing`。
- **場景 9（十次完整播放無洩漏）**：Given 手動 driver；When 連續十次 `play` 並各推進至結束；Then 每次結束後 `state == .idle`、`close()` 累計次數為 10、`CursorLocatorSurfaceMaking` 累計次數為 10，且各次 surface 的 weak 參照皆為 nil。
- **場景 10（06a 跨螢幕跟隨）**：Given 游標取樣序列由螢幕 A 切至螢幕 B；When 推進數幀；Then surface 收到 `move(toScreenFrame: B.frame)` 恰一次（同螢幕連續幀不重複搬遷），driver 收到 `retarget(displayID: B.displayID)` 恰一次。
- **場景 11（熱插拔：目標螢幕消失）**：Given 播放中，快照 provider 於第 N 幀起移除當前目標螢幕；When 推進至第 N 幀；Then surface 搬遷至重判定結果的 frame，播放不中斷。
- **場景 12（熱插拔：螢幕數歸零）**：Given 播放中，快照 provider 於第 N 幀起回傳空 entries 且 `mainIndex == nil`；When 推進至第 N 幀；Then 立即 `close()`、`state == .idle`，且不因此 throw（`play` 早已返回）。
- **場景 13（淡出）**：Given `play(duration: 1.5)`；When 推進至 1.3 之後的各幀；Then `setAlpha` 收到單調遞減且落於 (0, 1) 的值，1.5 前的最後一次為趨近 0；1.3 之前不曾收到小於 1.0 的值。
- **場景 14（逾時保險）**：Given `play` 後手動 driver **完全不送幀**（模擬顯示器休眠導致 CVDisplayLink 停擺）；When 觸發 deadline 樁；Then `close()` 被呼叫、`state == .idle`。
- **場景 15（stop 於播放中）**：Given 播放中；When `stop()`；Then `close()` 恰一次、`state == .idle`、deadline 已取消。
- **場景 16（stop 於 idle）**：Given `.idle`；When `stop()`；Then 無任何 surface 互動、不 throw、`state` 維持 `.idle`。
- **場景 17（stop 後在途幀）**：Given 播放中呼叫 `stop()`；When 對已關閉的 surface 再送一幀（模擬 CVDisplayLink 在途回呼）；Then 該幀被 generation token 忽略——`move` / `setAlpha` / `close` 皆未再被呼叫。
- **場景 18（duration <= 0）**：Given `play(duration: 0)`；When 推進首幀；Then surface 建立後於首幀即 `close()`、`state == .idle`、不 throw。

**錯誤路徑**

- **場景 19（EX-06-03）**：Given 必定 throw 的 `CursorLocatorSurfaceMaking`；When `play`；Then 拋出 `.windowCreationFailed`、`state` 維持 `.idle`、bridge 以 `FlutterError(code: "E_CL_WINDOW")` 回傳。
- **場景 20（退回 main 不中斷）**：Given 游標落於間隙；When `play`；Then 於 main 的 frame 建立 surface、`play` 不 throw、bridge 回傳 `result(nil)`。
- **場景 21（無可用螢幕）**：Given 快照為空且無 main；When `play`；Then 拋出 `.noAvailableScreen`、`CursorLocatorSurfaceMaking` 未被呼叫、bridge 以 `FlutterError(code: "E_CL_SCREEN")` 回傳。

**視窗屬性（app-hosted XCTest，對真實 NSWindow）**

- **場景 22（視窗屬性契約）**：Given production 的 surface 工廠；When 建立 surface；Then §2.5 表中各屬性逐項符合契約值，且視窗未成為 key window。

#### 六、邊界條件與約束

| 項目 | 契約 |
|------|------|
| `duration <= 0` | 首幀即結束，不 throw（Dart 側已由 FR-06 範圍 0.5~3.0 約束，原生層不重複驗證但須有定義行為） |
| `entries` 順序 | 命中取索引最小者，依賴傳入順序；production 直接傳 `NSScreen.screens` 不重排 |
| 同螢幕連續幀 | 不重複呼叫 `move` / `retarget` |
| 不變量 | `state == .idle` 等價於「控制器不持有任何 surface」，於所有公開方法前後成立 |
| `stop()` 於 idle | no-op，不 throw |
| 執行緒 | 公開方法與 `onFrame` 皆於 main thread；控制器不做鎖 |
| 純函式無全域 | `CursorScreenLocator.resolve` 不得讀 `NSScreen.screens` / `NSEvent.mouseLocation`，取樣責任在呼叫端 |
| 座標系 | 全域 AppKit 左下原點，容許負值；不得與 CGWindowList 左上原點混用 |
| 逾時保險寬限 | `duration + 0.5` 秒；此值為原生層自保門檻，不對使用者可見，不進 SettingsModel |

#### 七、驗收標準對應與 UC-06 覆蓋範圍

| 母票 acceptance | 驗證方式 | 對應場景 |
|-----------------|---------|---------|
| 1 游標所在螢幕建立特效視窗 | XCTest（判定 + 接線）+ 實機（視窗實際出現） | 1、3b、3c、3d、4 |
| 2 播放期間跨螢幕跟隨 | XCTest（`move` + `retarget` 呼叫）+ 實機（視覺） | 10、11 |
| 3 顯示於假全螢幕之上 | XCTest（app-hosted 屬性斷言）+ 實機（視覺覆蓋） | 22 |
| 4 重複觸發為重置非疊加 | XCTest | 7、8 |
| 5 播放結束後確實釋放、十次無洩漏 | XCTest（`close()` 計數 + weak dealloc + 逾時保險） | 5、6、9、14、15、17 |
| 6 兩類失敗有日誌並回報 | XCTest（throw 與 channel 回傳型別）+ 人工檢視 log | 2、19、20、21 |

UC-06 場景覆蓋與歸屬：

| UC-06 項目 | 歸屬 | 說明 |
|-----------|------|------|
| 主場景 2 判定目標螢幕 | 本票 .1 | 場景 1/2/3 系列 |
| 主場景 3 建立特效視窗 | 本票 .2 | 場景 4、22 |
| 主場景 4 播放特效（三種視覺） | **1.4.0-W3-001** | 本票僅提供 `contentLayer` 與逐幀驅動 |
| 主場景 5 結束（整層淡出 + 釋放） | 本票 .2 | 場景 13、5、6（淡出裁定見第三節） |
| 06a 跨螢幕跟隨 | 本票 .2 | 場景 10 |
| 06b 重複觸發 | 本票 .2 | 場景 7、8 |
| 06c 功能停用 | **1.4.0-W2-005**（熱鍵註冊與生命週期） | 停用時解除熱鍵註冊，不經原生特效層 |
| 06d 全螢幕之上 | 本票 .2 | 場景 22 的 level / collectionBehavior |
| 06e 免授權 | 無程式碼落點 | SPEC-008 NFR-03 為 `hotkey_manager` 的既有性質 |
| EX-06-01 熱鍵註冊失敗 | **1.4.0-W2-005** | E_CL_HOTKEY 不經本票 |
| EX-06-02 螢幕解析失敗 | 本票 .1 + .2 | 場景 2、20 |
| EX-06-03 視窗建立失敗 | 本票 .2 | 場景 19 |

#### 八、Phase 1.5 修訂對照與待 PM 追蹤

| 修訂項 | 處置 |
|--------|------|
| P0-2 `activeSurfaceCount` | 依裁定刪除；改 `close()` 計數 + weak dealloc（場景 6、9） |
| P0-4 泛型與 `CursorHitTestableScreen` | 依裁定刪除；`resolve` 改吃 `CursorScreenSnapshot` 回 index。**與裁定的差異**：裁定寫「吃 `[NSRect]`」，本節包成含 `displayID` 的快照值型別——P0-12 的重綁需要 displayID，若只傳 `[NSRect]` 則控制器須另開一條取 displayID 的路徑，等於同一份螢幕資訊分兩次取樣、可能不一致。判定層仍只讀 `frame`，純度不變 |
| P0-5 接線無訊號 | 依裁定補場景 4，並讓 `state.playing` 帶 `screenFrame` 使斷言不需窺探私有欄位 |
| P0-6 視窗屬性契約 | 依裁定改 XCTest 屬性斷言，契約值列於 §2.5（12 項） |
| P0-7 淡出語意 | 定義淡出窗並裁定**含在 duration 內**；與 UC-06 字面偏離，須 PM 追蹤（見下） |
| P0-8 不搶焦點 | 依裁定 `canBecomeKey = false` + `orderFrontRegardless()`，列入 §2.5 |
| P0-9 熱插拔 | 依裁定定義兩條路徑；**實作手段與裁定不同**：不訂閱 `didChangeScreenParameters`，改為每幀重建快照。理由：播放期間本就逐幀取樣，重建快照已涵蓋熱插拔，另訂閱通知會引入第二條狀態來源與非同步時序，且通知路徑無法以注入依賴測 |
| P0-10 時間語意 | 明定為**絕對時間戳**。**與裁定的差異**：裁定要求「delta + 單幀上限 + wall clock 保險」，本節改用絕對時間戳，使「睡眠喚醒後 elapsed 永不達 duration」結構性不可能（時間戳只會前進），無須 delta clamp；另以逾時保險涵蓋裁定未指出的更嚴重情形——CVDisplayLink 完全停擺、一幀都不再送達，delta clamp 對此無效 |
| P0-11 執行緒後置條件 | 採裁定給的第二個選項（generation token）；理由見 §2.7 末段（要求 driver 同步保證會在「播放自然結束時由 onFrame 內呼叫 stop」路徑上自我死結） |
| P0-12 重綁 displayID | 依裁定於 driver 協定補 `retarget(displayID:)`，呼叫時機定於搬遷當幀 |
| 衝突 `setOpacity` 存廢 | 保留並改名 `setAlpha`，重新定義為視窗屬性；理由見第三節 |
| 次要：單方法協定降 closure | 已採（§2.4 四個 typealias） |
| 次要：renderer 不鎖簽章 | 已採（§2.9），連帶改寫原場景 5 的 spy 斷言 |
| 次要：章節標題中文化與語意錨點 | 已採；場景編號保留以使既有引用（含 PM 修訂區塊的「§4 場景 3」）仍可解析，另附語意名稱 |
| 次要：UC-06 未涵蓋場景歸屬 | 已補（第七節覆蓋表） |
| 次要：座標系契約 | 已補（§2.1）並加場景 3b |

**待 PM 追蹤事項**：

1. 淡出是否含在 `duration` 內，本節裁定「含在內」與 UC-06 主場景 5 字面相反。需 PM 決定修 UC-06／SPEC-008 措辭或推翻本裁定。屬規格層變更，不在本票範圍。
2. `.screenSaver` 是否足以覆蓋所有假全螢幕情境，本節僅承接 SPEC-008 決策 1 的既有結論，未獨立驗證；場景 22 只能證明屬性值正確，覆蓋效果仍需實機。

（產出者：lavender-interface-designer；本節依 Phase 1.5 六視角審查修訂於 2026-07-31，止於 Phase 1，未推進 Phase 2）
### TDD Phase 1.5 規格多視角審查彙整（PM，2026-07-31）

依 /parallel-evaluation 情境 G 派發 6 視角（Consistency / Completeness / CogLoad + 常駐 linux + basil + 語言代理人 parsley），共回收 64 項發現。

| 視角 | 代理人 | 高/P0 | 中 | 低 |
|------|--------|-------|-----|-----|
| Consistency | saffron-system-analyst | 0 | 3 | 2 |
| Completeness | Plan | 9 | 12 | 5 |
| CogLoad | cinnamon-refactor-owl | 1 | 1 | 3 |
| Good Taste（常駐） | linux | 2 | 2 | 3 |
| 文字品質（常駐） | basil-writing-critic | 2 | 4 | 7 |
| AppKit 實務 | parsley-flutter-developer | 3 | 2 | 1 |

#### 視角衝突與裁定

| 爭點 | cinnamon | linux | 裁定 | 依據 |
|------|----------|-------|------|------|
| `activeSurfaceCount` 存廢 | 列為命名優良的正面例（「命名即文件」） | 主張刪除：與 `state` 同位元存兩份、證不了 acceptance 5、測試已擁有全部 surface | **採 linux（刪除）** | parallel-evaluation 衝突處理：加法 vs 減法預設減法 + linux 品味否決權；且 parsley 從 ARC 角度獨立佐證「計數歸零 != dealloc」 |

#### PM 查證後排除（代理人標為風險但實證不成立）

| 原始發現 | 提出者 | 查證方式 | 結論 |
|---------|--------|---------|------|
| `FullscreenCoverageDetector` 可能誤判特效視窗為外來全螢幕應用 | parsley Q4（自標不確定） | 讀 `MainFlutterWindow.swift:219-225` | **不成立**。以 `pid == ownerPID` 排除本 app 全部視窗，非僅時鐘視窗；特效視窗同 process 必被跳過 |
| Podfile 可能缺 `RunnerTests` target 致連結失敗 | parsley Q5 | `grep -n target macos/Podfile` | **不成立**。第 33 行已有巢狀 `target 'RunnerTests' do` |
| `extension NSScreen: Equatable` 有 retroactive conformance 衝突風險 | parsley Q6 後半 | `grep -rn "extension NSScreen" macos/Runner/` | **不成立**。零命中，無既有 conformance |
| GWT 場景 3 與半開區間語意「一致」 | saffron 發現 7 交叉表 | 推演 `[0,1920)` 與 `[1920,3840)` 於 x=1920 | **saffron 誤判**。實為矛盾，見下 P0-1 |

#### P0 發現（必須於 Phase 2 之前解決）

| # | 發現 | 提出者 | 影響 |
|---|------|--------|------|
| P0-1 | §2.1 散文「邊界座標歸屬左／下側」與自身半開區間定義及 §4 場景 3「命中右側」相反 | linux + basil（獨立雙確認） | 契約自相矛盾。兩種寫法各自都能「測試通過」，紅綠燈不會自曝，只有一種符合 `NSPointInRect` |
| P0-2 | `activeSurfaceCount` 對 acceptance 5 為無效斷言 | linux + parsley + Plan L6 | 計數歸零僅證控制器不再持有，證不了 NSWindow dealloc。留著會讓人誤以為洩漏已被測到 |
| P0-3 | Problem Analysis「僅 E_CL_WINDOW 走 FlutterError」與 Phase 1 的 `noAvailableScreen` 亦走 FlutterError 互斥，且 Phase 1 以「依 Problem Analysis」措辭掩蓋偏離 | basil C-1 | 權威敘述互斥，Phase 2 無從判斷依據何者。**此為 PM 段落之誤**，修 Problem Analysis 而非修 Phase 1 |
| P0-4 | 泛型 `CursorScreenResolution<Screen>` + `CursorHitTestableScreen`：`NSScreen` 未合成 `Equatable`，照此片段編譯失敗 | cinnamon + linux + parsley（三視角獨立收斂） | 同時是過度設計與編譯缺口。改 `resolve` 吃 `[NSRect]` 回 index 可一次解掉三項發現 |
| P0-5 | acceptance 1 的接線無任何自動訊號 | Plan C1 | 場景僅驗純函式，無場景斷言「命中螢幕 frame 被當作 `makeSurface` 引數」。判定正確但接線永遠傳 main 仍全綠 |
| P0-6 | acceptance 3「無法單元測」自陳不成立，且視窗屬性契約值全部缺席 | Plan C2 + C3 | PM 已查證 `BUNDLE_LOADER=$(TEST_HOST)` + `ENABLE_TESTABILITY=YES`，RunnerTests 為 app-hosted，可斷言 level / collectionBehavior / ignoresMouseEvents。契約值缺席使實機驗證亦無 pass/fail 判準 |
| P0-7 | 淡出（UC-06 主場景 5）無契約，`setOpacity` 為孤兒 API | Plan U1 | 每幀行為描述完全未提 opacity。淡出起算點、時長、是否含在 duration 內皆未定 |
| P0-8 | 「不搶焦點」未定義 | Plan U3 | `ignoresMouseEvents` 只擋滑鼠。若誤用 `makeKeyAndOrderFront` 而非 `orderFrontRegardless`，全螢幕播放中觸發會導致退出全螢幕 |
| P0-9 | 顯示器熱插拔期間行為未定義 | Plan L2 | 播放中目標螢幕被拔除時 surface 停留於已消失的 frame。專案已有 `didChangeScreenParameters` 訂閱先例（`MainFlutterWindow.swift:164-171`）可循 |
| P0-10 | `onFrame` 的 `TimeInterval` 語意未定（delta vs 絕對時間戳），無跳幀 clamp 與 wall-clock 保險 | Plan L4 | 睡眠喚醒後可能 elapsed 永不達 duration，surface 永久殘留。這是 acceptance 5 的真實洩漏向量，手動 driver 測不到 |
| P0-11 | CVDisplayLink 回呼派回 main 為 sync 或 async 未定，`stop()` 無後置條件 | Plan R1 + parsley Q3 | async 派送時 stop 後仍有在途幀，對已 `close()` 的 surface 呼叫 `move`/`setOpacity`。CVDisplayLink 最典型崩潰來源 |
| P0-12 | CVDisplayLink 跨螢幕搬遷後未重綁 `CGDirectDisplayID` | Plan L5 | 協定只有 start/stop 無重綁介面點。違反 SPEC-008 FR-03「對齊螢幕更新率」 |

#### 結論：Phase 1 產出需修訂後才可作為 Phase 2 依據

tdd-flow 定義 Phase 1.5 的目的為「確保 Phase 1 產出可作為測試和實作的穩定依據」。12 項 P0 中，P0-1 / P0-3 為契約矛盾，P0-4 為編譯缺口，P0-6 至 P0-12 為契約缺席（淡出、焦點、熱插拔、時間語意、執行緒後置條件、顯示器重綁）。以現狀進 Phase 2 將產出建立在矛盾與空白之上，故判定為**不通過，需修訂**。

修訂範圍分流見下節「Phase 1 修訂指示」。Phase 2 職責範圍與延後追蹤項另立。
### Phase 1 修訂指示（PM，Phase 1.5 裁定後）

P0-1（邊界語意）與 P0-3（E_CL_SCREEN 敘述）已由 PM 直接修訂，見上方兩處「PM 修訂」引言區塊。以下 10 項交由 lavender-interface-designer 修訂 Phase 1 產出，每項附已裁定方向；裁定依據為 Phase 1.5 六視角報告，設計者若有不同判斷須在修訂稿說明理由，不得逕自忽略。

| # | 修訂項 | 已裁定方向 | 裁定來源 |
|---|--------|-----------|---------|
| P0-2 | `activeSurfaceCount` | **刪除**。與 `state` 為同一位元存兩份，且證不了 acceptance 5。改由測試端斷言：spy surface 的 `close()` 呼叫次數 + `weak var` 確認 dealloc | linux（品味否決權）；parsley 佐證 weak 斷言在 ARC 下確定性可靠、非 flaky |
| P0-4 | 泛型與 `CursorHitTestableScreen` | **刪除協定與泛型**。`resolve` 改吃 `[NSRect]`、回傳 index（`case matched(index: Int)` / `.fellBackToMain(index: Int)` / `.unavailable`），呼叫端以 index 回頭索引 `NSScreen.screens` | cinnamon + linux + parsley 三視角獨立收斂；同時解掉 `NSScreen` 未合成 `Equatable` 的編譯缺口 |
| P0-5 | acceptance 1 接線無訊號 | 補場景：Given 游標在螢幕 B；When play；Then `makeSurface` 收到 B 的 frame 恰一次。現有場景只驗純函式，接線永遠傳 main 也會全綠 | Plan C1 |
| P0-6 | acceptance 3 驗證方式與視窗屬性契約 | 「無法單元測」自陳**不成立**（PM 查證 `BUNDLE_LOADER=$(TEST_HOST)` + `ENABLE_TESTABILITY=YES`，RunnerTests 為 app-hosted）。改為 XCTest 屬性斷言 + 實機僅驗視覺；並在邊界條件表補齊契約值：window level、collectionBehavior 集合、`ignoresMouseEvents`、`isOpaque`、`backgroundColor`、`hasShadow`、`canBecomeKey` | Plan C2 + C3 |
| P0-7 | 淡出語意 | 定義淡出窗：起算點、時長、曲線、**是否含在 duration 內**（含在內則實際亮度時長短於使用者設定，不含則總時長超出設定，須擇一並說明）。UC-06 主場景 5 要求「整層淡出」，現行每幀行為描述完全未提 opacity | Plan U1 |
| P0-8 | 不搶焦點 | `canBecomeKey = false`，以 `orderFrontRegardless()` 顯示而非 `makeKeyAndOrderFront(_:)`。誤用後者會使全螢幕播放中觸發熱鍵導致退出全螢幕，違反 UC-06 成功保證與 06d | Plan U3 |
| P0-9 | 顯示器熱插拔 | 定義每幀重判定時目標螢幕已消失的行為（搬遷或退回 main），以及播放中螢幕數歸零（合蓋）時立即結束並釋放。專案已有 `didChangeScreenParameters` 訂閱先例可循（`MainFlutterWindow.swift:164-171`）。此路徑以注入依賴完全可測，不需實機 | Plan L2 |
| P0-10 | `onFrame` 時間語意 | 明定參數為幀間隔 delta 或絕對時間戳；加入單幀 delta 上限與以 wall clock 為準的結束保險。睡眠喚醒後 elapsed 永不達 duration 會使 surface 永久殘留，是 acceptance 5 的真實洩漏向量，手動 driver 測不到 | Plan L4 |
| P0-11 | 執行緒後置條件 | 於 `CursorLocatorFrameDriving` 協定的 doc comment 明定：`onFrame` 保證於 main thread 呼叫（production 實作自行派回，呼叫端不需知道底層是 CVDisplayLink）；`stop()` 返回後保證不再收到 `onFrame`，或由控制器以 generation token 忽略過期幀。async 派送下 stop 後的在途幀對已 `close()` 的 surface 呼叫是 CVDisplayLink 典型崩潰來源 | Plan R1 + parsley Q3 |
| P0-12 | 跨螢幕重綁 displayID | 協定補重綁介面點（如 `setTargetScreen(_:)`）並定義呼叫時機。CVDisplayLink 綁定特定 `CGDirectDisplayID`，搬遷至不同更新率螢幕後未重綁則違反 SPEC-008 FR-03「對齊螢幕更新率」 | Plan L5 |

#### 待設計者裁定的審查衝突

`CursorLocatorSurface.setOpacity(_:)` 的存廢，兩份審查給出互斥建議：

- **linux（P2）**：刪除。surface 已把 `contentLayer` 交出去，淡出是 renderer 對圖層做的事；留著等於兩處都能改透明度，職責重疊
- **Plan（U1）**：保留並補契約。UC-06 主場景 5 要求「**整層**淡出」，而本票的 renderer 注入為 no-op（三種視覺特效留 W3-001）；若淡出歸 renderer，則本票交付物無人執行淡出，UC-06 主場景 5 落空

PM 不代為裁定介面歸屬。設計者須擇一並說明理由，判準建議為：「整層淡出」是否屬於單一特效的職責，或屬於 surface 生命週期的收尾階段。若判定歸 renderer，須同時說明本票如何滿足 UC-06 主場景 5，或明確將該場景改掛 W3-001 並記錄於驗收對應表。

#### 次要修訂（與上述一併處理）

- `CursorLocatorSurfaceProviding` 與 `CursorLocationSampling` 為單一方法協定，降級為 closure 型別（`(NSRect) throws -> CursorLocatorSurface`、`() -> NSPoint`），減少具名符號與測試樁類別（cinnamon + linux 一致）
- `CursorLocatorEffectRendering` 保留擴充點宣告，但**不在本票鎖定四方法簽章**；Phase 2 亦不撰寫其呼叫序列的 spy 測試——為尚未實作的功能預先鎖定呼叫順序，W3-001 動手時多半得先拆掉這些測試（linux P2）
- 節標題語言統一為中文語意標題（`Purpose` / `API Signatures` / `Decision Questions` 為 generic heading，grep 錨點弱且與語言約束規則不一致）；章節與場景的引用改用語意錨點而非序號，避免插入新節後引用靜默指向錯誤內容（basil W-1 / W-4）
- 補列本票不涵蓋的 UC-06 場景及其歸屬票號（06c 功能停用、06e 免授權、EX-06-01 熱鍵註冊失敗），使覆蓋範圍可追溯（Plan U7）
- 補座標系契約：所有 `NSPoint` / `NSRect` 為 AppKit 全域座標系（原點在主螢幕左下角，**y 軸向上為正**）。第二螢幕的 frame 於置在主螢幕左方時 x 為負、置在下方時 y 為負；置在上方時 y 為正。Phase 2 須有負座標排列案例（Plan U8）

> **PM 修訂（Phase 2 後，2026-07-31）**：本條原寫「第二螢幕置於主螢幕**上方或左方**時 frame 座標為負」，方向有誤——AppKit 全域座標 y 軸向上，上方螢幕的 y 為正，負 y 來自下方。由 sage-test-architect 於 Phase 2 設計 fixture 時發現並更正，屬 fixture 事實更正，不影響介面契約。
>
> **此為 `PC-SCLK-006` 在同一張 ticket 內的二度發生**，且第二次由記錄該 error-pattern 的 PM 本人所犯（撰寫時間晚於 PC-SCLK-006 建檔）。兩次皆為方向詞：第一次是「邊界座標歸屬左／下側」（Phase 1），第二次是本條。這證實該 pattern 的防護不能依賴「知道要小心」——記得規則的人在轉述他人建議時仍會犯，因為方向詞的錯誤不會被上下文的正確性擋下。可行的防護只有 PC-SCLK-006 已載明的那一條：**改為引用不變的參照**（API 語意或座標軸定義），不以左右上下描述。
### Phase 1.5 收尾與驗收（PM，2026-07-31）

**修訂驗收結果：通過。** 獨立查證（非採信代理人自陳）：PM 修訂區塊 2 個完好、Phase 1 節僅存 1 份（確為原地改寫非追加）、介面定義中已無 `activeSurfaceCount` / `CursorHitTestableScreen` / `setOpacity`（殘留 13 處命中全屬修訂說明與本彙整節的歷史提及）、GWT 場景由 9 擴為 22。

**三項偏離裁定經覆核全部成立且優於原裁定**：

| 項 | 偏離內容 | 覆核結論 |
|---|---------|---------|
| P0-4 | 不吃 `[NSRect]`，改含 `displayID` 的 `CursorScreenSnapshot` | 成立。P0-12 重綁需 displayID，只傳 frame 會使控制器另開取樣路徑，同一份螢幕資訊兩次取樣可能不一致 |
| P0-9 | 不訂閱 `didChangeScreenParameters`，改每幀重建快照 | 成立。播放期間本就逐幀取樣，通知會引入第二條狀態來源與非同步時序且無法以注入依賴測 |
| P0-10 | 不用 delta + clamp，改絕對時間戳 | 成立且更強。時間戳單調前進使「喚醒後 elapsed 永不達 duration」結構性不可能；並補上原裁定未涵蓋的更嚴重情形——CVDisplayLink 完全停擺不送幀時 delta clamp 不會被觸發 |

**衝突項裁定（`setOpacity` 存廢）採納**：設計者判定衝突根因為命名而非能力，改綁 `NSWindow.alphaValue` 並更名 `setAlpha`，使職責邊界成為型別層級（renderer 只取得 `contentLayer` 碰不到視窗，surface 只動視窗 alpha 不動內容）。linux 的職責重疊訴求與 Plan 的 UC-06 主場景 5 交付訴求同時滿足。UC-06 主場景 5 由本票交付，不改掛 1.4.0-W3-001。

#### 設計者交回的兩項待 PM 處理

**待追蹤 1（淡出語意與 UC-06 字面相反）——已處置，規格已修**

設計者採「淡出含在 duration 內」（淡出窗 `min(0.2s, duration*0.4)`），與 UC-06 主場景 5 原字面「時長**後**淡出」相反。經用戶裁示採「含在內」，理由：時長下限 0.5 秒若外加 200 ms 淡出將偏離設定值 40%，且「總時長 == 設定值」才是可斷言的不變量。

已同步修訂兩份規格（依 reference-stability-rules 規則 7，規格文件不引用 ticket ID，故修訂內容僅陳述規格本身）：

- `docs/usecases/UC-06-locate-cursor.md` 主場景 5 → v1.1：「特效總時長為使用者設定值，末段的整層淡出包含在此時長內；淡出完成後特效視窗釋放」
- `docs/spec/user-experience/SPEC-008-cursor-locator.md` FR-06 約束條件 → v1.1：新增「特效時長為含淡出的總時長」條款與理由，變更歷史已記錄

**待追蹤 2（`.screenSaver` 是否足以覆蓋所有假全螢幕情境）——不另建票，理由如下**

設計者指出本節僅承接 SPEC-008 決策 1 的 `.screenSaver`，未獨立驗證其對 native fullscreen（綠燈鈕）、假全螢幕影片、簡報播放三種情境的實際覆蓋效果；場景 22 只斷言屬性值等於契約值，不證明視覺上真的蓋住。

PM 判定此風險已被 acceptance 3 的實機驗證涵蓋（Phase 1 修訂後 acceptance 3 為「XCTest 屬性斷言 + 實機視覺確認」兩段式），無須另立 ticket；但 Phase 2 設計實機驗證清單時須將三種情境分列，不可僅測其一即判定通過。若實機顯示 `.screenSaver` 不足，屆時屬 SPEC-008 決策 1 的修訂，再依當時證據建票。

#### Phase 1.5 判定

Phase 1 產出經修訂後具備作為 Phase 2 依據的穩定性，Phase 1.5 通過。下一步派 sage-test-architect 執行 Phase 2 測試設計。
### TDD Phase 2 測試設計：XCTest 案例與測試策略（sage-test-architect，2026-07-31）

依據為上方「TDD Phase 1 功能設計」**Phase 1.5 修訂版**（介面契約 §2.1-§2.10、GWT 場景 §五、邊界條件 §六、驗收對應 §七）。本節只設計案例與策略，不寫實作程式碼、不寫最終測試碼。

#### 一、測試策略

##### 1.1 分層決策

| 層級 | 對象 | 測試型態 | 隔離邊界 | 子票 |
|------|------|---------|---------|------|
| 純運算 | `CursorScreenLocator.resolve` | 單元測試（無替身，直接餵值型別） | 無外部依賴，天然隔離 | .1 |
| 生命週期狀態機 | `CursorLocatorEffectController` | Sociable unit test（只替換四個注入點，控制器與 `CursorScreenLocator` 用真貨） | 只隔離 AppKit：視窗伺服器、CVDisplayLink、`NSScreen`/`NSEvent` 全域、GCD 排程 | .2 |
| 平台契約 | production `CursorLocatorSurfaceMaking` 產出的真實 `NSWindow` | app-hosted 屬性斷言 | 不隔離（就是要驗真實 AppKit 物件） | .2 |
| 錯誤碼對應 | Swift error → `FlutterError` 字面 | 單元測試（純對應函式，見 §六接線需求 N-1） | 不建 channel、不啟 Flutter engine | .2 |
| 視覺外觀、`.screenSaver` 實際覆蓋效果 | — | 實機驗證清單（§七） | — | .2 |

**Mock 策略**（Sociable Unit Tests 判準：只隔離外部世界）：

| 依賴 | 替身？ | 理由 |
|------|-------|------|
| `CursorScreenSnapshotProviding` | 是（fixture 序列） | 外部世界（`NSScreen.screens` 全域狀態，測試無法擺出多螢幕排列） |
| `CursorLocationSampling` | 是（座標序列） | 外部世界（`NSEvent.mouseLocation` 全域狀態） |
| `CursorLocatorSurfaceMaking` | 是（spy 工廠） | 外部世界（視窗伺服器）；同時是 acceptance 4/5 的唯一觀測面 |
| `CursorLocatorFrameDriving` | 是（手動 driver） | 外部世界（CVDisplayLink 執行緒與 wall clock） |
| `CursorLocatorDeadlineScheduling` | 是（手動 scheduler） | 外部世界（GCD 時鐘） |
| `CursorScreenLocator` | **否** | 內層純邏輯，控制器測試必須用真貨——否則場景 4/10/11/12 的「判定結果真的被拿去開視窗」失去意義 |
| `CursorLocatorPlayRequest` / `CursorScreenSnapshot` / `CursorLocatorPlaybackState` | **否** | 值型別，直接建構 |
| `CursorLocatorEffectRendering` | **否，且不寫 spy 斷言** | 見 §五「明確不做」 |

##### 1.2 計時與非同步約束（`.claude/rules/core/test-assertion-design-rules.md`）

本設計**全數**測試為確定性：

- 時間推進一律由手動 driver 呼叫端給定絕對時間戳，基準取固定值 `T0 = 1000.0`（不呼叫 `CACurrentMediaTime()`、不呼叫 `Date()`）。
- 逾時保險由手動 scheduler 的 `fire()` 觸發，不等真實延遲。
- **禁用**：`XCTestExpectation` / `waitForExpectations` / `Thread.sleep` / `DispatchQueue.asyncAfter` / `measure {}` / 任何 `elapsed` 與門檻的 `XCTAssertLessThan`。規則 D1（計時斷言禁入主套件）、D3（`DateTime.now()` 等價的 wall clock 不進斷言）於本設計無例外，`test/performance` 等價目錄不需建立。
- 浮點斷言用 `XCTAssertEqual(_:_:accuracy:)`，`accuracy` 取 `1e-9`（確定性算術，非量測值）。

##### 1.3 測試獨立性與 Phase 3b 拆分友善性

`Runner.xcscheme` 的 `TestableReference` 標 `parallelizable = "YES"`，XCTest 可跨 class 平行起多個 runner process。因此：

- 任何測試 class **不得**持有 `static var` 可變狀態；fixture 一律於 `setUp` 重建。
- 視窗屬性測試（T2.G）建立的真實 `NSWindow` 必須在 `tearDown` 內 `close()` 並釋放，且不假設獨佔螢幕、不假設自己是 front app。
- 五個測試 class 之間零共享，各自可獨立執行（`xcodebuild test -only-testing:RunnerTests/<Class>`），符合拆分友善設計。

| 測試群組 | 功能職責 | 所屬子票 | 跨群組依賴 |
|---------|---------|---------|-----------|
| A `CursorScreenLocatorTests` | 螢幕判定純函式 | **.1** | 無 |
| B `CursorLocatorTestDoubles`（support，非 test class） | 測試替身與 fixture | **.1**（基礎設施） | B 被 C/D/E 依賴（唯一跨群組依賴，且為單向唯讀） |
| C `CursorLocatorEffectControllerLifecycleTests` | 播放／重置／釋放／淡出／逾時／stop | .2 | 依賴 B |
| D `CursorLocatorEffectControllerScreenTests` | 接線、跨螢幕跟隨、熱插拔 | .2 | 依賴 B |
| E `CursorLocatorErrorMappingTests` | 錯誤路徑與 `FlutterError` 對應 | .2 | 依賴 B |
| F `CursorLocatorSurfaceContractTests` | 真實 `NSWindow` 屬性契約（app-hosted） | .2 | 無（不用 B） |

子票 .1 交付 A + B + §六接線；.2 交付 C/D/E/F。B 置於 .1 是刻意的——.1 完成即證明 XCTest 執行路徑可用，.2 不必再碰基礎設施。

#### 二、測試替身設計

檔案落點 `macos/RunnerTests/Support/CursorLocatorTestDoubles.swift`（子票 .1 建立）。

##### 2.1 `SurfaceCallLog`（class，**與 spy 分離持有**）

記錄所有 surface 呼叫的共享日誌。**設計要點：日誌必須由測試強持有、spy 只持有其參照**，如此 spy dealloc 後計數仍可斷言——這是 §三洩漏斷言能同時成立的前提。

| 欄位 | 型別 | 用途 |
|------|------|------|
| `events` | `[Event]` | `Event = .made(NSRect) / .move(NSRect) / .setAlpha(CGFloat) / .close`，含發生順序 |
| `makeCount` / `closeCount` / `moveCount` | `Int`（由 events 導出） | 場景 5/7/8/9 計數斷言 |
| `alphaValues` | `[CGFloat]`（由 events 導出） | 場景 13 淡出單調性 |
| `retargetedDisplayIDs` | `[CGDirectDisplayID]` | 場景 10 重綁斷言（由 driver 寫入同一 log 以保序） |

##### 2.2 `SpyCursorLocatorSurface`

實作 `CursorLocatorSurface`。`contentLayer` 回傳一個 `CALayer()`（本票 renderer 為空陣列，不被使用）。`move` / `setAlpha` / `close` 一律附加事件至持有的 `SurfaceCallLog`。**不記錄自身狀態**（狀態即事件），也**不被工廠強持有**。

##### 2.3 `SpySurfaceFactory`

提供 `make: (NSRect) throws -> CursorLocatorSurface`。行為可設定：

| 模式 | 行為 | 用於 |
|------|------|------|
| `.succeed` | 建立新 spy 並登記 weak box | 多數場景 |
| `.throwOnCall(n)` | 第 n 次呼叫 throw 指定錯誤 | 場景 19 |

持有 `weakBoxes: [WeakBox]`（`final class WeakBox { weak var value: AnyObject? }`）與 `requestedFrames: [NSRect]`。**不得**持有 spy 的強參照。

##### 2.4 `ManualFrameDriver`

實作 `CursorLocatorFrameDriving`。`start` 捕存 `onFrame` 與 `displayID`；`retarget` 追加至 log；`stop` 只標記 `isStopped = true` 並**保留 `onFrame` 閉包不清除**——保留是刻意的，場景 17 需要在 `stop()` 之後仍能人為送出一幀模擬在途回呼。額外提供：

- `emit(at timestamp: CFTimeInterval)`：送一幀
- `advance(to timestamp:, step:)`：以固定步長連續送幀（步長為給定值，非真實幀率）
- `startCount` / `stopCount`：場景 14/15 斷言 driver 確有停止

##### 2.5 `ManualDeadlineScheduler`

實作 `CursorLocatorDeadlineScheduling`。記錄 `scheduledDelays: [TimeInterval]`、`cancelCount`，提供 `fire()` 手動觸發最後一筆回呼。回傳的取消 closure 遞增 `cancelCount` 並使 `fire()` 失效。

##### 2.6 `ScreenSnapshotFixtures`

固定排列常數（全域 AppKit 座標，左下原點、y 軸向上）：

| Fixture | entries（依序） | mainIndex | 用途 |
|---------|----------------|-----------|------|
| `sideBySide` | `(0,0,1920,1080)`、`(1920,0,1920,1080)` | 0 | 場景 1、3 |
| `withGap` | `(0,0,1920,1080)`、`(2400,0,1920,1080)` | **1** | 場景 2、20（main 刻意不設 index 0，見下） |
| `secondaryLeft` | `(0,0,1920,1080)`、`(-1920,0,1920,1080)` | 0 | 場景 3b 負 x |
| `secondaryBelow` | `(0,0,1920,1080)`、`(0,-1080,1920,1080)` | 0 | 場景 3b 負 y |
| `secondaryAbove` | `(0,0,1920,1080)`、`(0,1080,1920,1080)` | 0 | 場景 3b 對照（正 y） |
| `overlapping` | `(0,0,1920,1080)`、`(960,0,1920,1080)` | 0 | 場景 3d |
| `empty` | `[]` | nil | 場景 3c、21 |
| `singleMain` | `(0,0,1920,1080)` | 0 | 場景 12 前態、預設 |

各 entry 的 `displayID` 取可辨識常數（`1`、`2`、`3`），使場景 10 的 `retarget` 斷言不依賴順序推論。

> **fixture 設計要點 1（避免假綠燈）**：`withGap` 的 `mainIndex` 刻意設為 **1** 而非 0。若 main 設在 index 0，實作把 `.fellBackToMain` 誤寫成「回傳 index 0」也會通過，退回邏輯形同未測。
>
> **fixture 設計要點 2（負座標方向的更正）**：Phase 1 §六與派發指示均寫「第二螢幕置於主螢幕**上方**或左方時 frame 座標為負」。AppKit 全域座標原點在主螢幕左下、y 軸向上，故螢幕在主螢幕**上方**時 `origin.y` 為**正**（`+1080`），在**下方**時才為負。負座標的兩個真實來源是「左方（負 x）」與「下方（負 y）」。本設計據此以 `secondaryLeft` + `secondaryBelow` 覆蓋負座標需求，並額外保留 `secondaryAbove` 作正 y 對照，覆蓋面不減。此為 fixture 事實更正，不影響 Phase 1 介面契約（契約只說「不得假設非負」，該敘述本身正確）。

#### 三、acceptance 5「無洩漏」的斷言設計（重點節）

**判準：計數歸零不得單獨作為洩漏證據。** 每一條洩漏相關斷言由兩個獨立訊號構成，缺一不成立：

| 訊號 | 斷言 | 證明什麼 | 單獨為何不足 |
|------|------|---------|-------------|
| S1 呼叫面 | `log.closeCount == N`（N 為預期播放次數） | 控制器確實走了收尾路徑、確實呼叫 `close()` | 只證「呼叫過」，不證物件被回收；控制器仍強持有也會通過 |
| S2 記憶體面 | 每次播放對應的 `weakBoxes[i].value == nil` | surface 已 dealloc，無人（含控制器、閉包、driver）持有 | 只證回收，不證走過正常收尾（例如 throw 後中途丟棄也會 nil） |

S1 + S2 同時成立才等於「正常收尾且確實釋放」。

**實現前提（三項，缺任一則 S2 恆假或恆真，測試失去鑑別力）**：

1. `SurfaceCallLog` 由測試強持有、與 spy 分離。若計數存在 spy 內，spy dealloc 後 S1 無法讀取，兩訊號無法並存。
2. `SpySurfaceFactory` 只以 `WeakBox` 登記 spy，**任何測試區域變數都不得殘留 spy 的強參照**。斷言前不可先 `let s = factory.lastSurface` 取出。
3. 播放與推進過程包在 `autoreleasepool { }` 內，斷言置於區塊之後。spy 為純 Swift class（非 `NSObject` 子類、非 `NSWindow`），ARC 下釋放為確定性，autoreleasepool 只是防禦性隔離，不引入時序不確定。

**反面設計（明確禁止）**：

- 禁止以「`state == .idle`」或任何由 `state` 導出的計數作為釋放證據——Phase 1.5 P0-2 已刪除 `activeSurfaceCount` 正是此理由，不得在測試端以等價形式復活。
- 禁止以「工廠呼叫次數 == close 次數」作為無洩漏證據——兩者皆為呼叫面計數，仍是 S1 的同義重複。
- 禁止對真實 `NSWindow` 做 weak 斷言（T2.G 群組不參與洩漏斷言）：`NSWindow` 受 AppKit 內部快取與 autorelease 影響，釋放時機非確定性，會產生 flaky。洩漏斷言只對 spy 成立，這是 `CursorLocatorSurface` 協定存在的目的之一。

**額外洩漏向量的覆蓋**（三條，皆屬 acceptance 5 但非「正常結束」路徑）：

| 向量 | 案例 | 說明 |
|------|------|------|
| CVDisplayLink 停擺、幀不再送達 | T2.C-08（場景 14） | 手動 driver 天生測不到，靠逾時保險注入點覆蓋 |
| `stop()` 後在途幀操作已關閉 surface | T2.C-11（場景 17） | 崩潰向量而非洩漏，但同屬釋放正確性 |
| 播放中螢幕全數消失 | T2.D-05（場景 12） | 非正常結束路徑，同樣須 S1+S2 |

#### 四、XCTest 案例設計（22 個編號場景 / 25 條 GWT 敘述逐一對應）

案例編號 `T1.*` 屬子票 .1，`T2.*` 屬子票 .2。全部案例的 Given/When/Then 承自 Phase 1 §五，此處只補「斷言如何成立」與「為何這樣斷言」。

##### 群組 A：`CursorScreenLocatorTests`（子票 .1，純函式，無替身）

| 案例 | 場景 | Given / When | Then（斷言設計） |
|------|------|-------------|-----------------|
| T1.A-01 | 1 正常判定 | `sideBySide`，游標 `(2000, 500)` | `resolve(...) == .matched(index: 1)`。整個 enum 做等值比較，不只比 index，確保回傳的是 `.matched` 而非 `.fellBackToMain(1)` |
| T1.A-02 | 2 間隙退回 | `withGap`（mainIndex=1），游標 `(2000, 500)`（落在 1920~2400 間隙） | `== .fellBackToMain(index: 1)`。**index 必須等於 mainIndex 而非 0**（fixture 要點 1） |
| T1.A-03 | 3 邊界命中右側 | `sideBySide`，游標 `(1920, 500)` | `== .matched(index: 1)`。鎖定半開區間語意 |
| T1.A-04 | 3 邊界唯一命中 | 同上 | 對 entries 逐一以 `NSPointInRect` 檢查，命中數恰為 1。與 T1.A-03 分開：前者驗「命中誰」，本案驗「不會兩個都中或都不中」 |
| T1.A-05 | 3 上邊界排除 | `sideBySide`，游標 `(500, 1080)`（等於 maxY） | `== .fellBackToMain(index: 0)`。maxY 為開端，不屬任何螢幕；驗證半開區間在 y 軸同樣成立 |
| T1.A-06 | 3 下／左邊界包含 | 游標 `(0, 0)` | `== .matched(index: 0)`。minX/minY 為閉端 |
| T1.A-07 | 3b 負 x 排列 | `secondaryLeft`，游標 `(-960, 540)` | `== .matched(index: 1)` |
| T1.A-08 | 3b 負 y 排列 | `secondaryBelow`，游標 `(960, -540)` | `== .matched(index: 1)` |
| T1.A-09 | 3b 正 y 對照 | `secondaryAbove`，游標 `(960, 1620)` | `== .matched(index: 1)`。與 T1.A-08 成對，證明判定對 y 正負皆正確而非湊巧 |
| T1.A-10 | 3b 負座標邊界 | `secondaryLeft`，游標 `(-1920, 0)` | `== .matched(index: 1)`。負值上的 minX 閉端，防止實作以 `abs` 或非負假設處理 |
| T1.A-11 | 3c 無可用螢幕 | `empty`（entries 空、mainIndex nil） | `== .unavailable` |
| T1.A-12 | 3c 有螢幕但無 main | entries 非空、游標落於間隙、`mainIndex == nil` | `== .unavailable`。與 T1.A-11 分開：驗「unavailable 的條件是 mainIndex 為 nil」而非「entries 為空」 |
| T1.A-13 | 3d 重疊取小 | `overlapping`，游標 `(1200, 500)`（兩者皆涵蓋） | `== .matched(index: 0)`。不做面積／距離仲裁 |
| T1.A-14 | 3d 重疊順序敏感 | 同 fixture 但 entries 順序對調 | `== .matched(index: 0)`（仍為陣列首位命中者）。證明「取索引最小」而非「取某個固定螢幕」 |
| T1.A-15 | §六 純度 | 任意 fixture，連續呼叫 `resolve` 三次 | 三次回傳相同；且案例本身**不設定任何全域狀態**。純度的結構性保證由「函式簽章不含 `NSScreen`」提供，本案例只做冪等回歸 |

##### 群組 C：`CursorLocatorEffectControllerLifecycleTests`（子票 .2）

預設 Given：`singleMain` 快照 provider、游標固定 `(960, 540)`、`SpySurfaceFactory(.succeed)`、`ManualFrameDriver`、`ManualDeadlineScheduler`、renderer 空陣列、`T0 = 1000.0`。

| 案例 | 場景 | When | Then（斷言設計） |
|------|------|------|-----------------|
| T2.C-01 | 5 單次播放釋放 | `play(duration: 1.5)`，`emit(T0)` 後 `emit(T0+0.5)`，最後 `emit(T0+1.5)` | 中途 `state` 為 `.playing(elapsed: 0.5, duration: 1.5, screenFrame: main.frame)`（elapsed 以 accuracy 比較）；結束後 `state == .idle`、`log.closeCount == 1`、`driver.stopCount == 1`、`scheduler.cancelCount == 1` |
| T2.C-02 | 6 釋放後不再持有 | 同上，全程包 `autoreleasepool` | `factory.weakBoxes[0].value == nil`（S2）**且** `log.closeCount == 1`（S1）。兩者同一案例內並列，避免拆開後只跑其中一半 |
| T2.C-03 | 5 首幀即入 playing | `play(1.5)` 後僅 `emit(T0)` | `state == .playing(elapsed: 0, ...)`；`log.makeCount == 1`。驗證起點時間戳取自首幀而非 `play` 當下的某個時鐘 |
| T2.C-04 | 7 重複觸發為重置 | `play(1.5)` → `emit(T0)` → `emit(T0+0.8)` → `play(duration: 2.0, tint: 另一色)` → `emit(T0+0.8)` | `log.makeCount == 1`、`log.closeCount == 0`；`state.duration == 2.0`、`state.elapsed == 0`；`log.alphaValues.last == 1.0`（復位）。**不**斷言 renderer 互動 |
| T2.C-05 | 7 重置後時間重新起算 | 承 T2.C-04，續 `emit(T0+2.7)` | 仍為 `.playing`（因重置起點為 `T0+0.8`，2.0 秒後才到 `T0+2.8`）；再 `emit(T0+2.8)` 後 `state == .idle`、`closeCount == 1`。此案例是「重置」與「疊加」的真正鑑別點——若實作只換參數不重設起點，T2.C-04 仍會全綠 |
| T2.C-06 | 8 播放中連按十次 | `play` 後 `emit(T0)`，接著連續 `play` 十次（期間僅 `emit(T0+0.1*k)`，不推進至結束） | `log.makeCount == 1`、`log.closeCount == 0`、`state` 每次皆為 `.playing`。以迴圈斷言每次觸發後即刻檢查，而非只檢查末態 |
| T2.C-07 | 9 十次完整播放無洩漏 | 迴圈十次：`play(1.5)` → `emit(T0+k*10)` → `emit(T0+k*10+1.5)` | `log.makeCount == 10`、`log.closeCount == 10`（S1）；`factory.weakBoxes.count == 10` 且**每一個** `value == nil`（S2）；每次結束後 `state == .idle`。時間戳基準逐輪前移，維持單調遞增 |
| T2.C-08 | 14 逾時保險 | `play(1.5)` 後**完全不送幀**，直接 `scheduler.fire()` | `scheduler.scheduledDelays == [2.0]`（`duration + 0.5`）；觸發後 `log.closeCount == 1`、`state == .idle`、`driver.stopCount == 1`；`weakBoxes[0].value == nil`。此案例即「CVDisplayLink 停擺」的唯一自動訊號 |
| T2.C-09 | 14 正常結束時保險已取消 | `play(1.5)` → 推進至結束 → 再 `scheduler.fire()` | `fire()` 為 no-op：`log.closeCount` 維持 1、不 crash。防止逾時回呼對已釋放 surface 二次 `close()` |
| T2.C-10 | 15 stop 於播放中 | `play(1.5)` → `emit(T0)` → `stop()` | `log.closeCount == 1`、`state == .idle`、`driver.stopCount == 1`、`scheduler.cancelCount == 1`、`weakBoxes[0].value == nil` |
| T2.C-11 | 17 stop 後在途幀 | 承 T2.C-10，記錄 `log.events.count` 為 `n`，接著 `driver.emit(T0+0.2)` | `log.events.count` 仍為 `n`——即在途幀對已 close 的 surface **未做任何呼叫**（`move` / `setAlpha` / `close` 全數未增加）；`state` 維持 `.idle`；不 crash。以「事件總數不變」而非逐一斷言個別計數，可涵蓋任何未預期的新呼叫 |
| T2.C-12 | 17 play 後舊 generation 幀 | `play(1.5)` → `emit(T0)` → `stop()` → `play(1.5)`（新一輪）→ 以**舊**的 onFrame 送 `emit(T0+0.1)` | 新一輪 surface 的事件不受影響：`log.makeCount == 2` 且第二個 spy 未收到任何 `move`/`setAlpha`。驗證 generation token 對「跨輪次的舊幀」同樣有效，而非只擋 stop 後 |
| T2.C-13 | 16 stop 於 idle | 未 `play`，直接 `stop()` | `log.events.isEmpty`、`state == .idle`、不 throw；再 `stop()` 一次仍然如此（冪等） |
| T2.C-14 | 13 淡出窗起點 | `play(1.5)`（`fadeDuration = min(0.2, 0.6) = 0.2`，窗起於 elapsed 1.3）→ 推進至 elapsed 0、0.5、1.0、1.29 | 這些幀的 `alphaValues` 皆為 `1.0`（或未被呼叫）；斷言「不曾出現小於 1.0 的值」 |
| T2.C-15 | 13 淡出單調遞減 | 承上，續推進 elapsed 1.30、1.35、1.40、1.45、1.49 | 對應 alpha 序列嚴格遞減且全數落於 `(0, 1)`；`alpha(1.30) ≈ 1.0`、`alpha(1.49)` 趨近 0（線性契約下 `(1.5-elapsed)/0.2`，以 accuracy 比對精確值）。斷言精確值而非僅遞減，鎖定線性曲線契約 |
| T2.C-16 | 13 短時長淡出窗 | `play(duration: 0.4)`（`fadeDuration = min(0.2, 0.16) = 0.16`，窗起於 0.24） | elapsed 0.20 時 alpha 為 1.0；elapsed 0.30 時 alpha 落於 `(0,1)`。驗證 `min` 分支的另一側，避免只測到固定 0.2 |
| T2.C-17 | 13 總時長不變量 | `play(1.5)`，推進至 elapsed 1.4999 與 1.5 | 前者仍 `.playing`、後者 `.idle`。鎖定「淡出含在 duration 內、總時長恆等於設定值」（Phase 1.5 用戶裁示 + UC-06 v1.1） |
| T2.C-18 | 18 duration 為 0 | `play(duration: 0)` → `emit(T0)` | 首幀即 `close()`：`log.makeCount == 1`、`log.closeCount == 1`、`state == .idle`、不 throw、`weakBoxes[0].value == nil` |
| T2.C-19 | 18 duration 為負 | `play(duration: -1)` → `emit(T0)` | 同 T2.C-18。負值屬 Dart 側已約束但原生層須有定義行為的殘餘輸入 |

##### 群組 D：`CursorLocatorEffectControllerScreenTests`（子票 .2）

| 案例 | 場景 | Given / When | Then（斷言設計） |
|------|------|-------------|-----------------|
| T2.D-01 | 4 接線 | 快照 `sideBySide`、游標固定於螢幕 B `(2500, 500)`；`play(1.5)` | `factory.requestedFrames == [B.frame]`（恰一次、引數為 B 而非 main）；`emit(T0)` 後 `state.screenFrame == B.frame`。此案例是「判定正確但接線永遠傳 main」的唯一防線（P0-5） |
| T2.D-02 | 4 接線於退回路徑 | `withGap`（mainIndex=1）、游標落間隙；`play` | `factory.requestedFrames == [entries[1].frame]`。退回結果同樣須被拿去開視窗 |
| T2.D-03 | 10 跨螢幕跟隨 | `sideBySide`；游標序列 A×3 幀 → B×3 幀；推進 6 幀 | `log` 中 `move(toScreenFrame: B.frame)` 恰出現 **1** 次（同螢幕連續幀不重複搬遷）；`driver.retargetedDisplayIDs == [B.displayID]` 恰一次；且 `move` 與 `retarget` 於同一幀內成對出現（以 log 事件相對順序斷言） |
| T2.D-04 | 10 來回跨螢幕 | 游標序列 A → B → A | `move` 出現 2 次（B、A），`retarget` 2 次。驗證搬遷判斷比較的是「當前所在」而非「是否曾搬遷過」 |
| T2.D-05 | 10 同螢幕不搬遷 | 游標於 A 內移動（座標變但螢幕不變）×5 幀 | `moveCount == 0`、`retargetedDisplayIDs.isEmpty`。與 T2.D-03 成對，防止實作每幀無條件 `move` 也讓 T2.D-03 通過 |
| T2.D-06 | 11 目標螢幕被拔除 | 播放中，快照 provider 於第 3 幀起由 `sideBySide` 改為只剩 A（游標仍在原 B 座標，現落於間隙、mainIndex=0） | 第 3 幀觸發 `move(toScreenFrame: A.frame)`、`retarget(A.displayID)`；`state` 仍 `.playing`；播放不中斷至 duration 結束 |
| T2.D-07 | 12 螢幕數歸零 | 播放中，快照 provider 於第 3 幀起回傳 `empty` | 第 3 幀即 `log.closeCount == 1`、`state == .idle`、`driver.stopCount == 1`、`scheduler.cancelCount == 1`、`weakBoxes[0].value == nil`（S1+S2）；**不 throw**（`play` 早已返回） |
| T2.D-08 | 12 歸零後續幀無操作 | 承 T2.D-07，再 `emit` 一幀 | `log.events.count` 不變（generation token 攔下）。與 T2.C-11 同型，涵蓋「自然結束」而非「stop 結束」的在途幀 |

##### 群組 E：`CursorLocatorErrorMappingTests`（子票 .2）

| 案例 | 場景 | Given / When | Then（斷言設計） |
|------|------|-------------|-----------------|
| T2.E-01 | 19 視窗建立失敗 | `SpySurfaceFactory(.throwOnCall(1))`；`play` | 拋出 `CursorLocatorError.windowCreationFailed(underlying:)`（以 `XCTAssertThrowsError` + 型別比對）；`state` 維持 `.idle`；`driver.startCount == 0`；`scheduler.scheduledDelays.isEmpty`（失敗路徑不得留下未取消的排程——這是隱性洩漏向量） |
| T2.E-02 | 19 失敗後可再次 play | 承 T2.E-01，工廠改為 `.succeed` 後再 `play` | 正常進入 `.playing`。驗證失敗未使狀態機卡死 |
| T2.E-03 | 21 無可用螢幕 | 快照 `empty`；`play` | 拋出 `.noAvailableScreen`；`factory.requestedFrames.isEmpty`（**未**呼叫工廠）；`driver.startCount == 0` |
| T2.E-04 | 20 退回 main 不中斷 | `withGap`、游標落間隙；`play` | **不 throw**；`factory.requestedFrames == [entries[1].frame]`；推進至結束正常釋放。鎖定「復原路徑不以 FlutterError 中斷播放」 |
| T2.E-05 | 19/21 錯誤碼字面 | 對 `CursorLocatorError` 各 case 呼叫錯誤對應函式（見 §六 N-1） | `.windowCreationFailed` → code `E_CL_WINDOW`；`.noAvailableScreen` → code `E_CL_SCREEN`；`details` 含 underlying 字串。字面以**字串常數逐字**斷言，不與實作共用同一常數（否則常數改名時測試跟著改，等於沒測） |
| T2.E-06 | 20 成功回傳 | 對應函式輸入為「無錯誤」情形 | 對應為 `result(nil)` 語意。與 T2.E-05 成對，確保成功路徑不誤入錯誤分支 |

##### 群組 F：`CursorLocatorSurfaceContractTests`（子票 .2，app-hosted，對真實 `NSWindow`）

`BUNDLE_LOADER = $(TEST_HOST)`（project.pbxproj:478/493/508，三個 configuration 皆有）與 `ENABLE_TESTABILITY = YES`（project.pbxproj:627，Debug）已由 PM 於 Phase 1.5 查證，本群組建立真實 `NSWindow` 並斷言 §2.5 契約值。

前置：`setUp` 以 production `CursorLocatorSurfaceMaking` 建立 surface（frame 取 `NSScreen.main?.frame` 或退回固定 `NSRect(0,0,800,600)`）；`tearDown` 必須 `close()` 並釋放。

| 案例 | 場景 | Then（斷言設計） |
|------|------|-----------------|
| T2.F-01 | 22 視窗基本屬性 | `styleMask == .borderless`、`isOpaque == false`、`backgroundColor == .clear`、`hasShadow == false`、`alphaValue == 1.0`、`isReleasedWhenClosed == false` |
| T2.F-02 | 22 層級 | `window.level == .screenSaver`（以 `NSWindow.Level.screenSaver` 常數比對，不用魔數） |
| T2.F-03 | 22 collectionBehavior | 逐項 `XCTAssertTrue(behavior.contains(.canJoinAllSpaces))` 等四項；並斷言**不含** `.moveToActiveSpace`（與 `.canJoinAllSpaces` 互斥，同時設會使跨 Space 行為未定義）。逐項比對而非整個集合等值，避免 AppKit 預設附加旗標導致脆性失敗 |
| T2.F-04 | 22 滑鼠穿透 | `ignoresMouseEvents == true` |
| T2.F-05 | 22 不搶焦點 | `canBecomeKey == false`；呼叫 `orderFrontRegardless()` 後 `isKeyWindow == false`。**不**斷言 `isVisible`（測試環境下 app 可能非 front，`isVisible` 非契約項） |
| T2.F-06 | 22 frame 覆蓋 | `window.frame == 傳入的 frame`（建立後未被 AppKit 調整；borderless 視窗不受標題列高度影響） |
| T2.F-07 | 22 未成為 key 的負向保證 | 呼叫 `makeKey()` 後 `isKeyWindow` 仍為 false | 直接鎖定 `canBecomeKey` 覆寫確實生效，而非僅未主動 makeKey（P0-8 的真正風險是誤用 `makeKeyAndOrderFront`） |

> **本群組不參與洩漏斷言**（見 §三反面設計）。`NSWindow` 釋放時機受 AppKit 內部持有影響，weak 斷言會 flaky。

##### 場景覆蓋核對

| Phase 1 場景 | 對應案例 |
|-------------|---------|
| 1 / 2 / 3 / 3b / 3c / 3d | T1.A-01 / A-02 / A-03~A-06 / A-07~A-10 / A-11~A-12 / A-13~A-14 |
| 4 | T2.D-01、T2.D-02 |
| 5 / 6 | T2.C-01、T2.C-03 / T2.C-02 |
| 7 / 8 | T2.C-04、T2.C-05 / T2.C-06 |
| 9 | T2.C-07 |
| 10 / 11 / 12 | T2.D-03~D-05 / T2.D-06 / T2.D-07、T2.D-08 |
| 13 | T2.C-14~C-17 |
| 14 / 15 / 16 / 17 / 18 | T2.C-08、C-09 / T2.C-10 / T2.C-13 / T2.C-11、C-12 / T2.C-18、C-19 |
| 19 / 20 / 21 | T2.E-01、E-02、E-05 / T2.E-04、E-06 / T2.E-03、E-05 |
| 22 | T2.F-01~F-07 |
| §六 純度／不變量 | T1.A-15；`state == .idle` 等價於不持有 surface 由 T2.C-02/07 的 S1+S2 共同保證 |

合計 **56 個 XCTest 案例**：子票 .1 十五個（群組 A）+ 測試替身檔；子票 .2 四十一個（C 十九、D 八、E 六、F 七、另 T2.C 群組共用 setUp）。

#### 五、明確不做

| 項目 | 理由 |
|------|------|
| `CursorLocatorEffectRendering` 的呼叫序列 spy 斷言 | Phase 1 §2.9 與 Phase 1.5 linux P2 裁定：為尚未實作的功能預鎖呼叫順序，1.4.0-W3-001 動手時會被迫先拆測試。本票 renderer 注入空陣列，測試只驗「空陣列下生命週期完整運作」，不驗控制器對 renderer 的任何互動 |
| NSLog 內容斷言 | 原生層日誌無注入點，Phase 1 未定義 logger 依賴。**建議不新增** logger 注入（加法且僅為測試而存在）；acceptance 6 的日誌面由 §七實機清單的 Console 檢視覆蓋，程式面由 T2.E-* 的 throw 與錯誤碼斷言覆蓋。驗收對應表原就寫「XCTest（throw 與 channel 回傳型別）+ 人工檢視 log」，與此一致 |
| 真實 CVDisplayLink 行為 | 綁定真實顯示器更新率屬平台行為，無確定性斷言方式；`retarget` 的**呼叫**已由 T2.D-03 覆蓋，其**效果**歸實機 |
| Dart 側 method channel 端對端 | 屬 1.4.0-W1-001 channel 骨架票範圍；本票只驗 Swift 側錯誤對應（T2.E-05/06） |
| `.screenSaver` 是否真的蓋住全螢幕 | 屬性值由 T2.F-02 斷言，覆蓋效果只能實機（Phase 1.5 待追蹤 2 已裁定不另建票，改以 §七三情境分列覆蓋） |
| 效能／幀率測試 | 無此 acceptance；且計時斷言禁入主套件 |

#### 六、XCTest target 接線：執行步驟與已知阻礙（子票 .1 工作量）

已查證現況（非推測）：

| 項目 | 現況 | 出處 |
|------|------|------|
| RunnerTests target | 存在，內容為 Flutter scaffold 空殼 `testExample()` | `macos/RunnerTests/RunnerTests.swift` |
| `BUNDLE_LOADER` / `TEST_HOST` | 三個 configuration（Debug/Release/Profile）皆已設定 | project.pbxproj:478/485、493/500、508/515 |
| `ENABLE_TESTABILITY` | `YES`，位於 **Debug** 專案層設定 | project.pbxproj:627 |
| App 模組名 | `screen_clock`（`PRODUCT_NAME = screen_clock`） | `macos/Runner/Configs/AppInfo.xcconfig:8` |
| Scheme | `Runner.xcscheme` 已含 `RunnerTests.xctest` 的 `TestableReference`，`skipped = "NO"`、`parallelizable = "YES"` | `macos/Runner.xcodeproj/xcshareddata/xcschemes/Runner.xcscheme:40-50` |
| CocoaPods | `Podfile` 第 33 行已有巢狀 `target 'RunnerTests'` | Phase 1.5 PM 查證 |
| App sandbox | **未啟用**（`DebugProfile.entitlements` 只有 `allow-jit` 與 `network.server`） | `macos/Runner/DebugProfile.entitlements` |

**執行步驟**：

1. 建立 `macos/RunnerTests/Support/CursorLocatorTestDoubles.swift` 與 `macos/RunnerTests/CursorScreenLocatorTests.swift`，**target membership 設為 RunnerTests only**；`macos/Runner/CursorLocatorEffectWindow.swift` 設為 **Runner only**。
2. 測試檔頂部：`import XCTest` + `@testable import screen_clock`；需 `FlutterError` 的檔案另加 `import FlutterMacOS`。
3. 先跑一次 `flutter build macos --debug` 產生 `Flutter/ephemeral` 設定與 Pods，再執行測試。
4. 測試命令（自 repo 根執行，不切換工作目錄）：
   `xcodebuild test -workspace macos/Runner.xcworkspace -scheme Runner -configuration Debug -destination 'platform=macOS' 2>&1 | tail -40`
   單一群組：附加 `-only-testing:RunnerTests/CursorScreenLocatorTests`。
5. 首次執行後刪除 scaffold 的 `testExample()`（或保留為 smoke case，二擇一，由 .1 實作者裁定並記錄）。

**已知阻礙與對策**：

| # | 阻礙 | 徵狀 | 對策 |
|---|------|------|------|
| B-1 | 必須用 `.xcworkspace` 而非 `.xcodeproj` | `xcodebuild` 報 Pods 相關 target 找不到 | 命令固定用 `-workspace macos/Runner.xcworkspace` |
| B-2 | `ENABLE_TESTABILITY` 僅 Debug | Release 下 `@testable import` 編譯失敗 | 命令固定 `-configuration Debug`；不在 Release 跑測試 |
| B-3 | 模組名含底線 | `@testable import screen_clock` 若誤寫為 `Runner` 會找不到模組 | 以 `PRODUCT_NAME` 為準，非 target 名 |
| B-4 | 新檔 target membership 需改 `project.pbxproj` | 手改易產生格式錯誤或 UUID 衝突，且此檔 Flutter 工具鏈也會動 | 優先以 Xcode GUI 加入檔案；若必須手改，一次只加一檔並立即 `xcodebuild -list` 驗證專案可解析 |
| B-5 | Access control | `private`/`fileprivate` 型別即使 `@testable` 也看不到 | 受測型別與協定一律 `internal`（預設），**不要**為測試改 `public` |
| B-6 | app-hosted 需啟動宿主 app 並簽章 | 簽章或 Gatekeeper 導致 test host 啟動失敗 | sandbox 未啟用，本機開發簽章（automatic / ad-hoc）即可；若失敗，先確認 `xcodebuild` 使用的 team 與 Xcode GUI 一致，再考慮 `CODE_SIGN_IDENTITY=-` |
| B-7 | app-hosted 會真的跑起 app，`MainFlutterWindow` 會建立時鐘遮罩視窗與 CGEventTap | 測試期間出現真實遮罩視窗；CGEventTap 可能要求輔助使用權限而彈窗 | 群組 A（.1）為純函式測試，即使宿主啟動也不受影響；若權限彈窗阻塞 CI，**建立追蹤票**而非在本票改動宿主啟動流程 |
| B-8 | `parallelizable = "YES"` | 多 runner process 平行執行 | 設計上已禁止 `static var` 共享狀態（§1.3）；若群組 F 因平行造成視窗互擾，改以 `-parallel-testing-enabled NO` 執行並記錄 |
| B-9 | `flutter test` 不涵蓋 Swift target | 誤以為 CI 已覆蓋 | Test Results 章節須分別記錄 `flutter test` 與 `xcodebuild test` 兩條命令與結果 |

**.1 的完成判準**：`xcodebuild test ... -only-testing:RunnerTests/CursorScreenLocatorTests` 實際輸出 15 個案例全數通過，且輸出貼入 .1 的 Test Results。在此之前不得宣告基礎設施就緒。

#### 七、實機驗證清單（子票 .2，三種情境分列）

Phase 1.5 收尾裁定：實機須將三種假／真全螢幕情境分列，不可僅測其一即判定通過。共同前置：多螢幕環境（至少兩台）、`screen_clock` 以 Debug 建置執行、特效功能已啟用且熱鍵已註冊、特效時長設為 3.0 秒（上限值，便於觀察）。

| # | 情境 | 操作步驟 | 通過判準 |
|---|------|---------|---------|
| M-1 | **native fullscreen（綠燈鈕）** | 1. 於螢幕 A 開啟任一 app（如 Safari），點左上綠燈鈕進入原生全螢幕 2. 游標移至螢幕 A 該全螢幕 Space 內 3. 按下定位熱鍵 | (a) 特效顯示於全螢幕內容**之上**，未被遮蔽 (b) 全螢幕 app **未退出**全螢幕、未失去焦點 (c) 特效期間仍可操作該 app（打字／捲動有反應） (d) 特效結束後畫面無殘留 |
| M-2 | **假全螢幕影片** | 1. 於螢幕 A 播放影片並進入播放器的全螢幕模式（YouTube 網頁全螢幕或 IINA/QuickTime 的無邊框全螢幕） 2. 游標移至螢幕 A 3. 按下定位熱鍵 | (a) 特效顯示於影片之上 (b) 影片**未暫停**、播放控制列未被觸發 (c) 特效期間影片持續播放 (d) 特效結束後無殘留、影片仍為全螢幕 |
| M-3 | **簡報播放** | 1. 於螢幕 A 以 Keynote 或 PowerPoint 進入簡報播放模式 2. 游標移至螢幕 A 3. 按下定位熱鍵 | (a) 特效顯示於投影片之上 (b) 簡報**未跳頁、未退出**播放模式 (c) 特效結束後簡報仍在原頁 |
| M-4 | 跨螢幕跟隨（視覺） | 1. 熱鍵觸發後，播放期間將游標由螢幕 A 拖曳至螢幕 B | 特效於同一次播放內搬遷至螢幕 B，過程無閃爍、無在 A 留下殘影 |
| M-5 | 重複觸發（視覺） | 1. 熱鍵觸發後 1 秒內再次按下熱鍵 ×3 | 畫面上**始終只有一層**特效（無疊加變亮／變濃）；每次觸發皆從頭播放 |
| M-6 | 淡出與總時長（視覺） | 1. 時長設 3.0 秒，觸發並計時 | 自觸發至完全消失約 3.0 秒（含末段淡出），非 3.2 秒；末段為漸淡非瞬滅 |
| M-7 | 十次觸發無殘留（視覺輔證） | 1. 完整觸發並等待結束 ×10 2. 觀察畫面與 Activity Monitor 的 window 數 | 無任何殘留視窗；記憶體無單調上升。**此項為輔證非主證**，主證為 T2.C-07 的 S1+S2 |
| M-8 | 錯誤路徑日誌 | 1. 觸發正常播放 2. 於 Console.app 過濾 process `screen_clock` | 正常路徑無 E_CL_* 錯誤；若曾發生退回 main，Console 可見對應 E_CL_SCREEN 訊息（acceptance 6 的日誌面人工覆蓋） |

**記錄方式**：M-1~M-8 逐項於子票 .2 的 Test Results 記錄「通過／不通過 + 觀察摘要」。M-1/M-2/M-3 任一不通過即代表 `.screenSaver` 對該情境不足，屬 SPEC-008 決策 1 的修訂範圍，依 Phase 1.5 收尾裁定「依當時證據建票」，不在本票直接改 level。

#### 八、Phase 3a 移交事項

**須由 Phase 3a / 3b 裁定的介面補充（本設計提出，未逕自決定）**：

| # | 事項 | 本節建議 |
|---|------|---------|
| N-1 | `CursorLocatorError` → `FlutterError` 的對應目前內嵌於 `CursorLocatorBridge.handlePlay` 的 catch 區塊，無法在不啟 channel 的情況下斷言錯誤碼字面 | 抽出**純對應函式**（如 `CursorLocatorError.flutterError` 或同檔的自由函式），輸入 error 輸出 `FlutterError`。純加一個無狀態函式，不引入新型別、不改變既有 channel 流程；T2.E-05/E-06 依賴此點。若 3a 判定不抽，須改以其他方式使錯誤碼字面可斷言，或明確將 E-05/E-06 降為實機驗證並記錄理由 |
| N-2 | `SurfaceCallLog` 需觀測 driver 的 `retarget` 與 surface 事件的**相對順序**（T2.D-03） | 兩個替身寫入同一個 log 物件即可，不需要 production 介面改動 |

**Phase 2 退出條件核對**：

- [x] 22 個編號場景（25 條 GWT 敘述）100% 有對應 XCTest 案例，無遺漏（§四覆蓋核對表）
- [x] 每個案例標明所屬子票（T1.* → .1；T2.* → .2）
- [x] acceptance 5 洩漏斷言為 S1（`close()` 計數）+ S2（`weak` 為 nil）雙訊號，明確禁止計數單獨作為證據（§三）
- [x] 在途幀防護有專屬案例（T2.C-11、T2.C-12、T2.D-08），斷言為「事件總數不變」
- [x] 逾時保險有專屬案例（T2.C-08、T2.C-09），涵蓋 CVDisplayLink 完全停擺
- [x] 負座標排列覆蓋左方（負 x）與下方（負 y），並更正 Phase 1「上方為負」的方向敘述（§2.6 要點 2）
- [x] app-hosted 視窗屬性斷言 7 案例，逐項對應 §2.5 契約 12 項
- [x] 實機驗證清單三種全螢幕情境分列（M-1/M-2/M-3），各含操作步驟與通過判準
- [x] XCTest target 接線步驟 5 步 + 已知阻礙 9 項，工作量歸屬 .1
- [x] 無計時斷言、無 wall clock、無 XCTestExpectation（§1.2）
- [x] 未為 `CursorLocatorEffectRendering` 撰寫呼叫序列 spy 斷言（§五）
- [x] 測試群組可獨立執行，跨群組依賴僅 B（support）單向唯讀（§1.3）

（產出者：sage-test-architect，2026-07-31；止於 Phase 2，未推進 Phase 3a）
### Phase 2 驗收（PM，2026-07-31）

**驗收通過。** 獨立查證：56 個 XCTest 案例分六群組、22 個編號場景 100% 有覆蓋對應、零計時斷言、零 wall clock、零 XCTestExpectation（時間推進一律用固定基準 T0 的手動 driver，符合 test-assertion-design-rules）、`CursorLocatorEffectRendering` 未寫 spy 斷言（符合不為未實作功能預鎖呼叫序列的約束）。

**設計品質值得記錄的兩點**：

1. 洩漏斷言採雙訊號（`close()` 計數 + `weak` 為 nil）並附三條反面禁令，其中明確禁止以「工廠呼叫數 == close 數」冒充無洩漏——該式仍屬呼叫面的同義重複，與 P0-2 刪除 `activeSurfaceCount` 的理由同源。另禁止對真實 `NSWindow` 做 weak 斷言（AppKit 內部持有致 flaky），此為 test-assertion-design-rules 不可靠斷言判準的正確應用。
2. fixture 防假綠燈設計：`withGap` 的 `mainIndex` 刻意設為 1，否則「一律回傳 index 0」的錯誤實作也會通過退回 main 的測試。

#### 座標系契約方向錯誤更正（PM 自身之誤）

sage 於建立 fixture 時發現「次要修訂」條目中的座標系契約方向有誤，已更正並於該處加註。**此為 PC-SCLK-006 在同一張 ticket 內的二度發生，且第二次由記錄該 error-pattern 的 PM 本人於建檔後所犯**。已將此復發案例回寫入 `PC-SCLK-006`，作為「本 pattern 的防護必須是機制性而非提醒性」的實證——同一人在知曉、剛記錄、高度警覺的狀態下仍於數十分鐘內復發。

#### sage 交回兩項的處置

**N-1（介面補充請求，交 Phase 3a 裁定）**：錯誤碼字面若內嵌於 `handlePlay` 的 catch 區塊，T2.E-05 / E-06 無法在不啟動 channel 的情況下斷言。sage 建議抽出無狀態的純對應函式（不改 channel 流程）。

PM 補充：此請求與延後票 `1.4.0-W2-026`（錯誤碼命名慣例與常數集中）**目標重疊但時序相反**——W2-026 排在 W2-006.2 之後，而 N-1 要求在本票內即抽出對應函式方能滿足測試可觀測性。Phase 3a 裁定時須一併考量：若採納 N-1 而在本票抽出對應函式，W2-026 的範圍應相應收斂為「命名慣例統一 + 常數落位」，避免兩票重工；若 3a 判定不抽，須明確將 E-05 / E-06 降為實機驗證並記錄理由，且該決定須回寫 W2-026 使其知悉本票未處理此面向。

**B-7（app-hosted 測試可能觸發輔助使用權限彈窗）**：宿主 app 啟動時 `MainFlutterWindow` 的 CGEventTap 可能彈出權限對話框，阻塞無人值守的測試執行。群組 A（子票 .1，純函式）不受影響，群組 F（子票 .2，真實 NSWindow）受影響。

依 decision-trigger-binding 規則，條件延後須有明確 trigger 與承接者，不可停留於「若發生再說」：本項已指定 trigger 為「子票 .2 執行 `xcodebuild test` 時實際觀察到權限彈窗阻塞」，承接動作為建立追蹤票處理宿主啟動流程的測試環境隔離，**禁止以繞過測試或改寫宿主啟動流程的方式在本票內就地處理**（後者屬 W2-015 拆分範圍，非本票職責）。此段須由執行子票 .2 的代理人於 Test Results 明確回報是否觸發。
### TDD Phase 3a 實作策略規劃（pepper-test-implementer，2026-08-01）

本節為語言無關的實作策略：只描述步驟序列、資料流與判定邏輯，不含 Swift 程式碼。依據為上方 Phase 1（Phase 1.5 修訂版介面契約）、Phase 2 測試設計（含第六節接線步驟、第八節移交事項 N-1/N-2）與 Phase 2 驗收。

#### 一、N-1 裁定：錯誤對應抽為純函式（採納）

**裁定：採納。** 於新檔 `macos/Runner/CursorLocatorErrorMapping.swift` 建立一個無狀態的對應單元，輸入為「播放結果」（成功或 `CursorLocatorError`），輸出為「channel 回應」（無錯誤／帶錯誤碼、訊息、細節的錯誤物件）。`handlePlay` 的 catch 區塊只呼叫此對應單元後交給 `result`，不自行組裝錯誤碼字面。

判準（三項，缺一則不採納）：

| 判準 | 本案是否成立 |
|------|------------|
| 是否為純加法（不改既有流程語意） | 成立。既有參數解析路徑與 `invalid_arguments` 分支完全不動，只把「新的兩個錯誤如何變成 channel 回應」放進獨立單元 |
| 是否讓既有無訊號的 acceptance 取得自動訊號 | 成立。acceptance 6「回報 Dart 側」目前只能實機檢視 Console；抽出後 T2.E-05/E-06 可在不啟 Flutter engine 的情況下逐字斷言 `E_CL_WINDOW` / `E_CL_SCREEN` |
| 是否引入新的可變狀態或新依賴方向 | 不成立（即無）。對應單元無儲存狀態、無 AppKit 依賴，只依賴 Flutter 的錯誤型別 |

**落點與理由**：不放進 `MainFlutterWindow.swift`（836 行，已超 cognitive-load 閾值 2.8 倍，Phase 1 §2.10 明定該檔只改一處）；也不放進 `CursorLocatorEffectWindow.swift`（該檔刻意只依賴 AppKit，混入 Flutter 型別會讓生命週期測試群組 C/D 被迫連帶引入 Flutter 依賴）。獨立第三檔使三方依賴方向為單向：effect window（AppKit）← mapping（Flutter）← bridge。

**常數落點**（CLAUDE.md §6：平台原生程式碼的常數屬該平台側，但仍須命名而非散落字面）：

| 常數 | 落點 | 說明 |
|------|------|------|
| `E_CL_SCREEN` / `E_CL_WINDOW` 字面 | `CursorLocatorErrorMapping.swift` 內一個專用的錯誤碼列舉 | 與既有 `CursorLocatorChannel` 列舉同型態（Swift 無法 import Dart 常數，以列舉承載相同字面，並在註解標明須與 `lib/app_constants.dart` 的 `AppCursorLocator` 逐項一致） |
| 錯誤訊息文字 | 同檔同列舉 | 訊息屬診斷字串，不對使用者可見、不進 Dart 側 |
| 淡出窗上限（0.2）、淡出比例（0.4）、逾時寬限（0.5） | `CursorLocatorEffectWindow.swift` 內一個計時常數列舉 | 三者皆為原生層自保門檻，Phase 1 §六明定不對使用者可見、不進 `SettingsModel` |
| 視窗屬性契約值 | 同檔一個視窗樣式列舉；AppKit 列舉值（`.borderless`、`.screenSaver`、`collectionBehavior` 成員）直接引用不另命名 | 依 CLAUDE.md「框架列舉值屬例外」條款 |
| 日誌前綴 `[cursor-locator]` | 沿用既有字面，不在本票統一 | 統一屬 `1.4.0-W2-026` 範圍（見下） |

**測試端的反向約束（Phase 2 已指定，實作須配合）**：T2.E-05 以字串字面逐字斷言，**不得**引用實作端的錯誤碼列舉。實作者不可為了「消除重複」而讓測試改用同一常數——那會使常數改名時測試同步變動，錯誤碼契約失去鎖定作用。

**對 `1.4.0-W2-026` 的回寫內容**（本票採納 N-1 後，該票範圍須收斂）：

```
本票（W2-026）範圍收斂，依 1.4.0-W2-006 Phase 3a 的 N-1 裁定：

已由 1.4.0-W2-006.2 完成、W2-026 不再重做：
- 建立 CursorLocatorError -> FlutterError 的純對應函式（落於 macos/Runner/CursorLocatorErrorMapping.swift）
- E_CL_SCREEN / E_CL_WINDOW 兩個錯誤碼字面已於該檔以專用列舉承載，非散落字面
- 對應函式已有測試覆蓋（RunnerTests 群組 E：T2.E-05 / T2.E-06，以字串字面逐字斷言）

W2-026 保留範圍：
1. 命名慣例統一：既有 CursorLocatorBridge 的 invalid_arguments（小寫蛇底線）與 SPEC-008 的
   E_CL_*（大寫底線前綴）兩套慣例共存，須裁定單一慣例並改寫既有碼；本票不得逕改 E_CL_* 兩碼的
   字面（已由 SPEC-008 定義且已有測試鎖定）
2. Dart 側常數集中：lib/app_constants.dart 的 AppCursorLocator 補上錯誤碼常數，並與 Swift 側
   列舉建立逐項一致的註解對照（現況 grep -rn E_CL lib/ 零命中）
3. 跨 bridge 盤點：settings bridge 等其他 bridge 的錯誤碼一併納入同一慣例
4. 日誌前綴字面（[cursor-locator] 等）的常數化，與錯誤碼同屬「Swift 側字面集中」議題

W2-026 執行時的必要驗證：改動錯誤碼字面會使 RunnerTests 群組 E 紅燈，屬預期；若裁定改名 E_CL_*，
須同步改 SPEC-008、UC-06、測試字面三處，缺一即為規格漂移。
```

#### 二、N-2 處置：共用 log 物件

**處置：測試端建立單一 log 物件，以建構子注入 spy 工廠與手動 driver，兩者只寫入不擁有。production 介面零改動。**

| 面向 | 策略 |
|------|------|
| 事件模型 | 單一有序事件序列，事件種類含「建立（帶 frame）」「搬遷（帶 frame）」「設定 alpha（帶值）」「關閉」「重綁 displayID（帶 id）」。跨物件的相對順序（T2.D-03 要求 move 與 retarget 同幀成對）由同一序列的索引直接判定，不需另做時間戳比對 |
| 計數與衍生值 | `makeCount` / `closeCount` / `moveCount` / `alphaValues` / `retargetedDisplayIDs` 一律由事件序列**導出**（過濾 + 映射），不另存欄位。避免兩份真相不同步，也讓「事件總數不變」型斷言（T2.C-11、T2.D-08）與計數型斷言建立在同一來源 |
| 持有方向（關鍵） | 測試強持有 log；spy 與 driver 各自強持有 log 的參照；**log 不得持有 spy 或 driver 的任何參照**。方向單向才使 S2（spy 的 weak 為 nil）與 S1（log 的 close 計數）能同時成立 |
| 生命週期 | log 於每個測試 `setUp` 重建（Phase 2 §1.3 禁 static 可變狀態）；spy 釋放後 log 仍存活，斷言在 `autoreleasepool` 區塊之後執行 |
| 執行緒 | 全程 main thread，log 不加鎖。production 驅動的跨執行緒派回責任在 production 端（Phase 1 §2.4 已規定 `onFrame` 保證於 main thread），測試替身不需防禦 |

**明確不做**：不讓 log 記錄「當前 alpha」「當前 frame」等狀態欄位。狀態即事件序列的重播結果；額外存狀態等於 Phase 1.5 已刪除的 `activeSurfaceCount` 反模式在測試端復活。

#### 三、子票 .1 實作步驟序列（螢幕判定純函式 + XCTest 接線）

依賴方向：步驟 1-3 為基礎設施（無測試可跑），步驟 4 起才有紅綠訊號。順序原則為「先讓一個最小案例真的跑起來」再擴充，理由見步驟 4。

```
步驟 1  建立值型別
        輸入：Phase 1 §2.1 的螢幕幾何快照定義
        產出：快照型別（含條目陣列與 main 索引）、條目型別（含 frame 與 displayID）
        判定：兩者皆為可等值比較的值型別，不引用任何全域螢幕來源

步驟 2  建立判定結果型別與判定單元
        產出：三態結果（命中 / 退回 main / 無可用螢幕）與單一靜態判定函式
        判定邏輯（虛擬碼）：
            對 entries 由索引 0 起依序檢查
                若 entry.frame 以半開區間涵蓋游標座標
                    回傳 命中(該索引)              // 取第一個命中者，不做面積或距離仲裁
            若 mainIndex 存在
                回傳 退回main(mainIndex)
            回傳 無可用螢幕
        涵蓋判定：直接使用平台矩形型別的「包含點」語意（minX <= x < maxX、minY <= y < maxY），
                  不自行以四則運算重寫比較式——自行重寫是邊界歸屬出錯的主要來源
        約束：函式簽章不得出現任何平台螢幕型別或游標取樣來源，取樣責任在呼叫端

步驟 3  將新檔加入建置 target
        判定型別所在檔 -> 僅屬 app target
        測試檔 -> 僅屬測試 target

步驟 4  以「單一 smoke 案例」先打通執行路徑
        只寫一個必然通過的案例（例如正常判定），跑一次完整測試命令
        判定：命令實際輸出「1 個案例通過」才算執行路徑就緒
        理由：15 個案例一次寫完再跑，首次失敗時無法分辨是接線問題還是判定邏輯問題；
              先打通路徑可讓後續每次失敗都只有一個可能來源

步驟 5  補齊測試替身與 fixture 檔（Phase 2 §二，屬 .1 交付）
        固定排列常數八組、spy surface、spy 工廠（含 weak 登記）、手動 driver、
        手動 deadline scheduler、共用 log（依本節第二段策略）
        判定：本檔僅被測試 target 引用；其正確性由 .2 的群組 C/D/E 使用時才驗證

步驟 6  補齊判定層 15 個案例（T1.A-01 ~ A-15）
        判定：全數通過，且命令輸出貼入 .1 的 Test Results

步驟 7  處理 scaffold 的 testExample()
        裁定並記錄：刪除或保留為 smoke case，二擇一，於 Test Results 說明選擇
```

**.1 不做的事**：不建立特效視窗控制器、不碰 `MainFlutterWindow.swift`、不建立錯誤對應檔（屬 .2）。

#### 四、子票 .2 實作步驟序列（視窗生命週期 + 跨螢幕跟隨）

依賴：.1 的判定單元與測試替身已就緒且實測綠燈。

```
步驟 1  定義注入介面與請求／狀態值型別
        四個注入點（快照提供、游標取樣、surface 建立、逾時排程）一律為函式型別；
        逐幀驅動與 surface 為協定；播放狀態為二態（閒置 / 播放中含 elapsed、duration、所在螢幕 frame）

步驟 2  實作控制器的 play（不含每幀行為）
        虛擬碼：
            取快照、取游標 -> 呼叫判定單元
            若 無可用螢幕 -> 記日誌、拋出「無可用螢幕」錯誤
                             （不建立 surface、不啟動驅動、不排程逾時）
            若 退回main   -> 記日誌（不中斷）
            決定目標 frame
            若 目前為播放中：
                重置——elapsed 起點清除、換上新 duration 與 tint、alpha 復位為 1、
                       目標螢幕與現況不同才搬遷、遞增世代序號、重排逾時
                （不建立第二個 surface、不呼叫關閉）
            否則：
                經工廠建立 surface（失敗則拋出「視窗建立失敗」錯誤，
                                    且不得留下已排程的逾時或已啟動的驅動）
                啟動驅動並綁定目標 displayID
                排程逾時（duration + 寬限）
                遞增世代序號
        關鍵不變量：任何拋出路徑結束後，狀態必須仍為閒置，且無殘留的排程與驅動

步驟 3  實作每幀行為（Phase 1 §2.8 五步）
        虛擬碼：
            若 幀所屬世代 != 當前世代 -> 直接返回      // 攔截在途幀，這是崩潰防線
            若 尚未記錄起點 -> 以本幀時間戳為起點
            重建快照、重取游標、重新判定
                無可用螢幕 -> 走「結束」子程序，記日誌，返回
                退回main   -> 記日誌，續行
                目標 frame 與現況不同 -> 搬遷 surface + 重綁驅動 displayID（同幀成對）
            elapsed = 本幀時間戳 - 起點
            若 elapsed 落於淡出窗（duration - fadeDuration 起算）
                設定 alpha = 剩餘時間 / fadeDuration    // 線性，端點為 1 與 0
            若 elapsed >= duration -> 走「結束」子程序
        「結束」子程序（單一出口，供自然結束 / 螢幕歸零 / 逾時 / 主動停止四條路徑共用）：
            停驅動 -> 取消逾時 -> 關閉 surface -> 釋放持有 -> 狀態設為閒置 -> 遞增世代序號
        設計要點：四條結束路徑共用同一子程序，是「十次觸發無洩漏」在所有路徑上一致成立的
                  結構前提；若各路徑各自收尾，必有某條路徑漏掉其中一步

步驟 4  實作 stop 與逾時回呼
        stop：閒置時為無動作；播放中走「結束」子程序
        逾時回呼：檢查世代序號後走「結束」子程序；正常結束已先取消，故不會二次關閉

步驟 5  實作 production 的 surface 與驅動
        surface：依 Phase 1 §2.5 十二項屬性契約建立視窗；顯示採不搶焦點的呈現方式
        驅動：以顯示器垂直同步機制驅動，回呼一律派回 main thread；提供重綁與停止

步驟 6  建立錯誤對應檔（N-1 裁定產物）
        輸入播放結果 -> 輸出 channel 回應；成功對應為「無錯誤」

步驟 7  改動 MainFlutterWindow.swift（唯一一處）
        bridge 持有一個控制器；handlePlay 在既有參數解析成功後改為委派，
        以錯誤對應單元決定回應。既有參數失敗分支完全不動

步驟 8  補齊測試群組 C（19）、D（8）、E（6）、F（7）
        建議順序：E（最小、驗證錯誤對應）-> C（生命週期主體）-> D（螢幕接線）-> F（真實視窗）
        理由：F 需啟動宿主 app，是唯一可能被環境阻擋的群組，放最後可避免環境問題
              擋住前面所有進度（B-6/B-7 風險隔離）

步驟 9  執行實機清單 M-1 ~ M-8，逐項記錄於 .2 的 Test Results
```

#### 五、XCTest 接線執行順序與 9 項阻礙因應

**接線順序**（與 .1 步驟 3-4 對應，逐步驗證而非一次到位）：

| 序 | 動作 | 驗證訊號 | 失敗時的下一步 |
|----|------|---------|--------------|
| 1 | 先跑一次既有 scaffold 的 `testExample()`，不加任何新檔 | 命令輸出 1 個案例通過 | 此時失敗即純環境問題（B-1/B-2/B-6），與本票程式碼無關，優先排除 |
| 2 | 加入 app target 的判定型別檔，重跑序 1 的命令 | 仍為 1 個案例通過（證明新檔不破壞建置） | 建置錯誤 -> 檢查 target membership（B-4） |
| 3 | 加入測試檔（僅一個 smoke 案例）與 `@testable` 匯入 | 2 個案例通過 | 找不到模組 -> 檢查模組名（B-3）；符號不可見 -> 檢查存取層級（B-5） |
| 4 | 擴充至 15 個案例 | 全數通過 | 純邏輯問題，來源唯一 |

順序原則：**每一步只引入一個新變數**。序 1 分離環境問題、序 2 分離建置設定問題、序 3 分離匯入與存取層級問題、序 4 才進入邏輯問題。

**9 項阻礙的因應**（Phase 2 §六已列徵狀與對策，此處補「何時處理、失敗後往哪走」）：

| # | 歸屬步驟 | 因應與升級路徑 |
|---|---------|--------------|
| B-1 workspace 非 project | 接線序 1 | 命令固定寫法，不臨場調整；此項屬命令書寫，不應消耗除錯輪次 |
| B-2 僅 Debug 可 `@testable` | 接線序 1 | 同上；不得為了「試試看」改用其他 configuration |
| B-3 模組名含底線 | 接線序 3 | 以產品名為準；匯入失敗時先輸出建置設定確認產品名，不猜測 |
| B-4 target membership | 接線序 2 | 優先以 IDE 加入檔案。若必須手改專案檔，一次只加一檔，加完立即以列出專案的命令驗證可解析；連續兩次手改失敗即停手回報 PM，不重複嘗試 |
| B-5 存取層級 | 接線序 3 | 受測型別維持預設層級；**禁止**為測試提升可見度——那是讓測試需求外洩為 production API |
| B-6 宿主簽章 | 接線序 1（.1）／步驟 8-F（.2） | 若簽章阻塞，先確認命令列與 IDE 使用同一 team，仍失敗則回報 PM，不自行改動簽章設定 |
| B-7 權限彈窗阻塞 | 步驟 8-F | 已由 Phase 2 驗收綁定 trigger：實際觀察到彈窗阻塞時，建立追蹤票處理測試環境隔離。**禁止**在本票就地改動宿主啟動流程（屬 `1.4.0-W2-015` 範圍）。執行者須在 Test Results 明確回報「是否觸發」，未觸發也要寫 |
| B-8 平行執行 | 步驟 8-F | 群組間零共享已由設計保證；若群組 F 因平行造成視窗互擾，改為關閉平行執行並記錄該事實，不改測試設計去遷就 |
| B-9 兩條測試命令 | .1 與 .2 的 Test Results | 兩條命令與各自結果分別記錄。只記其一即視為 Test Results 不完整 |

#### 六、技術債務識別（quality-baseline 規則 5）

| # | 項目 | 已有承接？ | 處置 |
|---|------|-----------|------|
| D-1 | `invalid_arguments` 與 `E_CL_*` 兩套錯誤碼慣例共存 | 是，`1.4.0-W2-026` | 不新建票；依第一節回寫該票收斂範圍 |
| D-2 | `MainFlutterWindow.swift` 836 行、多 domain 混載 | 是，`1.4.0-W2-015` | 不新建票。本票只新增委派一處，未加重該檔負擔 |
| D-3 | app-hosted 測試可能被輔助使用權限彈窗阻塞 | 是，Phase 2 驗收已綁 trigger（B-7） | 不預先建票；trigger 觸發時由 .2 執行者回報、PM 建票 |
| D-4 | 原生層日誌無注入點，錯誤日誌內容無自動訊號 | 否 | **建議建票**：「為 macOS 原生層引入可注入的日誌介面」。理由：acceptance 6 的日誌面目前只能靠人工檢視 Console，回歸時無訊號；Phase 2 已裁定本票不新增僅為測試而存在的注入點，故屬後續獨立議題而非本票缺口。優先級建議 P2 |
| D-5 | `xcodebuild test` 未納入既有測試執行流程，`flutter test` 不涵蓋 Swift target | 否 | **建議建票**：「將 RunnerTests 的 xcodebuild test 納入專案測試執行流程與文件」。理由：本票建立的 56 個 XCTest 案例若不進入常規執行路徑，等同一次性驗證，後續改動不會觸發紅燈，回歸防護在下一張票就失效。優先級建議 P1（本票是專案首次有 Swift 測試，此缺口自本票起才成立） |
| D-6 | production 逐幀驅動與真實視窗覆蓋效果無自動測試 | 否 | **不建票**。屬平台行為的固有邊界，Phase 2 §五已列為「明確不做」並以實機清單 M-1~M-8 覆蓋；建票無可執行的收斂動作 |
| D-7 | Swift 側與 Dart 側常數（channel 名、參數鍵、錯誤碼）以註解維持一致，無機械驗證 | 否 | **建議建票**：「建立 Dart／Swift 跨語言常數字面一致性的機械檢查」。理由：現況靠註解提醒，改一側忘另一側不會有任何訊號，且本票再新增兩個錯誤碼字面使對照面擴大。優先級建議 P2；與 `1.4.0-W2-026` 相關但不重疊（W2-026 是「集中」，本項是「持續驗證兩側一致」） |

#### 七、待 PM 裁決事項

| # | 事項 | 需要的決定 |
|---|------|-----------|
| 1 | `1.4.0-W2-026` 範圍收斂 | 將第一節的回寫內容寫入該票（本代理人不對非派發範圍的 ticket 執行寫入） |
| 2 | D-4 / D-5 / D-7 三項建議建票 | 是否建立、優先級、排入哪個 Wave |
| 3 | 子票 .2 新增第三個原生檔 `CursorLocatorErrorMapping.swift` | Phase 1 §2.10 只提到兩個檔案落點，本裁定新增一檔，需 PM 確認可接受（理由見第一節「落點與理由」） |

**Phase 3a 退出條件核對**：

- [x] N-1 已裁定（採納），含落點、常數落位、對 W2-026 的完整回寫文字
- [x] N-2 已處置（共用 log 的持有方向與事件模型），確認 production 介面零改動
- [x] 兩張子票各有步驟序列，以虛擬碼與流程描述表達，無 Swift 程式碼
- [x] XCTest 接線順序為四段逐步驗證，9 項阻礙皆有歸屬步驟與升級路徑
- [x] 技術債務 7 項全數列出，3 項建議建票並附理由與優先級建議
- [x] 常數落點依 CLAUDE.md §6 指明（平台側列舉承載，並標明與 Dart 側對照）

（產出者：pepper-test-implementer，2026-08-01；止於 Phase 3a，未撰寫任何 production 程式碼、未改動 macos/ 下任何檔案）

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.4.0-W1-009 | chars: 284 -->

### Task Reference
- 1.4.0-W1-009 what: 檢視 1.4.0-W1-001 與 PROP-003/SPEC-008/UC-06 的規格一致性

### Rationale Chain
- 1.4.0-W1-009 why: 文件層對齊新規範後，W1-001 的 Context Bundle 與驗收條件需回頭確認引用正確且規格未漂移

### Related Files
- docs/work-logs/v1/v1.4/v1.4.0/tickets/1.4.0-W1-001.md  # from 1.4.0-W1-009
- docs/proposals/PROP-003-cursor-locator.md  # from 1.4.0-W1-009
- docs/spec/user-experience/SPEC-008-cursor-locator.md  # from 1.4.0-W1-009
- docs/usecases/UC-06-locate-cursor.md  # from 1.4.0-W1-009

### UC Context
#### UC-06: 快速定位滑鼠游標
Spec 位置: docs/app-use-cases.md:154
主要流程:
  1. **觸發**
  2. **判定目標螢幕**
  3. **建立特效視窗**
  4. **播放特效**
  5. **結束**
  6. **使用者找到游標**
