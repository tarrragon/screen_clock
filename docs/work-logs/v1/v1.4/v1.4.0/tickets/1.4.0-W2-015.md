---
id: 1.4.0-W2-015
title: 拆分 MainFlutterWindow.swift 的多 bridge 單檔累積
type: IMP
status: in_progress
version: 1.4.0
wave: 2
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.4.0-W1-001
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: 既有債務不在 W1-001 本輪處理，於閾值觸發時提取為獨立檔案
  rationale: quality-baseline-rule-5
who:
  current: parsley-flutter-developer
  history: {}
what: 拆分 MainFlutterWindow.swift 的多 bridge 單檔累積
when: 本檔突破 900 行或新增第四個 bridge 時（以先到者為準）；1.4.0-W2-006 特效層視窗管理將大幅增加本檔行數，屆時應重新評估
where:
  layer: 待定義
  files:
  - macos/Runner/MainFlutterWindow.swift
  - macos/Runner/
  - macos/Runner.xcodeproj/project.pbxproj
why: Phase 4 評估時本檔為 759 行，已超 cognitive-load 300 行閾值 2.5 倍；1.4.0-W2-042 於 2026-08-06
  重新實測為 888 行（2.96 倍），因 1.4.0-W2-006（特效視窗管理）已完成，原「屆時規模會明顯上升」的預期已實現。單檔累積 5 個型別：MainFlutterWindow（第
  6 行）/ FullscreenCoverageDetector（133）/ InputBindingBridge（283）/ CursorLocatorChannel（736，private
  enum）/ CursorLocatorBridge（764）。1.4.0-W2-042 的依賴分析判定本票為 MainFlutterWindow.swift
  相關 7 張票的結構性前置且無前置依賴（blockedBy 為空），為 DAG Layer 0 唯一起點——先拆使後續票落點清晰，後拆則前面所有變更需跟著搬移，兩案認知負擔累計相差約
  4.3 倍（估算，非實測）。本票同時是 pbxproj 序列化鏈 W2-015 至 W1-021.1 至 W2-034 至 W3-011 的首張
how:
  task_type: Implementation
  strategy: 將各 bridge 提取為獨立 Swift 檔案（FullscreenCoverageDetector.swift / InputBindingBridge.swift
    / CursorLocatorBridge.swift），MainFlutterWindow.swift 僅保留視窗本身與註冊呼叫。提取時保留既有的強參考持有與
    weak self closure 模式，不改變生命週期行為。既有透明視窗設定（isOpaque / backgroundColor）不得改動
acceptance:
- '[x] 各 bridge 提取為獨立 Swift 檔案，MainFlutterWindow.swift 僅保留視窗本身與註冊呼叫'
- '[x] 既有強參考持有與 weak self closure 模式保留，無 retain cycle'
- '[x] 既有透明視窗設定（isOpaque / backgroundColor）未改動'
- '[ ] flutter build macos 成功；flutter test 全套件通過'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-08-06T11:28:37'
completed_at: null
created: '2026-07-31'
updated: '2026-07-31'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

拆分 MainFlutterWindow.swift 的多 bridge 單檔累積

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

MainFlutterWindow.swift（888 行，5 型別）拆分為 4 檔：

- MainFlutterWindow.swift（120 行）：僅保留 class MainFlutterWindow 與
  registerLaunchAtStartupChannel/isLaunchAtStartupEnabled/setLaunchAtStartupEnabled，
  awakeFromNib 內仍依原順序建立三個 bridge 並強參考持有。
- FullscreenCoverageDetector.swift（148 行）：原封不動搬移。
- InputBindingBridge.swift（460 行）：原封不動搬移，含 CGEventTap 建立/拆除、
  weak self closure、hidUsageToCGKeyCode 對照表。
- CursorLocatorBridge.swift（166 行）：含 CursorLocatorChannel enum、
  CursorLocatorArgument<T> enum、extractCursorLocatorArgument 函式、CursorLocatorBridge class。

project.pbxproj 新增三筆 PBXBuildFile + 三筆 PBXFileReference + Runner group 三個子項
+ Sources build phase 三筆條目，緊鄰 MainFlutterWindow.swift 對應行插入，ID 前綴以
FSD/IBB/CLB 區分三檔避免與既有 hex ID 衝突。

不變式驗證：isOpaque/backgroundColor 透明設定未改動；awakeFromNib 建立三個 bridge 的
順序與強參考持有欄位（fullscreenDetector/inputBindingBridge/cursorLocatorBridge）逐字保留；
InputBindingBridge 內 [weak self] closure（channel handler、reportCapturedButton）與
CursorLocatorBridge 內 [weak self] closure 皆逐字保留，無 retain cycle 風險變化。

副帶修正：test/platform/channel_contract_test.dart 原硬編碼讀取
MainFlutterWindow.swift 全文比對 channel 字面，拆分後該檔已不含 cursor_locator/
fullscreen_detect 字面，改為合併讀取 CursorLocatorBridge.swift +
FullscreenCoverageDetector.swift 維持原守衛意圖（防止 Dart/Swift 兩側 channel
字面漂移）。

Phase 4 評估：本票純屬結構性檔案拆分（無邏輯變更），程式碼品質已在拆分後維持原評分；
拆分後四檔皆遠低於 300 行 cognitive-load 閾值。無新增技術債務。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
執行 `./scripts/run-tests.sh`（單一入口）：

1. `flutter test`：全數通過（202 tests, 0 failures，需先修正 test/platform/channel_contract_test.dart
   讀取路徑——原硬編碼 MainFlutterWindow.swift 路徑，拆分後 cursor_locator/fullscreen_detect 字面
   已移出，改為合併讀取 CursorLocatorBridge.swift + FullscreenCoverageDetector.swift 兩檔內容）。
2. `pod install`（macos/）+ `flutter build macos --debug`：成功，確認 pbxproj 四類條目
   （PBXBuildFile / PBXFileReference / Group / PBXSourcesBuildPhase）皆正確補齊，三個新檔
   （FullscreenCoverageDetector.swift / InputBindingBridge.swift / CursorLocatorBridge.swift）
   成功納入 build。
3. `xcodebuild test`（RunnerTests, Debug, platform=macOS）：全數通過（Test Suite 'All tests' passed）。

最終 `./scripts/run-tests.sh` 輸出：`=== 全部測試通過 (flutter test + xcodebuild test) ===`

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.4.0-W1-001 | chars: 325 -->

### Task Reference
- 1.4.0-W1-001 what: 建立 滑鼠定位器基礎建設（常數 + SettingsModel schema 4 + channel 骨架）

### Rationale Chain
- 1.4.0-W1-001 why: SPEC-008 FR-06 要求三項設定（啟用開關 / 特效時長 / 主色調）持久化並將 schemaVersion 由 3 升為 4；FR-01 與特效播放需經 method channel 橋接。常數、資料模型與 channel 契約為 W2 特效層與 W4 面板的共同前置，須先定案以免介面反覆

### Related Files
- lib/app_constants.dart  # from 1.4.0-W1-001
- lib/models/settings_model.dart  # from 1.4.0-W1-001
- lib/platform/cursor_locator.dart  # from 1.4.0-W1-001
- test/models/settings_model_test.dart  # from 1.4.0-W1-001
### PM 派發前補料（2026-08-06，承接 W2-042 分析）

#### 一、現況實測（取代 why 原記的 759 行基準）

`macos/Runner/MainFlutterWindow.swift` = **888 行**，5 個型別位置：

| 型別 | 行號 |
|------|------|
| `class MainFlutterWindow: NSWindow` | 6 |
| `final class FullscreenCoverageDetector` | 133 |
| `final class InputBindingBridge` | 283 |
| `private enum CursorLocatorChannel` | 736 |
| `final class CursorLocatorBridge` | 764 |

注意 `CursorLocatorChannel` 是 `private enum`（非 class），grep 時需含 private 前綴才找得到。

#### 二、新增 Swift 檔必須手動補 project.pbxproj（W1-028 實證，本票最大風險）

本專案**非** filesystem-synchronized group。每新增一個 `.swift` 檔，須在 `macos/Runner.xcodeproj/project.pbxproj` 補齊四類條目：

1. `PBXBuildFile`
2. `PBXFileReference`
3. Group（歸入 Runner group）
4. `PBXSourcesBuildPhase`

**漏補的症狀會偽裝成「測試沒跑到」而非「檔案沒編譯」**——因為檔案存在於磁碟但不在 build phase，編譯器不報錯，症狀出現在執行期。若 acceptance 4 的 `flutter build macos` 通過但行為異常，優先檢查此處。

參考既有拆分產物的 pbxproj 條目寫法：`CursorLocatorRippleRenderer.swift` / `CursorLocatorSpotlightRenderer.swift` / `ButtonEventConsumption.swift` 皆為既有拆分成果，格式可直接對照。

#### 三、拆分粒度參考基準

`macos/Runner/` 現有小檔的行數，可作為拆分後各檔的合理規模參考：

| 既有拆分產物 | 行數 |
|-------------|------|
| `CursorLocatorRippleRenderer.swift` | 114 |
| `CursorLocatorSpotlightRenderer.swift` | 113 |
| `CursorLocatorBorderFlashRenderer.swift` | 79 |
| `CursorLocatorErrorMapping.swift` | 51 |
| `ButtonEventConsumption.swift` | 34 |

#### 四、本票是七張票的共同前置，落點穩定性優先於美觀

W2-042 判定本票為 DAG Layer 0 唯一起點，後續 W2-026 / W2-019 / W1-021.1 / W3-010 / W2-035 / W2-034 / W3-011 皆直接或間接依賴本票完成。因此**拆分後的檔名與型別歸屬一旦定案就不宜再改**——後續七張票的 where.files 會以此為基準。若拆分過程發現更佳的邊界劃分，請在 ticket 記錄理由後再改，不要靜默調整。

#### 五、不得觸碰的既有行為（對應 acceptance 2 與 3）

- 強參考持有與 `weak self` closure 模式：提取後生命週期行為須與現狀一致，勿順手改為其他記憶體管理寫法
- 透明視窗設定 `isOpaque` / `backgroundColor`：CLAUDE.md 明列為 macOS 透明遮罩的必要原生改動，改動會破壞核心功能

#### 六、acceptance 條目數的說明（dispatch-readiness 軟警告）

`ticket track dispatch-readiness` 對本票發出「acceptance 4 > 2，建議拆分」軟警告。PM 覆核判定**不拆**：4 條中僅第 1 條為功能職責（拆檔），其餘 3 條為不變式驗證（無 retain cycle / 透明設定未動 / build 與 test 通過）。該閾值以 acceptance 條目數近似功能職責數，對「一個職責 + 多個不變式」的票型會高估，屬已知的啟發式限制。
