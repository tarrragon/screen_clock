---
id: 1.4.0-W1-001
title: 建立 滑鼠定位器基礎建設（常數 + SettingsModel schema 4 + channel 骨架）
type: IMP
status: completed
version: 1.4.0
wave: 1
priority: P0
parent_id: null
children:
- 1.4.0-W1-001.1
- 1.4.0-W1-001.2
- 1.4.0-W1-001.3
- 1.4.0-W1-001.4
- 1.4.0-W1-001.5
blockedBy:
- 1.4.0-W1-009
- 1.4.0-W1-011
- 1.4.0-W1-015
relatedTo: []
spawned_tickets:
- 1.4.0-W2-007
- 1.4.0-W2-008
- 1.4.0-W2-009
- 1.4.0-W2-010
- 1.4.0-W2-011
- 1.4.0-W2-012
- 1.4.0-W2-013
- 1.4.0-W2-014
- 1.4.0-W2-015
- 1.4.0-W2-016
- 1.4.0-W2-017
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: 先落地常數與資料模型契約,W2 特效層與 W4 面板依此並行開發,避免介面反覆
  rationale: spec-008-fr06-schema-upgrade-as-shared-prerequisite
who:
  current: parsley-flutter-developer
  history: {}
what: 建立 滑鼠定位器基礎建設（常數 + SettingsModel schema 4 + channel 骨架）
when: v1.4.0 W1，W3/W4 的前置
where:
  layer: Domain
  files:
  - lib/app_constants.dart
  - lib/models/settings_model.dart
  - lib/platform/cursor_locator.dart
  - macos/Runner/MainFlutterWindow.swift
  - test/models/settings_model_test.dart
why: SPEC-008 FR-06 要求三項設定（啟用開關 / 特效時長 / 主色調）持久化並將 schemaVersion 由 3 升為 4；FR-01 與特效播放需經
  method channel 橋接。常數、資料模型與 channel 契約為 W2 特效層與 W4 面板的共同前置，須先定案以免介面反覆
how:
  task_type: Implementation
  strategy: 新增 AppCursorLocator 常數類（channel 名 screen_clock/cursor_locator、play 方法名、參數鍵、動畫預設值），Swift
    端字面對齊；SettingsModel 新增三欄位並升 schema 至 4，fromJson 缺欄補預設（沿用 SPEC-004 容錯模式）；建立 lib/platform/cursor_locator.dart
    橋接骨架（比照 fullscreen_detector.dart）；MainFlutterWindow 註冊 channel handler 骨架（收到 play
    先僅 NSLog，特效層留 W2）；測試涵蓋 v3 資料升 v4 與 round-trip
acceptance:
- '[x] AppCursorLocator 常數類建立，channel 名 / 方法名 / 參數鍵 / 動畫預設值集中且 Swift 端字面一致（禁硬編碼）'
- '[x] SettingsModel 新增啟用開關 / 特效時長 / 主色調三欄位，schemaVersion 由 3 升為 4'
- '[x] fromJson 對 v3 兩種形態（v3-early 缺 bindingsSeeded / v3-late 含 bindingsSeeded）皆補預設值不拋例外；fromJson(model.toJson())
  == model round-trip 通過'
- '[x] 特效時長欄位名含單位（如 cursorEffectDurationMs），不使用無單位的 duration 命名'
- '[x] 主色調以 ARGB int 儲存並經 _asColor 存取，不直接序列化 Color 物件'
- '[x] lib/platform/cursor_locator.dart 橋接骨架建立，MethodChannel 可注入以利測試（比照 fullscreen_detector.dart）'
- '[x] MainFlutterWindow 註冊 cursor_locator channel handler 骨架，收到 play 先以 NSLog 記錄（特效層留
  W2）'
- '[x] flutter test 全數通過、flutter analyze 無警告；既有設定讀寫功能不回歸'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-31T09:24:19'
completed_at: '2026-07-31T11:23:24'
created: '2026-07-29'
updated: '2026-07-29'
---

# Execution Log

## Task Summary

建立 滑鼠定位器基礎建設（常數 + SettingsModel schema 4 + channel 骨架）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 拆分決策與 dispatch-readiness 覆核紀錄（PM，2026-07-31）

本票原始 dispatch-readiness 為 FAIL（acceptance 8 條 > 4）。手動覆核後確認實質功能職責為 3 個，超過 cognitive-load 的「功能職責數 > 2 須拆分」門檻，且 where.files 5 個剛好卡在上界。經用戶裁示拆為 3 張子票：

| 子票 | 職責 | 檔案 |
|------|------|------|
| 1.4.0-W1-001.1 | AppCursorLocator 常數類 | lib/app_constants.dart |
| 1.4.0-W1-001.2 | SettingsModel schema 4 三欄位 | lib/models/settings_model.dart、test/models/settings_model_test.dart、SPEC-009 |
| 1.4.0-W1-001.3 | cursor_locator channel 骨架（Dart + Swift） | lib/platform/cursor_locator.dart、macos/Runner/MainFlutterWindow.swift |

依賴關係：.3 blockedBy .1（兩側皆引用 .1 定義的常數字面）；.1 與 .2 檔案互斥可平行派發（ticket track parallel-check 已確認）。父票 Context Bundle 強調的「介面須一次定案」不受拆分影響——三張子票同在 W1 完成，契約邊界未變，拆的是執行單元。

### 子票 dispatch-readiness 覆核（閾值 1 高估的判定依據）

閾值 1 以 acceptance 條目數近似功能職責數，含驗證類與約束類條目時會高估（ticket SKILL.md 已註明 PM 應手動覆核）。三張子票的覆核結果：

| 子票 | 機械判定 | acceptance 分類 | 實質職責 | 覆核結論 |
|------|---------|----------------|---------|---------|
| .1 | WARN（3 > 2） | 功能 1 / 約束 1（命名帶單位） / 驗證 1 | 1 | 不再拆 |
| .2 | FAIL（6 > 4） | 功能 1（加三欄升 schema 4） / 約束 3（單位命名、ARGB int、INV-03） / 驗證 2 | 1 + 規格回填 | 不再拆 |
| .3 | WARN（4 > 2） | 功能 1（一個 channel 契約的兩端） / 約束 1（字面一致） / 驗證 2 | 1 | 不再拆 |

.2 不再拆的理由：SPEC-009 治理條款 G-03 明文要求「新增設定欄位必須提升版本號並登錄演進表」，加欄與演進表回填是同一改動的兩面，拆開會製造無意義的交接並讓兩者可能不同步。

.3 不再拆的理由：Dart 側與 Swift 側是同一 channel 契約的兩端，acceptance 對兩側字面一致有硬性要求；拆成兩票會讓字面比對跨越 ticket 邊界，反而升高不一致風險。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 執行方式與拆分結果

本票原始 dispatch-readiness FAIL（acceptance 8 條 > 4），覆核後確認實質功能職責 3 個超過拆分門檻，經用戶裁示拆為子票執行。Phase 4 評估後追加第五張。

| 子票 | 職責 | 產出 |
|------|------|------|
| .1 | AppCursorLocator 常數類 | channel 契約四組 + FR-03/04/05/06 參數，68 行 |
| .2 | SettingsModel schema 4 | 三欄位 + v3-early/v3-late 相容 + SPEC-009 A.1 回填 |
| .3 | cursor_locator channel 骨架 | CursorLocator（可注入 MethodChannel）+ Swift CursorLocatorBridge |
| .4 | FR-06 預設值跨層對齊 | 單位 / 型別 / 單一來源三處歧異修正 |
| .5 | Phase 4 重構 | play 簽章對齊 SPEC + 轉換例外納入 catch + Color 轉換收斂 + SPEC 更新 |

.1 與 .2 平行派發（檔案互斥），.3 序列於 .1 之後，.4 為 .1/.2 整合後的銜接修正，.5 為 Phase 4 產出。

最終狀態：flutter test 147/147 通過、flutter analyze 0 issue。

### Phase 4 審查證據

執行 `/parallel-evaluation` 情境 B（重構評估），五視角並行：linux（Good Taste，universal_lens）、basil-writing-critic（文字品質，universal_lens）、Redundancy、Coupling、Complexity。

**結論：需要重構**。linux 評分 Acceptable；Redundancy 明確不支持「無需重構」；Complexity 的「不需重構」限於認知負擔維度（所有函式 CI 在優良至可接受範圍），三者在各自維度上無矛盾。

共 16 項發現，全數建票追蹤：

| 執行時機 | Ticket | 內容 |
|---------|--------|------|
| 本票 .5（已完成） | 1.4.0-W1-001.5 | play 簽章對齊 SPEC、轉換例外納入 catch、Color 轉換收斂、SPEC 宣告更新 |
| W2 P1 | 1.4.0-W2-007 | method channel 失敗安全包裝共用實作（順帶修 debugPrint 違規） |
| W2 P1 | 1.4.0-W2-009 | Dart 與 Swift 契約字面守衛測試（建議 W2-006 前落地） |
| W2 P1 | 1.4.0-W2-010 | 特效時長值域 0.5-3.0 執行期夾制 |
| W2 P1 | 1.4.0-W2-016 | Swift 參數型別轉換失敗的靜默行為與 log 可定位性 |
| W2 P2 | 1.4.0-W2-008 | AppCursorLocator 職責邊界拆分與渲染常數歸屬 |
| W2 P2 | 1.4.0-W2-011 | JSON 鍵字面收斂至 AppSettingsKeys |
| W2 P2 | 1.4.0-W2-012 | 測試重複斷言與同分支覆蓋整理 |
| W2 P2 | 1.4.0-W2-013 | 欄位預設值單一定義位置 |
| W2 P2 | 1.4.0-W2-017 | 程式碼註解與 ticket 文件的文字層缺陷（3 Critical + 12 Warning） |
| W2 trigger 綁定 | 1.4.0-W2-014 | SettingsModel 欄位膨脹方案評估（schema 5 或 300 行觸發） |
| W2 trigger 綁定 | 1.4.0-W2-015 | MainFlutterWindow.swift 多 bridge 單檔累積（900 行或第 4 bridge 觸發） |

各子票 Solution 的「Phase 4 審查證據」章節記錄該層的完整發現與論證。

### 本票執行過程的三項教訓

**一、檔案互斥不蘊含語意獨立**。.1 與 .2 經 parallel-check 確認檔案無衝突而平行派發，但兩票各自決定同一組設定的預設值（.1 為 int 毫秒 + MaterialColor，.2 為 double 秒 + 普通 Color），整合後三處歧異。兩票測試各自全綠（136/136 與 142/142），因兩處定義從未在同一編譯單元相遇。parallel-check 驗檔案衝突，驗不出「兩票是否會各自決定同一件事」。此教訓已於派發 .3/.4 時應用——兩者檔案同樣互斥但改為序列，因 .3 會引用 .4 正要改名的常數。

**二、worktree base 有兩種失效模式**。第一種是 origin/main 落後（有 hook 擋）；第二種是分支名重用——.1 的代理人依 Context Bundle 建了 feat 分支，PM 清理 worktree 時該分支 ref 留存，.3 的代理人 checkout 到它，base 缺 .2 與 .4。根源在 PM 撰寫的 Context Bundle 陳述了「PM 已建立 <branch>」這個會過期的環境狀態。本案 .3 僅引用未變動的 channel 契約四組常數，整合後實測 146/146 確認無害，但該無害是巧合而非設計。

**三、任務 context 指定解法形態會使執行者實作弱手段**。本票 .3 的 Context Bundle 寫「換算點須明確命名或註解標示」，.1 的 acceptance 寫「不使用無單位的 duration」，兩者皆指定了解法形態而非問題。執行者忠實實作該形態（具名轉換函式 + 六行註解 + 釘住 round() 的測試），而 SPEC-008:277 早已訂了更強的型別解 Duration。三明示角度：Why 正確、Consequence 正確、Action 跳過了「先問有沒有型別能承載」這一層。已記錄為 error-pattern PC-SCLK-003，並於 1.4.0-W2-017 追蹤 context 來源的同步修正（只修程式碼會讓下一張同類任務複製同一形態）。

### 自檢結果（Layer 1）

**執行者**: rosemary-project-manager（PM 收尾自檢）
**執行時機**: complete 前
**掃描類別**: A（文字品質）/ B（禁用字）/ C（Schema 結構）/ D（追蹤完整性）

| 類別 | 發現項目 | 處理方式 |
|------|---------|---------|
| B1/B2/B3 | Solution / Test Results / Problem Analysis 全文檢查，無簡體字、無 emoji、無禁用詞 | 通過 |
| C1 | Problem Analysis 已填（拆分決策與 dispatch-readiness 覆核紀錄），非佔位符 | 通過 |
| C2 | 本票追加內容均寫入既有 Schema 章節（Problem Analysis / Solution / Test Results），未新增自訂 H2 | 通過 |
| A1 | Solution 各段落含 Why / Consequence / Action：拆分決策段有覆核判定表與不再拆的理由；三項教訓段各含機制、後果與已採取的對應 | 通過 |
| A2 | 資訊優先序：各段先給結論（拆分結果表 / Phase 4 結論 / 教訓標題），後給論證 | 通過 |
| D1 | Phase 4 的 16 項發現全數對應 ticket ID，追蹤表無空白 Ticket 欄（quality-baseline 規則 5 / decision-trigger-binding 規則 1） | 通過 |
| D2 | 五張子票全數 completed，children 欄位與實際狀態一致 | 通過 |
| D3 | error-pattern PC-SCLK-003 已記錄並同步 README 索引；其防護 D（修正時同步修 context 來源）已由 1.4.0-W2-017 承接 | 通過 |

**自檢結論**: 通過。

**未執行項如實標註**：本票未派發 acceptance-auditor 做獨立驗收——五張子票各自完成時已由 PM 逐項查證世界平面（commit 存在性、分支歸屬、程式碼內容、測試實測數字），且 Phase 4 五視角評估已涵蓋跨層審查，判定重複派發驗收代理人的邊際價值低於其成本。此為 PM 判斷，非規則豁免。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
整合驗證於主 repo main 分支執行（非 worktree），涵蓋五張子票全部 merge 後的完整狀態。

| 階段 | commit | flutter test | flutter analyze |
|------|--------|-------------|----------------|
| .1 + .2 + .4 整合後 | 8092d5f | 146/146 通過 | No issues found |
| .5 Phase 4 重構後 | a4a91cf | 147/147 通過 | No issues found |

147 = 146 基線 + .5 淨增 1（cursor_locator_test 新增 2 筆 NaN 分量與例外契約測試、移除 1 筆已不適用的秒轉毫秒四捨五入測試——Duration 內部為整數微秒，該斷言隨 _durationSecondsToMs 移除而失去對象）。

**stale base 風險的實證排除**：1.4.0-W1-001.3 的代理人 worktree 誤 checkout 至 .1 留下的分支，base 不含 .2 與 .4，其回報的 140/140 是在缺兩票的程式碼上測得。PM 靜態分析判定 .3 僅引用未變動的 channel 契約四組常數，與 .2 的 schema 4 變更正交；整合後實測 146/146 與預期（142 + 4）吻合，確認判定成立。

**測試綠燈的邊界**（quality-baseline 規則 1）：146/146 全綠期間，main 上同時存在三項未被任何測試捕捉的缺陷——play() 簽章偏離 SPEC-008:277（需比對規格才能發現）、轉換點位於 try 區塊外使 NaN 輸入拋未捕捉例外（需損毀設定檔才觸發）、_colorToInt 手刻迴繞與 SDK 飽和語意分歧（需超出 sRGB 色域才顯現）。三者皆由 Phase 4 多視角評估發現，非測試訊號。

## Context Bundle

### 任務定位

v1.4.0 滑鼠定位器的共同前置。四張後續實作票依賴本票定案的介面：1.4.0-W2-005（全域熱鍵，需啟用開關與常數）、1.4.0-W2-006（特效層視窗管理，需 channel 骨架）、1.4.0-W3-001（視覺特效，需動畫參數常數）、1.4.0-W3-002（設定面板，需三個欄位）。介面反覆的成本會乘以四，故本票的契約須一次定案。

### 規範依據（依權威順序）

| 來源 | 提供什麼 |
|------|---------|
| docs/spec/data-management/SPEC-009-settings-storage-data-contract.md v1.1 | 資料契約的權威來源。A 區 INV-01 至 INV-09、A.1 演進表（含 v4 條目）、A.3 治理條款 G-01 至 G-04 |
| docs/spec/user-experience/SPEC-008-cursor-locator.md | FR-06 設定項目的語意定義（三欄位的用途、值域、預設值） |
| docs/spec/user-experience/domain-map.md | CursorLocatorDomain 的 bundle 邊界與依賴方向 |
| 1.4.0-W1-011 Solution（已完成） | schemaVersion 行為的實測結論與三項實作註記 |

SPEC-008 與 SPEC-009 對同一欄位的描述若有出入，以 SPEC-009 為準（資料契約是欄位定義的權威，SPEC-008 是功能需求）。

### INV-03 約束（本票最須注意的一條）

SPEC-009 將 INV-03（只加欄、不改型別、不刪欄）標為「本契約最脆弱的一條，無機械強制訊號」。schema 4 是 INV-03 生效後的第一次升版，本票的加欄方式即是該不變式的示範案例。

具體要求：

- 只新增欄位，不修改任何既有欄位的名稱、型別或語意
- 不移除任何既有欄位（含已無用者）
- schemaVersion 由 3 升為 4，並於 SPEC-009 A.1 演進表新增對應列（依 G-03：新增一般設定欄位必須提升版本號並登錄演進表）

1.4.0-W1-015 已建立 v1/v2/v3-early/v3-late 四組 golden fixture 迴歸測試（本票 blockedBy 該票）。若本票的改動違反 INV-03，那組測試會轉紅——這是方案 C 設計的機械訊號，不可為了讓測試通過而修改 fixture 內容。

### W1-011 的三項實作註記（已納入 acceptance）

| 註記 | 理由 |
|------|------|
| 時長欄位名含單位（如 cursorEffectDurationMs） | W1-011 實測 E3：語意或單位變更現行機制完全無法偵測（birthDate 若由毫秒改秒，946684800 會被讀成 1970-01-12 並在下次存檔永久寫入錯誤值）。欄位名帶單位是唯一能在 code review 階段攔截此類變更的訊號 |
| 主色調以 ARGB int 經 _asColor 存取 | 與既有欄位的處理方式一致；Color 物件直接序列化會在 Flutter 版本升級時產生格式風險 |
| round-trip 測試涵蓋 v3 兩種形態 | v3-early 與 v3-late 的 schemaVersion 皆為 3，差異僅在 bindingsSeeded 鍵是否存在。只測其一會漏掉 _migrateBindingSeed 的觸發分支 |

### schema 4 三欄位的定義來源

SPEC-009 A.1 的 v4 條目已記錄語意、型別與預設值，但來源標註為 SPEC-008 FR-06（規格）而非 code——因為當時尚未實作。本票是把該規劃落為實作。

實作後須回頭確認 SPEC-009 A.1 的 v4 條目與實際 code 一致；若有出入，以實作為準並更新 SPEC-009（於 Solution 記錄差異與更新內容）。

### 執行約束

- 不修改既有欄位（INV-03）
- 不修改 1.4.0-W1-015 建立的 fixture 內容
- Swift 端的 channel 名、方法名、參數鍵須與 AppCursorLocator 常數字面一致，兩側任一硬編碼即視為未達成
- 本票只做骨架：channel handler 收到 play 僅 NSLog，特效實作留 1.4.0-W2-006 與 1.4.0-W3-001

### 驗收驗證命令

| 驗收條件 | 驗證命令 | 期望 |
|---------|---------|------|
| 常數集中且兩側一致 | grep 常數值於 lib/app_constants.dart 與 macos/Runner/ 比對 | 字面一致，無散落字串 |
| schema 4 與 round-trip | flutter test test/models/ | 全綠，含 v3 兩形態 |
| INV-03 未被違反 | flutter test（含 W1-015 的 golden fixture） | 全綠 |
| 全套件無回歸 | flutter test && flutter analyze | 測試全綠、analyze 無警告 |

### Commit Policy

單一 commit，訊息格式：feat(1.4.0-W1-001): 滑鼠定位器基礎建設（常數 + schema 4 + channel 骨架）

本票修改 lib/ 與 test/，不在 branch-verify-hook 豁免清單，須在 feature branch 上進行（PM 於派發前建立）。禁用 git commit --amend（見 PC-SCLK-001）。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-07-31T11:23:24
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending
