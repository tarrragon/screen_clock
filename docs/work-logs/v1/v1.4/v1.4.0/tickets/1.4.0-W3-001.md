---
id: 1.4.0-W3-001
title: 實作 三種視覺特效繪製（聚光燈 / 邊框閃爍 / 游標波紋）
type: IMP
status: in_progress
version: 1.4.0
wave: 3
priority: P0
parent_id: null
children:
- 1.4.0-W3-001.1
- 1.4.0-W3-001.2
- 1.4.0-W3-001.3
- 1.4.0-W3-001.4
blockedBy:
- 1.4.0-W2-006
relatedTo: []
spawned_tickets: []
source_ticket: 1.4.0-W1-009
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: 三種特效同票（共用視窗與時間軸），與視窗管理分票
  rationale: 三者共用同一 NSWindow 與 CVDisplayLink 時間軸，拆開反增跨票同步成本（1.4.0-W1-009 備註）；但與視窗生命週期職責不同故分票
who:
  current: parsley-flutter-developer
  history: {}
what: 實作 三種視覺特效繪製（聚光燈 / 邊框閃爍 / 游標波紋）
when: 特效層視窗管理票完成後（需 NSWindow 與 CVDisplayLink 就緒）
where:
  layer: Presentation
  files:
  - macos/Runner/
why: SPEC-008 FR-03 聚光燈效果、FR-04 螢幕邊框閃爍、FR-05 游標波紋擴散；UC-06 主成功場景步驟 4；PROP-003 checklist
  第 3 項
how:
  task_type: Implementation
  strategy: 於既有特效 NSWindow 的 CALayer 上繪製三種效果：聚光燈為全螢幕壓暗加游標位置的明亮圓形區域；邊框閃爍為螢幕四周邊框閃三次；游標波紋為三圈同心圓向外擴散並淡出；三者共用同一
    CVDisplayLink 時間軸，總時長由 SettingsModel 的特效時長設定驅動；主色調由設定的 ARGB int 經 channel 傳入（見
    SPEC-009 A 區欄位定義）；所有尺寸、圈數、透明度曲線等數值定義為 Swift 側常數，禁止散落魔術數字
acceptance:
- '[ ] 聚光燈壓暗全螢幕並在游標位置保留明亮圓形區域'
- '[ ] 螢幕邊框閃爍三次'
- '[ ] 游標波紋為三圈同心圓擴散淡出'
- '[ ] 三種特效總時長受設定的特效時長驅動'
- '[ ] 主色調套用設定值，非硬編碼顏色'
- '[ ] 特效數值定義為具名常數，無散落魔術數字'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-08-03T18:11:25'
completed_at: null
created: '2026-07-31'
updated: '2026-07-31'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

實作 三種視覺特效繪製（聚光燈 / 邊框閃爍 / 游標波紋）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 三方整併任務規格（PM 於三張子票完成後撰寫）

四張子票的實作已全部完成。`.1`（時間軸基礎）與 `.2`（聚光燈）已 merge 進 main；
`.3`（邊框閃爍）與 `.4`（波紋）仍在各自 worktree 分支，需整併。

| 子票 | 分支 | 狀態 |
|------|------|------|
| `.1` 時間軸基礎 | 已刪除 | completed，已在 main |
| `.2` 聚光燈 FR-03 | 已刪除 | completed，已在 main（merge `08d4577`） |
| `.3` 邊框閃爍 FR-04 | `worktree-agent-ab637cb8d8a250971` | 實作完成，HEAD `0acd337`，待整併 |
| `.4` 波紋 FR-05 | `worktree-agent-acf2c0ca56638c037` | 實作完成，HEAD `d15d581`，待整併 |

### 三處預期衝突與解法

PM 已實測 `.4` 對含 `.2` 的 main 之衝突面，三處全部落在派發前預告的位置：

**1. `MainFlutterWindow.swift` 的 renderers 陣列**

三張各自把自己那項加進 `CursorLocatorCompositeRenderer(renderers: [...])`。
陣列順序即 sublayer 疊加順序（先加者在下），**PM 指定 z-order 為**：

```
[CursorLocatorSpotlightRenderer(), CursorLocatorRippleRenderer(), CursorLocatorBorderFlashRenderer()]
```

理由：聚光燈是覆蓋全螢幕的壓暗遮罩，必須墊底，否則會蓋住波紋與邊框；波紋在游標處疊於壓暗之上；邊框位於最外層不受壓暗影響。此順序無法由任一子票單獨決定——三個代理人各在獨立 worktree，都只看得到空陣列。

**2. `CursorLocatorEffectConstants`（`CursorLocatorEffectWindow.swift`）**

三張各新增自己的常數區塊（`.2` 五個、`.3` 三個、`.4` 若干）。三方並列附加即可，
不刪除任何一方的常數，保持各自的 doc comment。

**3. `macos/Runner.xcodeproj/project.pbxproj`**

三張各註冊兩個新檔（renderer + 測試）到對應 target。各自的 UUID 前綴不同
（`.2` 用 `5C1A0B0012D34E5A9F0A00xx`、`.4` 用 `RIP1E`/`RIP2E`），不會撞號，
但同區塊相鄰行會衝突。**此檔解錯會使 Xcode 專案無法開啟**，須逐區塊確認
三方條目都在（PBXBuildFile / PBXFileReference / PBXGroup children / PBXSourcesBuildPhase files）。

### 測試基準校正

`.3` 與 `.4` 兩張獨立實測 main 的 XCTest baseline 為 **85**（非 ticket 早期記載的 84）。
PM 亦以逐檔 `grep -c "func test"` 加總複驗得 85。

三張各自在自己的 worktree 實測：`.2` → 99、`.3` → 101、`.4` → 97。三者的 base 都只含
自己那一張，故整併後的總數**不是**任一數字，須實跑確認。預期為 85 + 15 + 16 + 12 = 128，
但這是相加推算，以 `scripts/run-tests.sh` 實跑結果為準。

### worktree 前置（三張都遇到）

新建 worktree 初次跑 `xcodebuild test` 會因缺 `macos/Pods/` 與 `macos/Flutter/ephemeral/`
而失敗，屬 build 層問題非程式碼問題。執行 `flutter pub get` +
`flutter build macos --debug --config-only` 後恢復。`macos/Pods/` 已被 gitignore，不進 commit。

### 整併後必須確認的事

三種特效首次同時存在於同一 `contentLayer`，以下是各子票單獨測試涵蓋不到的：

- 三個 renderer 的 sublayer 疊加順序符合上述 z-order
- `attach` / `detach` 三者皆被 composite 正確廣播，`detach` 後 layer 無殘留 sublayer
- 三種特效的常數無命名碰撞（各自前綴 `spotlight` / `flash` / `ripple`）
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

### 整併結果

四張子票已全部整併至單一分支。`.1`／`.2` 原已在 base，本次以 merge（非 rebase／
cherry-pick）納入 `.3` 與 `.4`，兩個來源分支歷史未被改寫。

| 步驟 | commit | 內容 |
|------|--------|------|
| merge `.3` | `e07a09b` | 邊框閃爍 FR-04（來源 HEAD `0acd337`） |
| merge `.4` | `66a68d7` | 波紋 FR-05（來源 HEAD `d15d581`） |
| 整併測試 | `2d94609` | CursorLocatorEffectCompositionTests |

三個 renderer 的實作內容、`CursorLocatorEffectController`、
`WindowCursorLocatorSurface`、`LayerHostingView` 均未改動。

### 三處衝突解法

**1. renderers 陣列（`MainFlutterWindow.swift`）** — 依 PM 指定 z-order 定案為
`[Spotlight, Ripple, BorderFlash]`，順序未更動。註解改寫為說明「陣列順序即
sublayer 疊加順序」與三者的相對理由，並標明此決定屬整併票（各子票在獨立
worktree 內都只看得到空陣列，無法單獨決定）。

**2. `CursorLocatorEffectConstants`** — 三方常數並列附加，未刪任何一方，各自
doc comment 保留。前綴 `spotlight` / `flash` / `ripple` 無命名碰撞（編譯通過即
為證，同一 enum 內重名會編譯失敗）。

**3. `project.pbxproj`** — 兩次 merge 共 5 個衝突區塊，全部為「保留雙方」。
合併後逐區塊實測確認三方條目齊全：

| 區塊 | Spotlight | BorderFlash | Ripple |
|------|-----------|-------------|--------|
| PBXBuildFile | 2（renderer + test） | 2 | 2 |
| PBXFileReference | 2 | 2 | 2 |
| PBXGroup children | Runner 1 + RunnerTests 1 | 1 + 1 | 1 + 1 |
| PBXSourcesBuildPhase | Runner 1 + RunnerTests 1 | 1 + 1 | 1 + 1 |

Xcode 專案可正常載入的實證：`xcodebuild build-for-testing` 與
`xcodebuild test` 皆成功，若 pbxproj 結構損壞兩者都會在解析階段失敗。

### 整併層級測試（新增）

`macos/RunnerTests/CursorLocatorEffectCompositionTests.swift`（5 case）。受測對象
是 production 的 renderer 組合本身，涵蓋各子票單獨測不到的三件事：

| 測試 | 涵蓋 |
|------|------|
| `testAttach_stacksSpotlightBelowRippleBelowBorderFlash` | sublayer 疊加順序：底層帶 mask（聚光燈）、中段 3 個 CAShapeLayer（波紋）、最外層帶 borderWidth（邊框） |
| `testAttach_isBroadcastToAllThreeRenderers` | composite 對 attach 的廣播，三者皆接上 |
| `testRender_doesNotAddSublayersPerFrame` | render 只更新不新增（逐幀累積會無上限成長） |
| `testDetach_removesEverySublayer` | detach 後 layer 無殘留 sublayer |
| `testReattach_afterDetachDoesNotAccumulateLayers` | 重播不累積圖層 |

### 自檢結果

- [x] 兩個來源分支以 merge 納入，歷史未改寫（`git log` 可見兩個 merge commit）
- [x] z-order 為 `[Spotlight, Ripple, BorderFlash]`，未更動
- [x] 三方常數並列，無刪除、無命名碰撞
- [x] pbxproj 四類區塊三方條目齊全（上表為實測 grep 結果）
- [x] 三個 renderer 實作與控制器／surface／LayerHostingView 未改動
- [x] `scripts/run-tests.sh` 全綠（實跑，非推算）
- [x] 編譯警告驗證非空訊號：獨立 `-derivedDataPath` 全新建置，SwiftCompile/
      SwiftDriver 任務 143 筆且改動檔（三個 renderer + 新測試檔）皆在編譯清單內
      （TEST-SCLK-001）。`macos/Runner/` 與 `macos/RunnerTests/` 零警告；
      13 筆警告全數來自 Pods（HotKey）、XCTest dylib 版本、Flutter Assemble
      腳本，屬既有非本次引入。scratch DerivedData 已刪除。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 執行指令

`./scripts/run-tests.sh`（flutter test + xcodebuild test，單一入口），退出碼 0，
輸出結尾 `=== 全部測試通過 (flutter test + xcodebuild test) ===`。

worktree 前置：`flutter pub get` + `flutter build macos --debug --config-only`
（如 Problem Analysis 預告，新建 worktree 缺 `macos/Pods/` 與
`macos/Flutter/ephemeral/`，執行後恢復）。

### 實跑測試數

| 套件 | 通過 | 失敗 |
|------|------|------|
| flutter test | 179 | 0 |
| xcodebuild test（XCTest） | 132 | 0 |

XCTest 逐類實測分佈：

| 測試類別 | 數量 |
|---------|------|
| CursorLocatorEffectControllerLifecycleTests | 19 |
| CursorLocatorBorderFlashRendererTests | 17 |
| CursorScreenLocatorTests | 15 |
| CursorLocatorSpotlightRendererTests | 14 |
| CursorLocatorEffectFrameTests | 14 |
| CursorLocatorSurfaceContractTests | 12 |
| CursorLocatorRippleRendererTests | 12 |
| CursorLocatorFadeAlphaTests | 9 |
| CursorLocatorEffectControllerScreenTests | 8 |
| CursorLocatorErrorMappingTests | 6 |
| CursorLocatorEffectCompositionTests（本票新增） | 5 |
| RunnerTests | 1 |
| 合計 | 132 |

### 與 ticket 預估的差異

Problem Analysis 預估整併後 XCTest 為 128（85 + 15 + 16 + 12 的相加推算）。
實跑為 132，其中 5 個是本票新增的整併層級測試，故純整併後為 127，較推算少 1。

差異來源可定位：推算把 `.3` 的貢獻記為 16（101 減 85），但
`CursorLocatorBorderFlashRendererTests` 實測為 17 個 case，
`CursorLocatorSpotlightRendererTests` 實測 14（推算記 15）。兩者相抵後淨少 1。
推算採用「各 worktree 總數相減」，與逐類實測有一格差距；ticket 已預告 128 為
推算值非斷言，此處以實跑數為準。無測試遺失：132 等於 127 整併加 5 新增，
0 失敗、0 skip。

### 編譯驗證（避開 TEST-SCLK-001 空訊號）

`xcodebuild build-for-testing -workspace macos/Runner.xcworkspace -scheme Runner
-configuration Debug -destination 'platform=macOS' -derivedDataPath /tmp/w3001-dd`
結果為 `** TEST BUILD SUCCEEDED **`。

SwiftCompile/SwiftDriver 任務 143 筆（非零，證明確實重編而非增量跳過），
且 `CursorLocatorSpotlightRenderer.swift`、`CursorLocatorBorderFlashRenderer.swift`、
`CursorLocatorRippleRenderer.swift`、`CursorLocatorEffectCompositionTests.swift`
四個改動檔皆出現在編譯清單內。

警告 13 筆，全數位於 `macos/Pods/`（HotKey 的 protocol conformance）、
XCTest dylib 部署版本、Flutter Assemble 的 Run Script 三處，屬既有問題；
`macos/Runner/` 與 `macos/RunnerTests/` 自身零警告。scratch DerivedData 已刪除。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.4.0-W1-009 | chars: 284 -->

### Task Reference
- 1.4.0-W1-009 what: 檢視 1.4.0-W1-001 與 PROP-003/SPEC-008/UC-06 的規格一致性

### Rationale Chain
- 1.4.0-W1-009 why: 文件層對齊新規範後，W1-001 的 Context Bundle 與驗收條件需回頭確認引用正確且規格未漂移

### Related Files
- docs/work-logs/v1/v1.4/v1.4.0/tickets/1.4.0-W1-001.md  # from 1.4.0-W1-009
- docs/proposals/PROP-003-cursor-locator.md  # from 1.4.0-W1-009
- docs/spec/user-experience/SPEC-008-cursor-locator.md  # from 1.4.0-W1-009
- docs/usecases/UC-06-locate-cursor.md  # from 1.4.0-W1-009

### UC Context
#### UC-06: 快速定位滑鼠游標
Spec 位置: docs/app-use-cases.md:154
主要流程:
  1. **觸發**
  2. **判定目標螢幕**
  3. **建立特效視窗**
  4. **播放特效**
  5. **結束**
  6. **使用者找到游標**
