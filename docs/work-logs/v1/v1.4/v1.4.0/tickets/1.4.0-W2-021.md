---
id: 1.4.0-W2-021
title: 補上 SettingsModel.copyWith 的特效時長值域夾制
type: IMP
status: pending
version: 1.4.0
wave: 2
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: 獨立建票，不併入 SR-4 的 getter 重構
  rationale: SR-5 屬正確性問題、SR-4 屬設計改善，綁一起會讓正確性修復等待低優先重構
who:
  current: 待派發
  history: {}
what: 補上 SettingsModel.copyWith 的特效時長值域夾制
when: 1.4.0-W3-002 設定面板落地後風險實體化，越早越好
where:
  layer: 待定義
  files:
  - lib/models/settings_model.dart
  - test/models/settings_model_test.dart
why: 1.4.0-W2-010 在 fromJson 加了 _asClampedDuration，但 copyWith 未夾制。SettingsController.update
  走的正是 copyWith，故執行期主路徑（設定面板 → update → copyWith → CursorLocatorHotkeyController._onTriggered
  → CursorLocator.play）可讓越界值直達原生層。W2-010 當時的範圍決策理由是「copyWith 呼叫端只有測試與尚未實作的設定面板」，該前提已因
  1.4.0-W3-002 動工而失效。目前唯一防線是設定面板 Slider 的 min/max 邊界，屬 UI 層偶然保護而非模型層保證；任何非 Slider
  的 update 路徑（程式化設定、未來的數值輸入框、匯入設定）都會繞過它。來源：1.4.0-W2-005 Phase 4 pe-linux 發現8（SR-5）
how:
  task_type: Implementation
  strategy: 沿用 W2-010 已建立的 _asClampedDuration 語意（越界但有限→夾至邊界；NaN/Infinite→落回預設），在 copyWith
    對 cursorLocatorEffectDurationSeconds 套用同一夾制；須與 W2-010 的實作共用同一函式而非另寫一份，避免兩處夾制語意漂移。同時評估建構子路徑是否需要同等保護——W2-010
    記錄 const 建構子的 initializer list 無法呼叫任意函式，若確認無法保護則在 Solution 明示此為已知邊界
acceptance:
- '[ ] copyWith 對越界值夾制至邊界，測試涵蓋下界/上界/界內/NaN/Infinite'
- '[ ] 夾制邏輯與 fromJson 共用同一函式，非兩份實作'
- '[ ] 已評估並記錄 const 建構子路徑是否可同等保護'
- '[ ] 全套件測試綠燈'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-07-31'
updated: '2026-07-31'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補上 SettingsModel.copyWith 的特效時長值域夾制

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending
