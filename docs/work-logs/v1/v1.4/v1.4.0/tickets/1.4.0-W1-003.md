---
id: 1.4.0-W1-003
title: 遷移 proposals-tracking.yaml 至新版 tracking schema
type: DOC
status: completed
version: 1.4.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy:
- 1.4.0-W1-002
relatedTo: []
spawned_tickets:
- 1.4.0-W1-018
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-documentation-integrator
  history: {}
what: 遷移 proposals-tracking.yaml 至新版 tracking schema
when: doc status 回報提案總數 0，現有 dict-keyed 格式不符 tracking_schema.py SSOT
where:
  layer: Presentation
  files:
  - docs/proposals-tracking.yaml
why: 待定義
how:
  task_type: Implementation
  strategy: 依 .claude/skills/doc/doc_system/core/tracking_schema.py 的 PROPOSALS_TRACKING_SCHEMA
    改寫：proposals 由 dict-keyed 改 list（entry 補 id 欄位）、confirmed 改名 confirmed_at、補頂層
    usecases 與 specs 兩個 key；同時校準 PROP-001/002 狀態（v1.0.0/v1.3.0 已發布應為 implemented，checklist
    逐項回填 verified_by ticket ID）；以 doc status 與 doc list proposals 驗證解析成功
acceptance:
- '[x] proposals 為 list 格式且每個 entry 含 id/title/status'
- '[x] 頂層含 proposals/usecases/specs 三個 key'
- '[x] 確認日期欄位名為 confirmed_at'
- '[x] doc status 正確回報 3 個提案（非 0）'
- '[x] PROP-001/PROP-002 狀態與 checklist 依已發布版本校準'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-07-29T15:51:19'
completed_at: '2026-07-29T16:01:35'
created: '2026-07-29'
updated: '2026-07-29'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

遷移 proposals-tracking.yaml 至新版 tracking schema

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：文件變更的背景和原因）

### 影響範圍

（選填：哪些文件或章節受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### dispatch-readiness FAIL 的 PM 覆核

輸出：閾值 1 FAIL（acceptance 5 > 4）。依 ticket SKILL.md，閾值 1 以 acceptance 條目近似功能職責數，含驗證類條目時高估，須 PM 覆核。

| acceptance 條目 | 性質 |
|----------------|------|
| proposals 為 list 且 entry 含 id/title/status | 職責 1 產出（schema 遷移） |
| 頂層含三個 key | 職責 1 產出（同一次改寫的不同面向） |
| 確認日期欄位名為 confirmed_at | 職責 1 產出（同上） |
| doc status 回報 3 個提案 | 驗證（職責 1 的驗證手段） |
| PROP-001/002 狀態與 checklist 校準 | 職責 2 產出（狀態校準） |

實際功能職責 2 項：schema 格式遷移、提案狀態校準。前三條是同一次 YAML 改寫的三個檢查面向，非三個獨立職責。兩項職責同票的理由：改寫 YAML 時必然逐 entry 處理，狀態校準是同一次遍歷的附帶動作，拆開反而要讀寫同檔兩次。

結論：不拆分，直接派發。
Schema 遷移完成：proposals dict→list（每筆補 id）、confirmed→confirmed_at、新增頂層 usecases/specs（最小可用形式：id+title+status+path，僅含既有檔案 UC-01/02/03/06、SPEC-001~009）。狀態校準：PROP-001/002 依已發布版本（v0.1.0+v0.2.0、v1.3.0）判定 implemented，checklist 逐項回填 verified_by；PROP-003 判定 confirmed（v1.4.0 進行中）。
## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

本 ticket 有非例行判斷（checklist verified_by 的 ticket ID 對應），記錄如下供覆核：

### verified_by 對應依據

PROP-001（v0.1.0 + v0.2.0，已發布，判定 implemented）：

| checklist 項目 | verified_by | 依據 |
|---------------|-------------|------|
| macOS 透明全螢幕視窗 | 0.1.0-W1-005 | 手動驗收 UC-01+UC-02 ticket，涵蓋 frameless/always-on-top/no-shadow 驗證 |
| macOS click-through | 0.1.0-W1-005 | 同上 ticket 涵蓋 UC-02 click-through 驗證 |
| 螢幕中央時鐘 widget | 0.2.0-W1-007 | 手動驗收 + CPU 量測 ticket，涵蓋每秒更新與預設樣式驗證 |
| Cmd+Q 退出 timer dispose | 0.2.0-W1-003 | Timer.periodic + dispose 清理實作 ticket |

PROP-002（v1.3.0，已發布，判定 implemented）。查法：`git log --oneline -- macos/Runner/MainFlutterWindow.swift` 逐 commit 對照 ticket ID（W2-003 ticket 文字內「本票只做 tap 建立+分派框架,具體動作留 W3」與實際 commit `e5cc4b9 feat(1.3.0-W2-003): CGEventTap 整合 + 拖曳滾動 + 預設綁定` 不一致，以 git commit 實際內容為準，非 ticket 計畫文字）：

| checklist 項目 | verified_by | 依據 |
|---------------|-------------|------|
| 綁定 domain（模型+序列化+schema v3） | 1.3.0-W2-002 | ticket 標題與 acceptance 直接對應 |
| CGEventTap 全域攔截 + 移除 Sandbox + 輔助使用權限 | 1.3.0-W2-001, 1.3.0-W2-003 | W2-001 完成 sandbox 移除與權限查詢/請求；W2-003 完成 CGEventTap 實際整合。兩票缺一不構成完整項目，故填兩 ID（逗號分隔，非虛構單一 ID） |
| 動作①拖曳滾動 | 1.3.0-W2-003 | git commit e5cc4b9 證實拖曳滾動實際在此票落地（非 ticket 計畫文字所稱的 W3） |
| 動作②綁定快捷鍵 | 1.3.0-W5-001 | commit 121ebaf「原生 HotkeyAction 合成執行（FR-05）」 |
| 設定面板整合 | 1.3.0-W4-003, 1.3.0-W4-004 | W4-003 綁定清單顯示/刪除；W4-004 新增流程+偵測捕捉+方向/靈敏度參數調整，兩票共同構成面板整合 |
| 未授權安全停用 + 既有功能非 sandbox 回歸 | null | 查無對應開發 ticket。v1.3.0-main.md 記錄此為「聯合驗收項（與用戶一起，不在開發票）」，2026-06-16 記錄「非 sandbox 全功能回歸」通過，但無 ticket ID 可引用。依指示不虛構 ID，status 標 done（有實證記錄）、verified_by 留 null |

PROP-003（v1.4.0，進行中）：checklist 維持 pending/null，因版本尚在 Wave 1，尚無完成證據。

### usecases/specs 收錄範圍

usecases 僅收錄已存在檔案的 UC-01/02/03/06；UC-04/UC-05（PROP-002 usecase_refs 引用）尚無獨立檔案（`docs/app-use-cases.md` 標註「待 1.4.0-W1-005 補建」），故未列入 usecases 陣列，避免 path 欄位指向不存在檔案。specs 收錄 docs/spec/ 下全部 9 個既有 SPEC 檔案（含 SPEC-009，非本提案引用但屬既有文件，一併納入索引完整性）。

---

## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

本 ticket 為 schema 遷移，非程式碼變更，無自動化測試；依 Context Bundle「驗收驗證命令」逐條實測，實際輸出如下：

**[1] doc status**（`.claude/skills/doc/.venv/bin/doc status`，需用 venv 內建的 `doc` console script，`python3 -m doc_system.cli` 因 cli.py 缺 `if __name__ == "__main__"` guard 不會實際執行 main()）：
```
=== 提案追蹤狀態摘要 ===
提案總數: 3

  confirmed       1
  implemented     2
```
符合期望：回報 3 個提案（非 0）。

**[2] doc list proposals**：
```
=== proposals ===
ID              標題                                  狀態
------------------------------------------------------------
PROP-001        桌面螢幕透明時鐘遮罩                          draft
PROP-002        滑鼠按鍵綁定（拖曳滾動與快捷鍵）                    draft
PROP-003        多螢幕環境滑鼠定位器                          draft
```
符合期望：列出 PROP-001/002/003。附註：「狀態」欄顯示 draft，來源是 `docs/proposals/PROP-*.md` 各檔 frontmatter 的 status 欄位（list_cmd.py 讀取獨立文件 frontmatter，非讀 tracking.yaml），與本 ticket 修改的 proposals-tracking.yaml status 欄位是不同資料來源，不在本 ticket where.files 範圍內，未一併校準。

**[3] doc nav PROP-003**：
```
=== 引用地圖: PROP-003 — 多螢幕環境滑鼠定位器 ===

  related_proposals:
    - PROP-001
    - PROP-002
  spec_refs:
    - spec/user-experience/SPEC-008-cursor-locator.md
  usecase_refs:
    - usecases/UC-06-locate-cursor.md
```
符合期望：列出關聯 spec 與 UC。

**[4] YAML 合法性**（`python3 -c "import yaml,sys; yaml.safe_load(open('docs/proposals-tracking.yaml'))"`）：無例外，額外驗證 `d=yaml.safe_load(...); list(d.keys())` 回傳 `['proposals', 'usecases', 'specs']`。

**[5] 頂層 key 齊備**（`grep -E "^(proposals|usecases|specs):" docs/proposals-tracking.yaml`）：
```
proposals:
usecases:
specs:
```
三行皆命中，符合期望。

五條驗收驗證命令全數通過，無放寬條件。

---

## Context Bundle

### 規範依據

- .claude/skills/doc/doc_system/core/tracking_schema.py — PROPOSALS_TRACKING_SCHEMA 是權威 schema（建立端），本 ticket 依它改寫資料
- .claude/skills/doc/references/tracking.md — tracking 規範描述層

注意兩者不一致時的判定順序：canonical 常數（tracking_schema.py）優先於文件描述（tracking.md）。references/tracking.md 的範例仍用舊的 dict-keyed 格式與 confirmed 欄位名，那是待同步的下游描述，不可據以改寫資料。此判定依 .claude/rules/core/tool-output-trust-rules.md 規則 6。

### 現況證據

doc status 輸出：提案總數 0（實際 docs/proposals-tracking.yaml 有 3 個提案）。原因為現有格式是 dict-keyed-by-id，新 schema 要求 list。

### Schema 差異對照

| 面向 | 現況（舊） | 目標（PROPOSALS_TRACKING_SCHEMA） |
|------|-----------|----------------------------------|
| 頂層 key | 僅 proposals | proposals / usecases / specs 三個 |
| proposals 結構 | dict，key 為 PROP-NNN | list，每個 entry 含 id 欄位 |
| entry 必填 | 無明確定義 | id / title / status |
| 確認日期欄位名 | confirmed | confirmed_at |
| entry 選填 | — | priority / completed_note / target_version / proposed / source / spec_refs / usecase_refs / ticket_refs / checklist / canonical_ssot / tracking_ticket / depends_on |

usecases 與 specs 兩個新頂層 key 的 entry 結構未在 schema 常數中明確定義必填欄位；建立時採最小可用形式（id + title + status + 指向檔案路徑），並在 ticket 的 Solution 章節記錄所採結構與理由。

### 狀態校準（第二項職責）

現有 tracking 的提案狀態與實際發布進度脫節，須依 docs/todolist.yaml 的版本完成狀態校準：

| 提案 | target_version | 版本實際狀態 | 應校準為 |
|------|---------------|-------------|---------|
| PROP-001 | v0.1.0（實際跨 v0.1.0 至 v1.0.0） | 已發布 | 依 checklist 逐項核對後判定 implemented 或 confirmed |
| PROP-002 | v1.3.0 | 已發布 | 同上 |
| PROP-003 | v1.4.0 | 進行中（active） | confirmed（已進入實作規劃） |

checklist 每項的 verified_by 須填實際完成該項的 ticket ID。查法：ticket track list --all 逐版本比對，或 grep docs/work-logs/ 下 ticket 的 what 欄位。查不到對應 ticket 的項目，verified_by 留 null 並在 Solution 記錄，不虛構 ticket ID。

### 執行約束

- 不改變任何提案的實質內容（title / target_version / checklist item 文字維持原樣）
- spec_refs 路徑須用 1.4.0-W1-002 重命名後的新路徑（本 ticket blockedBy W1-002，執行時該重命名已完成，須先 ls docs/spec/ 確認實際檔名）
- 狀態校準以版本發布事實為依據，非以感覺推測；判定依據寫入 Solution 章節

### 驗收驗證命令

| 驗收條件 | 驗證命令 | 期望 |
|---------|---------|------|
| 格式可解析 | doc status | 回報 3 個提案（非 0） |
| list 可運作 | doc list proposals | 列出 PROP-001/002/003 |
| 導航可運作 | doc nav PROP-003 | 列出關聯 spec 與 UC |
| YAML 合法 | python3 -c "import yaml,sys; yaml.safe_load(open('docs/proposals-tracking.yaml'))" | 無例外 |
| 頂層 key 齊備 | grep -E "^(proposals|usecases|specs):" docs/proposals-tracking.yaml | 三行 |

### Commit Policy

單一 commit，訊息格式：docs(1.4.0-W1-003): 遷移 proposals-tracking.yaml 至新版 schema 並校準提案狀態

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-07-29
**Executing Agent**: thyme-documentation-integrator
**Review Status**: pending

變更摘要：`docs/proposals-tracking.yaml` 全檔重寫。頂層 `proposals` 由 dict-keyed-by-id 改為 list（每筆補 `id` 欄位）；`confirmed` 欄位改名 `confirmed_at`；新增頂層 `usecases`（UC-01/02/03/06，最小可用形式 id+title+status+path）與 `specs`（SPEC-001~009，同形式）兩個 key。狀態校準：PROP-001、PROP-002 由 draft 改為 implemented（依 v0.1.0+v0.2.0、v1.3.0 已發布事實），checklist 逐項補 `verified_by`（詳見 Solution 章節對應表，1 項查無對應 ticket 依指示留 null）；PROP-003 由 draft 改為 confirmed（v1.4.0 進行中）。title / target_version / checklist item 文字、spec_refs 路徑（已由 1.4.0-W1-002 完成重命名，本 ticket 未再變動）皆未更動。
