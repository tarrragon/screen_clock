---
id: 1.4.0-W2-006.2.2
title: 接線 production 視窗驅動與 channel 錯誤回傳
type: IMP
status: in_progress
version: 1.4.0
wave: 2
priority: P0
parent_id: 1.4.0-W2-006.2
children: []
blockedBy:
- 1.4.0-W2-006.2.1
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: parsley-flutter-developer
  history: {}
what: 接線 production 視窗驅動與 channel 錯誤回傳
when: 1.4.0-W2-006.2.1 完成後（依賴其控制器介面）
where:
  layer: Infrastructure
  files:
  - macos/Runner/CursorLocatorEffectWindow.swift
  - macos/Runner/CursorLocatorErrorMapping.swift
  - macos/Runner/MainFlutterWindow.swift
  - macos/RunnerTests/
why: SPEC-008 FR-02、EX-06-03 視窗建立失敗；UC-06 場景 06d 覆蓋全螢幕應用。母票 Phase 3a §四步驟 5-7 為 production
  接線層，引入真實 NSWindow、CVDisplayLink 與 Flutter 型別，並含唯一一處 MainFlutterWindow.swift 改動
how:
  task_type: Implementation
  strategy: 依母票 Phase 3a §四步驟 5-7 實作：production surface 依 Phase 1 §2.5 十二項屬性契約建立視窗且不搶焦點，驅動以顯示器垂直同步機制回呼並一律派回
    main thread；建立錯誤對應檔 CursorLocatorErrorMapping.swift（N-1 裁定產物，不放入 CursorLocatorEffectWindow.swift
    以免該檔被迫引入 Flutter 依賴）；MainFlutterWindow.swift 僅改 handlePlay 委派一處，既有參數解析失敗分支完全不動。測試群組順序
    E 先於 F（F 需啟動宿主 app，為唯一可能被環境阻擋者，放最後隔離 B-6/B-7 風險）
acceptance:
- '[ ] 特效視窗屬性符合 Phase 1 §2.5 十二項契約（透明、ignoresMouseEvents、level 高於假全螢幕應用等），以群組 F 的
  XCTest 對真實 NSWindow 斷言'
- '[ ] 錯誤對應函式與 bridge 委派使 E_CL_WINDOW 與 E_CL_SCREEN 經 play 的 result 正確回傳，群組 E 以字串字面逐字斷言錯誤碼（禁引用實作端列舉）'
- '[ ] 實機清單 M-1 至 M-8 逐項執行並記錄結果，含三種全螢幕情境分列；B-7 權限彈窗是否觸發須明確回報（未觸發亦須寫）'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-08-01T14:36:09'
completed_at: null
created: '2026-08-01'
updated: '2026-08-01'
protocol_version: '2.0'
current_phase: Phase 3b
assignee: parsley-flutter-developer
tdd_phase_source: manual
---

# Execution Log

## Task Summary

接線 production 視窗驅動與 channel 錯誤回傳

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發前 dispatch-readiness 覆核（PM，2026-08-01）

回報閾值 1 WARN（acceptance 條目 3 > 2），閾值 2、3 PASS。依規定手動覆核。

**覆核結果：不再拆。** 三條 acceptance 對應的實際功能職責為 2，第三條屬驗證類：

| acceptance | 性質 | Phase 3a 步驟 |
|-----------|------|--------------|
| 1 視窗屬性十二項契約 | 功能職責 A：production surface 與驅動 | 步驟 5 |
| 2 錯誤對應與 bridge 委派 | 功能職責 B：Flutter 層接線 | 步驟 6-7 |
| 3 實機 M-1 ~ M-8 | 驗證類，非獨立功能 | 步驟 9 |

#### 為何不沿 A/B 再拆一次

表面上 A（AppKit）與 B（Flutter）分屬不同層，看似可比照 `.2.1` / `.2.2` 的拆法再切一次。三項理由使其不划算：

1. **拆後前半無端到端驗證途徑**：production surface 與驅動若獨立成票，只能驗證視窗屬性（群組 F），無法確認它在真實播放鏈路中可用——`play` 呼叫要經 bridge 才進得來。實機清單 M-1 ~ M-8 全部需要完整鏈路，會整批落到後半，使前半的「完成」缺乏實質驗收訊號。
2. **深度已達上限**：本票 depth 3 等於 `MAX_TICKET_DEPTH`，無法再往下建子票，只能建平行票並以 `blockedBy` 串接。平行票會失去 `.2.1` / `.2.2` 那種「同一父票下的兩半」的血緣語意，追蹤成本上升而收益不明。
3. **兩者共同構成單一可交付狀態**：本 group 的目標是「特效能實際播放」。A 或 B 任一單獨完成都不構成可用功能，母票 Phase 4 也必須等兩者齊備才有評估標的。

#### 與 `.2` 拆分決策的差異

`1.4.0-W2-006.2` 當初 FAIL（6 條 > 4）而拆，是因為六條各對應不同實作面且**拆分點恰好落在依賴方向的界線上**——`.2.1` 純 AppKit 全用替身、可獨立驗證，`.2.2` 才引入真實視窗與 Flutter。本票的 A/B 之間沒有這種乾淨界線：B 依賴 A 的產物才能運作，A 缺少 B 則無法端到端驗證。同樣是「跨層」，可拆性不同。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

### 實作內容

依母票 Phase 3a §四步驟 5-7 實作：

1. **production surface 與驅動**（`CursorLocatorEffectWindow.swift` 新增）：
   - `NonKeyEffectWindow`（`NSWindow` 子類別，覆寫 `canBecomeKey` 回傳 `false`，
     使「不搶焦點」在型別層級成立）
   - `WindowCursorLocatorSurface`：依 Phase 1 §2.5 十二項屬性契約建立視窗
     （borderless / `.screenSaver` / 四項 collectionBehavior / ignoresMouseEvents
     / isOpaque=false / backgroundColor=.clear / hasShadow=false / alphaValue=1.0
     / isReleasedWhenClosed=false），以 `orderFrontRegardless()` 顯示（不搶焦點）
   - `DisplayLinkCursorLocatorFrameDriving`：`CVDisplayLink` 實作，
     `CVDisplayLinkSetOutputHandler` 回呼經 `DispatchQueue.main.async` 派回
     main thread；`retarget` 呼叫 `CVDisplayLinkSetCurrentCGDisplay`
   - `makeProductionCursorLocatorDeadlineScheduler()`：
     `DispatchQueue.main.asyncAfter` 實作，回傳取消 closure
   - `productionCursorScreenSnapshot()` / `productionCursorLocation()`：讀
     `NSScreen.screens` / `NSScreen.main` / `NSEvent.mouseLocation`
   - `NSColor(cursorLocatorArgb:)`：ARGB 整數轉 `NSColor`（tint 參數轉換）

2. **錯誤對應檔**（新檔 `CursorLocatorErrorMapping.swift`，N-1 裁定產物）：
   - `CursorLocatorErrorMapping.flutterError(for:)`：`CursorLocatorError?` ->
     `FlutterError?` 純函式，`nil` 輸入回傳 `nil`
   - `CursorLocatorErrorCode` 列舉承載 `E_CL_WINDOW` / `E_CL_SCREEN` 字面
   - 未放入 `CursorLocatorEffectWindow.swift`（維持該檔僅依賴 AppKit）與
     `MainFlutterWindow.swift`（該檔僅改一處，見下）

3. **bridge 委派**（`MainFlutterWindow.swift` 唯一改動）：
   - `CursorLocatorBridge` 新增 `controller: CursorLocatorEffectController`
     屬性，於 `init` 以四個 production 依賴建構
   - `handlePlay` 既有參數解析成功後，改為 `try controller.play(request)`；
     成功 `result(nil)`，`CursorLocatorError` 經 `CursorLocatorErrorMapping`
     轉為 `FlutterError` 回傳，非預期錯誤降級為 `E_CL_WINDOW`
   - 既有 `invalid_arguments` 分支（第 789-810 行）完全未動

### 檔案落點確認

三檔依賴方向單向：`CursorLocatorEffectWindow.swift`（AppKit）<-
`CursorLocatorErrorMapping.swift`（Flutter）<- `MainFlutterWindow.swift`
（bridge）。無循環，符合母票 Phase 3a 裁定。

### project.pbxproj

手動新增三個檔案的 `PBXBuildFile` / `PBXFileReference` / group children /
`PBXSourcesBuildPhase` 項目（`CursorLocatorErrorMapping.swift` 歸 Runner
target；`CursorLocatorErrorMappingTests.swift` /
`CursorLocatorSurfaceContractTests.swift` 歸 RunnerTests target）。已以
`xcodebuild -list -project macos/Runner.xcodeproj` 驗證專案可正確解析
（Runner / RunnerTests / Flutter Assemble 三個 target 皆列出）。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## Context Bundle

### 權威來源：母票 1.4.0-W2-006

本票只執行 TDD Phase 3b 實作。Phase 1 介面契約、Phase 2 測試設計、Phase 3a 實作策略全部位於母票 Solution 章節：

```
ticket track full 1.4.0-W2-006
```

| 需要的內容 | 母票章節 |
|-----------|---------|
| **本票的實作步驟序列（步驟 5-7）** | 「TDD Phase 3a 實作策略規劃」§四 |
| 視窗屬性十二項契約 | 「TDD Phase 1 功能設計」§2.5 |
| 測試群組 E（6 案例，錯誤對應）、F（7 案例，真實視窗屬性） | 「TDD Phase 2 測試設計」§四 |
| 實機驗證清單 M-1 ~ M-8（三種全螢幕情境分列） | 「TDD Phase 2 測試設計」§七 |
| N-1 裁定：錯誤對應抽為純函式的落點與常數落位 | 「TDD Phase 3a 實作策略規劃」§一 |
| E_CL_SCREEN 兩條路徑的語意分野 | 母票 Problem Analysis「規格落差澄清」節 |
| **重置分支不得遞增世代序號**（虛擬碼修正） | 母票 Solution「Phase 3a 虛擬碼修正」節 |

### 前置票已交付、可直接使用

| 產物 | 來源 | 位置 |
|------|------|------|
| `CursorScreenLocator.resolve` 判定純函式與三態結果型別 | `1.4.0-W2-006.1` | `macos/Runner/CursorLocatorEffectWindow.swift` |
| `CursorLocatorEffectController` 狀態機、四個注入點、`CursorLocatorSurface` 與 `CursorLocatorFrameDriving` 兩協定 | `1.4.0-W2-006.2.1` | 同上 |
| 測試替身全套（`SurfaceCallLog`、spy surface／工廠、`ManualFrameDriver`、`ManualDeadlineScheduler`） | `1.4.0-W2-006.2.1` | `macos/RunnerTests/Support/CursorLocatorTestDoubles.swift` |
| 統一測試入口 | `1.4.0-W2-033` | `./scripts/run-tests.sh`（同時跑 Dart 與 Swift） |

現有測試基線：XCTest 43 通過、`flutter test` 179 通過。本票新增後兩者皆不得下降。

### 本票的三項工作

1. **production surface 與驅動**（步驟 5）：依 Phase 1 §2.5 十二項屬性契約建立真實 `NSWindow`，顯示採不搶焦點方式；驅動以顯示器垂直同步機制回呼並一律派回 main thread，提供重綁與停止。
2. **錯誤對應檔**（步驟 6，N-1 裁定產物）：新增 `macos/Runner/CursorLocatorErrorMapping.swift`。**不得放進 `CursorLocatorEffectWindow.swift`**——該檔刻意只依賴 AppKit，混入 Flutter 型別會使測試群組 C/D 被迫連帶引入 Flutter 依賴，這是 N-1 裁定的核心理由，亦是 `.2.1` 與本票拆票的依據。
3. **bridge 委派**（步驟 7）：`MainFlutterWindow.swift` **僅改一處**——`CursorLocatorBridge` 的 `handlePlay` 在既有參數解析成功後改為委派至控制器，以錯誤對應單元決定回應。既有參數失敗分支（`invalid_arguments`）完全不動。該檔現為 836 行、已超 cognitive-load 閾值 2.8 倍，不得再增其他職責（拆分屬 `1.4.0-W2-015`）。

### 測試端的反向約束（Phase 2 已指定）

T2.E-05 / E-06 必須以**字串字面**逐字斷言錯誤碼，**不得**引用實作端的錯誤碼列舉。實作者不可為消除重複而讓測試改用同一常數——那會使常數改名時測試同步變動，錯誤碼契約失去鎖定作用。

### 測試群組執行順序（Phase 2 §四指定）

E（6，最小、驗證錯誤對應）→ F（7，真實視窗）。F 需啟動宿主 app，是唯一可能被環境阻擋的群組，放最後可避免環境問題擋住前面所有進度。

### B-7 權限彈窗：已綁 trigger 的條件延後

宿主 app 啟動時 `MainFlutterWindow` 的 CGEventTap 可能彈出輔助使用權限對話框，阻塞無人值守執行。母票 Phase 2 驗收已指定：

- trigger：本票執行 `xcodebuild test` 時**實際觀察到**權限彈窗阻塞
- 承接動作：建立追蹤票處理宿主啟動流程的測試環境隔離
- **禁止**以繞過測試或改寫宿主啟動流程的方式在本票內就地處理（後者屬 `1.4.0-W2-015` 範圍）
- 執行者須於 Test Results **明確回報是否觸發，未觸發亦須寫**

### E_CL_SCREEN 兩條路徑（acceptance 3 的判準）

| 情形 | 行為 |
|------|------|
| 游標落於螢幕排列間隙，退回 main | 僅記 NSLog，**播放照常進行**，`play` 回傳成功 |
| 無任何可用螢幕 | 以 `FlutterError` 回傳且不播放，歸入同一錯誤碼（不新增碼以免擴張 SPEC-008 定義的集合） |

判準：**復原路徑不以 FlutterError 中斷播放**。會到達 Dart 的 E_CL_SCREEN 語意唯一（無可用螢幕）。

**禁止**新增原生至 Dart 的反向 channel 或 EventChannel——「回報 Dart 側」指 `play` 這次 method channel 呼叫以 `result(FlutterError(...))` 回傳，屬同步回應。該做法違反 SPEC-008 介面規格。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending
