---
id: 1.4.0-W2-042
title: 規劃 MainFlutterWindow.swift 多票競爭的執行順序與依賴 DAG
type: ANA
status: in_progress
version: 1.4.0
wave: 2
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第一層:需求分析
  final_decision: 先做完整依賴分析並回填 blockedBy，再派發任何 Swift 側 ticket
  rationale: quality-baseline 規則 3 設計問題立即修正；7 張票無序共用單一檔案，隨意派發會反覆衝突返工，且返工成本隨已完成票數上升
who:
  current: saffron-system-analyst
  history: {}
what: 規劃 MainFlutterWindow.swift 多票競爭的執行順序與依賴 DAG
when: 派發任何 Swift 側 ticket 之前
where:
  layer: 待定義
  files:
  - docs/work-logs/v1/v1.4/v1.4.0/tickets/1.4.0-W1-021.1.md
  - docs/work-logs/v1/v1.4/v1.4.0/tickets/1.4.0-W2-015.md
  - docs/work-logs/v1/v1.4/v1.4.0/tickets/1.4.0-W2-019.md
  - docs/work-logs/v1/v1.4/v1.4.0/tickets/1.4.0-W2-026.md
  - docs/work-logs/v1/v1.4/v1.4.0/tickets/1.4.0-W2-034.md
  - docs/work-logs/v1/v1.4/v1.4.0/tickets/1.4.0-W2-035.md
  - docs/work-logs/v1/v1.4/v1.4.0/tickets/1.4.0-W3-010.md
  - docs/work-logs/v1/v1.4/v1.4.0/tickets/1.4.0-W3-011.md
  - macos/Runner/MainFlutterWindow.swift
  - macos/Runner.xcodeproj/project.pbxproj
why: PM 於 1.4.0-W1-021 拆分後盤點發現：7 張 pending ticket 的 where.files 皆含 macos/Runner/MainFlutterWindow.swift
  或 macos/Runner/，但其中僅 1.4.0-W2-035 有 blockedBy。這些票的依賴至少有三種不同性質糾纏——結構性（W2-015 要把 759
  行 5 個類別拆成多檔，其前後執行會使其餘票的落點完全不同）、慣例性（W2-026 定 FlutterError 錯誤碼命名慣例，W1-021.1 與 W3-010
  是其使用者）、基礎設施（W2-034 的可注入日誌介面可能影響 W1-021.1 的錯誤路徑寫法）。無序派發會反覆衝突與返工。本 session 已三度遇到定案者未先於使用者的排序缺口（W2-008→W2-013、W2-013→W2-011、W2-026→W1-021.1），皆為建票當下看不出、準備派發時才浮現，見
  PC-SCLK-009
how:
  task_type: Implementation
  strategy: 逐張讀 7 個 ticket md 的 why / how.strategy / acceptance 與 where.files，並讀 macos/Runner/MainFlutterWindow.swift
    現況（唯讀，759 行 5 個類別：MainFlutterWindow / FullscreenCoverageDetector / InputBindingBridge
    / CursorLocatorBridge / CursorLocatorChannel）。對每一對可能相依的票判定依賴性質與方向，判定依據須為具體的程式碼落點或產出物引用關係，非抽象推論。特別釐清
    W2-015（拆檔）相對其餘票的位置——先拆使後續票落點清晰但拆檔期間其他變更會衝突，後拆則前面所有變更需跟著搬移，兩案的實際成本須以檔案與行數量化比較。產出
    DAG 後以 ticket track set-blocked-by 回填。注意 1.4.0-W2-006 亦將在此檔加入特效視窗管理（見 W2-015 的
    why），需確認其狀態並納入考量
acceptance:
- '[ ] 7 張票的依賴性質分類（結構性 / 慣例性 / 基礎設施 / 守衛）與逐對判定依據記錄於重現實驗結果，判定依據為具體程式碼落點或產出物引用關係'
- '[ ] 產出可執行的 DAG 含關鍵路徑，每條依賴附「若順序顛倒會發生什麼」的具體後果'
- '[ ] 依 DAG 以 set-blocked-by 回填各票，回填結果與 DAG 一致且無循環'
- '[ ] 分析中識別的新問題（如兩票實為同一工作應合併、或某票前提已不成立）建 ticket 並回填 spawned_tickets'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-08-06T09:47:37'
completed_at: null
created: '2026-08-06'
updated: '2026-08-06'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

規劃 MainFlutterWindow.swift 多票競爭的執行順序與依賴 DAG

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

7 張 pending ticket 的 where.files 皆含 `macos/Runner/MainFlutterWindow.swift` 或 `macos/Runner/`，但建票當下各自獨立，幾乎無 `blockedBy` 標記。三種不同性質的依賴糾纏在同一個 888 行單檔中，無序派發會導致反覆衝突與返工。

### 影響範圍

**直接影響的 8 張票**（含 W3-011 因 pbxproj 競爭納入）：

| Ticket | 類型 | 主要動作 | 涉及的 macos/Runner/ 檔案 | 新增檔案？ |
|--------|------|---------|--------------------------|----------|
| W2-015 | 結構性拆分 | 把 888 行拆為 4 檔 | MainFlutterWindow.swift | 3 個新 .swift |
| W2-026 | 慣例統一 | 統一錯誤碼命名 + Dart 側常數集中 | MainFlutterWindow.swift, CursorLocatorErrorMapping.swift | 否 |
| W2-019 | 契約守衛 | 擴充 input_binding channel 契約字面守衛 | MainFlutterWindow.swift | 否 |
| W1-021.1 | 功能實作 | 原生輸入層錯誤 channel 回報 | MainFlutterWindow.swift | 1 個新 .swift |
| W2-034 | 基礎設施 | 原生層可注入日誌介面 | 全部 macos/Runner/*.swift | 可能 1 個 |
| W3-010 | 邊界收斂 | 錯誤碼組裝收斂至 mapping 單一來源 | MainFlutterWindow.swift, CursorLocatorErrorMapping.swift, CursorLocatorEffectWindow.swift | 否 |
| W2-035 | 守衛 | 跨語言常數一致性機械檢查 | 讀 MainFlutterWindow.swift（不修改） | 否 |
| W3-011 | 結構性拆分 | CursorLocatorEffectWindow.swift 拆分 | CursorLocatorEffectWindow.swift | 1 個新 .swift |

**已驗證事實**：
- MainFlutterWindow.swift 現為 888 行（`wc -l` 確認），cognitive-load 300 行閾值的 2.96 倍
- W2-006 已 completed（`status: completed, completed_at: 2026-08-03T10:24:02`）
- W2-006.2 已 completed，W2-026 的「範圍收斂」前提已滿足
- W3-009 已 completed，W3-011 的既有 blockedBy 已清除
- Dart 側 `lib/app_constants.dart` 無任何 `E_CL` 字面（`grep -rn E_CL lib/` 零命中，確認 W2-026 的需求仍存在）
- MainFlutterWindow.swift:837 的 `"invalid_arguments"` 與 CursorLocatorErrorMapping.swift:47-50 的 `E_CL_WINDOW`/`E_CL_SCREEN` 兩套命名慣例並存

### 相關 Error Pattern

- **PC-SCLK-009**：上游 ticket 完成使下游 Context Bundle 失效，派發前未重新校準（本 ticket 的直接成因）
- **cognitive-load 規則**：888 行 / 5 型別 / 單檔承載 3+ domain 超出所有閾值

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## Context Bundle


### PM 派發前實測（2026-08-06，main HEAD 900e702）

#### 一、W2-015 的量測基準已過期

| 項目 | W2-015 的 why 記錄 | 實測現況 |
|------|------------------|---------|
| 行數 | 759 | **888**（+129） |
| 型別數 | 5 | 5（相符） |

W2-015 的 why 寫「1.4.0-W2-006 **將**在此檔加入特效視窗管理，屆時規模會明顯上升」——W2-006 已於本輪之前完成，該預期已實現。888 行為 cognitive-load 300 行閾值的 2.96 倍。

檔內 5 個型別的實際位置（`private enum` 需含 private 前綴才 grep 得到）：

| 型別 | 行號 |
|------|------|
| `class MainFlutterWindow: NSWindow` | 6 |
| `final class FullscreenCoverageDetector` | 133 |
| `final class InputBindingBridge` | 283 |
| `private enum CursorLocatorChannel` | 736 |
| `final class CursorLocatorBridge` | 764 |

#### 二、project.pbxproj 是所有新增 Swift 檔的共同競爭點（本 ticket 的關鍵約束）

1.4.0-W1-028 實證：此專案非 filesystem-synchronized group，**新增 Swift 檔需手動補 `project.pbxproj` 的 PBXBuildFile / FileReference / Group / SourcesBuildPhase 四類條目**，漏補的症狀會偽裝成「測試沒跑到」而非「檔案沒編譯」。

因此凡會新增 `macos/Runner/` 下檔案的票，都會寫入 `project.pbxproj`：

| ticket | 新增檔案 |
|--------|---------|
| 1.4.0-W1-021.1 | `InputBindingErrorMapping.swift`（1 個） |
| 1.4.0-W2-015 | 拆分產物（多個） |
| 1.4.0-W3-011 | 拆分產物（多個） |

`project.pbxproj` 是 Xcode 專案檔，格式為巢狀屬性清單且含大量 UUID，**merge 衝突極難人工解析**。這使「同時派發兩張會新增 Swift 檔的票」的風險遠高於一般同檔衝突。此約束須納入 DAG——即使兩票在 `.swift` 層面正交，只要都新增檔案就不宜並行。

#### 三、1.4.0-W3-011 已納入分析範圍（where.files 已補）

`macos/Runner/CursorLocatorEffectWindow.swift` 為 **1005 行**（比 MainFlutterWindow.swift 更大），已有 1.4.0-W3-011「拆分 CursorLocatorEffectWindow.swift」pending 追蹤，不需另建票。

該票不動 MainFlutterWindow.swift，原不在本 ticket 初始範圍，但因上述 pbxproj 競爭而納入——兩個拆分票若並行會在同一個 pbxproj 產生大量 UUID 條目衝突。

`macos/Runner/` 現況（總計 2297 行）：

| 檔案 | 行數 |
|------|------|
| `CursorLocatorEffectWindow.swift` | 1005 |
| `MainFlutterWindow.swift` | 888 |
| `CursorLocatorRippleRenderer.swift` | 114 |
| `CursorLocatorSpotlightRenderer.swift` | 113 |
| `CursorLocatorBorderFlashRenderer.swift` | 79 |
| `CursorLocatorErrorMapping.swift` | 51 |
| `ButtonEventConsumption.swift` | 34 |

兩個超標檔佔全目錄 82%。後五個小檔皆為既有拆分產物，可作為拆分粒度的參考基準。

#### 四、已知的一條依賴（PM 已回填，供驗證）

`1.4.0-W1-021.1` 已標 `blockedBy: 1.4.0-W2-026`。理由：.1 要**新增**錯誤碼，而 W2-026 要**統一**錯誤碼命名慣例（現況兩套並存——`invalid_arguments` 小寫蛇底線 vs SPEC-008 定義的 `E_CL_SCREEN` 大寫前綴，且三個錯誤碼字面未納入常數集中）。若 .1 先做則無慣例可依，很可能加出第三套。

此判定請於分析中驗證，若有更適當的順序請更正並記錄理由。

#### 五、本 ticket 的成因脈絡

PM 於 1.4.0-W1-021 拆分後盤點才發現這 7 張票共用單檔且幾乎無排序。本 session 已三度遇到同型缺口——定案者未先於使用者（W2-008→W2-013、W2-013→W2-011、W2-026→W1-021.1），三次都是建票當下看不出、準備派發時才浮現。已記錄為 `PC-SCLK-009`（上游 ticket 完成使下游 Context Bundle 失效，派發前未重新校準）。

本 ticket 的目的是把這類「只在派發前才浮現的依賴」一次盤清並固化進 `blockedBy`，而非繼續逐條猜測補上。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending
