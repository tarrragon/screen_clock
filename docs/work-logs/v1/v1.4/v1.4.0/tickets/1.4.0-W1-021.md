---
id: 1.4.0-W1-021
title: 補上 原生輸入層錯誤路徑的使用者可見提示
type: IMP
status: pending
version: 1.4.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy:
- 1.4.0-W1-020
relatedTo:
- 1.4.0-W1-020
spawned_tickets: []
source_ticket: 1.4.0-W1-005
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:錯誤處理
  final_decision: 補使用者可見提示，不僅補日誌
  rationale: observability-rules 規則 5 要求平台互動程式碼自帶日誌，但本案缺的是使用者層回饋；ux-design-evaluation
    的錯誤恢復要求錯誤訊息可被使用者理解並有下一步
who:
  current: parsley-flutter-developer
  history: {}
what: 補上 原生輸入層錯誤路徑的使用者可見提示
when: 1.4.0-W1-005 補建 UC-05 時發現
where:
  layer: Presentation
  files:
  - macos/Runner/MainFlutterWindow.swift
  - macos/RunnerTests/InputBindingErrorMappingTests.swift
  - lib/input/input_binding_channel.dart
  - lib/input/input_binding_controller.dart
  - lib/widgets/settings_panel.dart
  - lib/app_constants.dart
  - test/input/input_binding_channel_test.dart
  - test/widgets/settings_panel_binding_test.dart
  - docs/usecases/UC-05-bind-mouse-button-hotkey.md
why: 1.4.0-W1-005 讀原生實作時發現：CGEventTap 建立失敗與 hotkey keyCode 轉 CGKeyCode 查無對應時僅產生 NSLog，無使用者可見提示也無明確重試路徑，使用者只會看到綁定無作用而無從得知原因
how:
  task_type: Implementation
  strategy: 定位 macos/Runner/MainFlutterWindow.swift 中僅 NSLog 的錯誤路徑（CGEventTap 建立失敗、keyCode
    查無對應）；經 method channel 將失敗狀態回傳 Dart 層；於設定面板綁定清單顯示失敗狀態與原因（如輔助使用權限未授權、按鍵不支援）；文案集中於
    lib/app_constants.dart 的 AppText，禁止硬編碼字串；參考既有權限引導 UI 的呈現方式保持一致；同步更新 UC-05 的例外場景（原標註為未定義）與
    SPEC-007 對應 FR
acceptance:
- '[ ] CGEventTap 建立失敗時設定面板顯示可理解的失敗原因'
- '[ ] hotkey keyCode 查無對應時該綁定於清單標示不支援'
- '[ ] 所有新增文案定義於 AppText，無硬編碼字串'
- '[ ] UC-05 例外場景由未定義更新為實際行為'
- '[ ] 既有成功路徑行為不變，測試全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-07-29'
updated: '2026-07-29'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

補上 原生輸入層錯誤路徑的使用者可見提示

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.4.0-W1-005 | chars: 131 -->

### Task Reference
- 1.4.0-W1-005 what: 補建 UC-04 與 UC-05 用例文件

### Related Files
- docs/usecases/UC-04-bind-drag-to-scroll.md  # from 1.4.0-W1-005
- docs/usecases/UC-05-bind-mouse-button-hotkey.md  # from 1.4.0-W1-005
- docs/app-use-cases.md  # from 1.4.0-W1-005

### UC Context
#### UC-05: 綁定按鍵快捷鍵
Spec 位置: docs/app-use-cases.md:129
主要流程:
  1. **按下綁定鍵**
  2. **合成組合鍵事件**
  3. **前景 app 接收**
  4. **放開綁定鍵**

### PM 派發前補料（1.4.0-W1-021）

#### where.files 已補齊真實寫入集

原 where.files 只列 4 檔，但 acceptance 條 4 要改 UC-05、條 5 要求測試全綠，兩者的寫入目標都未列入。補齊後為 9 檔（含 2 個既有測試檔、1 個建議新增的 Swift 測試檔、1 個 UC 文件）。`dispatch-readiness` 檢查 4 由 WARN 轉 PASS，閾值 2 由 PASS 轉 WARN——真實複雜度在補齊後才浮現，見下方「拆分訊號」。

#### Swift 側錯誤測試已有可複用先例（避開權限彈窗風險）

CLAUDE.md 記載的已知風險是：`macos/RunnerTests/` 目前僅含純函式判定測試，若新增啟動 `MainFlutterWindow`（CGEventTap）的 app-hosted 測試，無人值守執行時可能彈出輔助使用權限對話框而阻塞。

本 ticket 不需承擔該風險，專案已有現成的規避模式：

| 角色 | 檔案 |
|------|------|
| 錯誤映射純函式 | `macos/Runner/CursorLocatorErrorMapping.swift`（`enum CursorLocatorErrorMapping`，13 行起） |
| 對應純函式測試 | `macos/RunnerTests/CursorLocatorErrorMappingTests.swift`（Spy / Manual fixture 注入，不啟動宿主 app） |
| 呼叫點 | `macos/Runner/MainFlutterWindow.swift:841`，`result(CursorLocatorErrorMapping.flutterError(for: error))` |

該測試檔頭另有一條約束值得沿用：測試 class 不持有 `static var` 可變狀態，fixture 一律於 `setUp` 重建。

where.files 中的 `macos/RunnerTests/InputBindingErrorMappingTests.swift` 是依此先例的建議落點，實際命名與是否抽出獨立 enum 屬 Phase 3a 實作策略，執行者可依判斷調整並在 Solution 說明。

#### 錯誤狀態回報通道已有可複用先例

不需另建一套原生到 UI 的狀態通道。既有的輔助使用授權狀態走的正是本 ticket 需要的路徑：

- `lib/input/input_binding_controller.dart:28` — `ValueNotifier<bool> _permissionGranted`，內部可寫
- 同檔 31 行 — 對外僅暴露唯讀 `ValueListenable<bool> get permissionGranted`
- `lib/widgets/settings_panel.dart:18-19` — 面板監聽該 notifier 顯示授權狀態與引導
- 同檔 57-58 行 — 面板開啟時主動 `refreshPermission()` 刷新（SPEC-007 FR-07）
- 授權狀態變化由原生經 channel 回報，控制器再補下傳綁定

錯誤狀態（CGEventTap 建立失敗、keyCode 查無對應）建議沿同一形狀：原生經 channel 回報 → controller 以唯讀 notifier 暴露 → 面板監聽呈現。`lib/input/input_binding_channel.dart:112 / 127 / 139` 是既有的 channel 訊息處理與參數異常分支，新訊息類型應與之並列。

#### UC-05 寫入邊界（與 1.4.0-W1-020 分工）

`docs/usecases/UC-05-bind-mouse-button-hotkey.md` 兩個區塊分屬不同 ticket：

| 區塊 | 行號 | 負責 ticket |
|------|------|------------|
| 例外場景中兩處「無；查無對應 UI 呈現機制，標註為未定義」 | 83、93 | 本 ticket，acceptance 條 4 |
| 已知限制 | 121 起 | 1.4.0-W1-020 |

本 ticket 只更新 83 / 93 行的使用者提示欄位，不動已知限制章節。

#### 前置相依：1.4.0-W1-020 必須先完成

已標入 `blockedBy`。W1-020 要判定 SPEC-007 FR-03 描述的事件消費與 `handleButtonUp` 實際行為何者為正確意圖，判定結果會 spawn 修正 ticket。本 ticket 要把「實際行為」寫進 UC-05 例外場景——若該行為正是 W1-020 判定為缺陷、應被修掉的行為，先寫進規格等於固化 bug。派發本 ticket 前先讀 W1-020 的判定結論。

#### 拆分訊號（派發前需決定）

`dispatch-readiness` 兩項未過：

- 閾值 1 FAIL：acceptance 5 條 > 4。PM 覆核認為屬條目數高估——條 3（文案集中 AppText）是規範約束、條 5（測試全綠）是回歸條件、條 4 是文件同步，真正的功能職責只有條 1 與條 2 且共用同一機制
- 閾值 2 WARN：where.files 9 檔 > 5，且跨 Swift 原生層、Dart channel 層、Dart UI 層、文件層四個層次

兩項疊加後拆分訊號明確。建議依 domain 邊界拆為原生側（錯誤映射與 channel 回傳）與 Dart 側（狀態接收與面板呈現），UC-05 更新併入其一。實際拆法待 PM 於派發前決定。

### 前置更新：1.4.0-W1-020 判定已出爐（本節取代上方「前置相依」的待確認狀態）

W1-020 已完成，判定結論：

- **SPEC-007 FR-03 為正確意圖，實作有缺口**。FR-03 要求綁定鍵的 otherMouseDown 與 otherMouseUp 皆回傳 nil 消費，與 PROP-002:157「一律吃掉為使用者明確選擇」一致
- **落差**：`handleButtonUp:670` 僅檢查 `draggingAction != nil`，Hotkey 綁定無拖曳狀態故 mouseUp 於 674 行放行穿透
- **使用者後果**：當前主流 macOS app 的側鍵 back/forward 由 mouseDown 驅動，orphan mouseUp 被 NSResponder 預設機制安靜丟棄，無可觀察誤觸發。此為低風險但仍屬實作缺陷——判定不建立在風險評估上，而在 SPEC 的防禦性意圖
- **證據強度**：路徑 A/B 為程式碼路徑追蹤（硬證據，含行號與回傳值）；使用者後果為 macOS 事件模型分析，W1-020 已明確標註未含實機操作。機械驗證由 W1-028 的 acceptance 條 3 承擔
- **UC-05 已知限制章節**已由 W1-020 更新並標註待修正 ticket

對本 ticket 的影響：UC-05 例外場景（83 / 93 行）要寫的是 CGEventTap 建立失敗與 keyCode 查無對應兩條錯誤路徑的使用者提示，與 mouseUp 消費落差無關，故 W1-020 的判定不改變本 ticket 的內容。前置阻擋已解除。

#### 新增同檔相依：1.4.0-W1-028

W1-020 spawn 出 1.4.0-W1-028（IMP，P2，修 `handleButtonUp` 消費 Hotkey mouseUp），其 where.files 為 `macos/Runner/MainFlutterWindow.swift`，與本 ticket 同檔。

| 面向 | 判斷 |
|------|------|
| 邏輯相依 | 無。W1-028 動 handleButtonUp 的消費條件；本 ticket 動 CGEventTap 建立失敗處與 keyCode 轉換處，關注點不重疊 |
| 檔案衝突 | 有。同檔不同區域，W1-028 另需新增狀態追蹤變數（形似 `draggingAction`，宣告於 295 行附近），與本 ticket 可能觸及的 tap 建立區（383 行附近）距離近 |

兩者不宜並行。W1-028 範圍小（單檔單函式）且屬 SPEC 對齊的缺陷修復，建議先於本 ticket 執行。
