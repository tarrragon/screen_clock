---
id: 1.4.0-W3-026
title: 複查 W3-018 幾何同步測試是否為空斷言
type: ANA
status: in_progress
version: 1.4.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 1.4.0-W3-027
source_ticket: 1.4.0-W3-021
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: W3-021 執行時發現的測試有效性疑慮，建 ANA 票以 RED 對照實測確認
  rationale: quality-baseline-rule-5
who:
  current: parsley-flutter-developer
  history: {}
what: 複查 W3-018 幾何同步測試是否為空斷言
when: 任意；但在其他票倚賴該測試作為回歸保護之前
where:
  layer: 待定義
  files:
  - macos/RunnerTests/CursorLocatorSurfaceContractTests.swift
why: 1.4.0-W3-021 撰寫 animationKeys 斷言時實測發現：以裸 CALayer 作宿主時，即使移除 composite 的 CATransaction
  disableActions，animationKeys() 仍回傳空——隱含動畫只在圖層樹已 commit 且連上實際 render context 時才生成。該票初版測試因此是空斷言（無論修復存在與否都會綠），改用
  NSWindow 加 layer-hosted NSView 並於幀間 CATransaction.flush() 後，移除 transaction 的 RED
  對照才確實兩條皆 failed。1.4.0-W3-018 的 testGeometrySyncDoesNotStartImplicitAnimation 使用同一手法斷言
  animationKeys 為空，若其宿主環境不足以生成隱含動畫，該測試同為空斷言——那將使 W3-018 的隱含動畫防護實際上沒有回歸保護。該測試用的是真實
  WindowCursorLocatorSurface，可能無此問題，但 W3-021 執行時未實測確認
how:
  task_type: Analysis
  strategy: 以 RED 對照法實測：暫時移除 LayerHostingView.synchronizeHostedLayer 內的 CATransaction
    disableActions，執行 testGeometrySyncDoesNotStartImplicitAnimation，確認它是否轉紅。轉紅則該測試有分辨力，本票結論為無問題並記錄實證；未轉紅則該測試為空斷言，需依
    1.4.0-W3-021 的手法補強宿主環境（NSWindow + layer-hosted NSView + 幀間 flush）並建 IMP 票執行。順帶掃描
    macos/RunnerTests/ 是否有其他以 animationKeys 或類似 CoreAnimation 副作用作斷言的測試，一併確認宿主環境是否足夠
acceptance:
- '[ ] 以 RED 對照實測 testGeometrySyncDoesNotStartImplicitAnimation 是否具分辨力，結論含實測輸出'
- '[ ] 若為空斷言則建立對應 IMP 票補強，並於本票 spawned_tickets 回填'
- '[ ] 已掃描 RunnerTests 內其他倚賴 CoreAnimation 副作用的斷言，逐一記錄宿主環境是否足夠'
tdd_phase: phase3b
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-08-04T10:48:39'
completed_at: null
created: '2026-08-03'
updated: '2026-08-03'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

複查 W3-018 幾何同步測試是否為空斷言

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果
### 實驗方法

RED 對照法（沿用 1.4.0-W3-021 已驗證有效的實測手法）：暫時移除
`LayerHostingView.synchronizeHostedLayer`（macos/Runner/CursorLocatorEffectWindow.swift）
內的 `CATransaction.setDisableActions(true)`，執行
`CursorLocatorSurfaceContractTests.testGeometrySyncDoesNotStartImplicitAnimation`，
觀察該測試是否轉紅。若轉紅代表測試具分辨力（無問題）；若仍為綠燈代表測試對隱含動畫
無感知，屬空斷言。

指令：
```
xcodebuild test -workspace macos/Runner.xcworkspace -scheme Runner \
  -configuration Debug -destination 'platform=macOS' \
  -only-testing:RunnerTests/CursorLocatorSurfaceContractTests/testGeometrySyncDoesNotStartImplicitAnimation
```

### 實驗執行

第一輪（移除 disableActions，不加額外 flush）：
- 修改 `synchronizeHostedLayer` 移除 `CATransaction.setDisableActions(true)`
- 執行測試 -> **TEST SUCCEEDED**（`testGeometrySyncDoesNotStartImplicitAnimation() passed (0.010 seconds)`）
- 結果：未轉紅

第二輪（同上，額外在測試內 `surface.move(toScreenFrame: moved)` 後加
`CATransaction.flush()`，比照 1.4.0-W3-021 composition 測試在寫入後 flush 的手法，
排除「未 commit 到 render context」這個混淆變因）：
- 執行測試 -> **TEST SUCCEEDED**（`testGeometrySyncDoesNotStartImplicitAnimation() passed (0.031 seconds)`）
- 結果：仍未轉紅

兩輪實測後已還原全部暫時修改（`git diff --stat macos/` 確認為空），並執行
`scripts/run-tests.sh` 全量驗證，flutter test 與 xcodebuild test 皆通過（含
CursorLocatorSurfaceContractTests 全數 GREEN）。

附帶掃描：`grep -rln "animationKeys" macos/RunnerTests/*.swift` 僅命中
`CursorLocatorEffectCompositionTests.swift`（用 1.4.0-W3-021 已驗證有效的
`makeHostedLayer` 手法：獨立 layer-hosted `NSView` + 幀間 `CATransaction.flush()`）
與 `CursorLocatorSurfaceContractTests.swift`（本票標的）。宿主環境已逐一確認，無其他
倚賴 CoreAnimation 副作用斷言的測試需要複查。

### 實驗發現

**已驗證的事實**：`testGeometrySyncDoesNotStartImplicitAnimation` 為空斷言——移除
受測程式碼的隱含動畫防護（`CATransaction.setDisableActions(true)`）後，該測試仍維持
GREEN，無論是否額外補 `CATransaction.flush()` 皆同。即使該測試使用真實
`WindowCursorLocatorSurface`（含 `NonKeyEffectWindow` 並於 `init` 呼叫
`orderFrontRegardless()`，比 1.4.0-W3-021 最初失敗版本的裸宿主更「真實」），仍無法
使 `animationKeys()` 觀察到隱含動畫被啟動。

**仍未驗證的假設**：確切原因未深究（可能是 XCTest 執行環境下 window server
連線狀態、或需要额外的 run loop 迭代讓 CVDisplayLink 真正連上 render context，
與 1.4.0-W3-021 composition 測試用的獨立 `NSView`（無 `orderFront`，僅單次
`CATransaction.flush()` 建立初始 commit）在連線時機上有差異）。本票範疇僅要求確認
是否具分辨力，不要求根因定位；根因與具體修法留給衍生的 IMP 票（見 Solution）。

**結論**：1.4.0-W3-018 的隱含動畫防護目前**沒有回歸保護**——若未來有人移除或破壞該
`CATransaction` 防護，此測試不會發現。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.4.0-W3-021 | chars: 898 -->

### Task Reference
- 1.4.0-W3-021 what: 收斂 隱含動畫停用至 composite 層並修復邊框閃爍失效

### Rationale Chain
- 1.4.0-W3-021 why: 1.4.0-W3-001 Phase 4 審查發現 P0 功能性缺陷：CursorLocatorBorderFlashRenderer 全檔無 CATransaction 或 withoutImplicitAnimations（實測 grep 命中 0 次，Spotlight 7 次、Ripple 3 次）。其 render 每幀直接寫 borderLayer.opacity，該屬性為 animatable；宿主是 layer-hosted layer，sublayer 的 delegate 為 nil，AppKit 不回傳停用 action，故每幀啟動預設 0.25 秒隱含動畫。而 flashCycleDuration 為 200 ms 短於動畫時長，後幀持續覆蓋前幀，實際輸出被低通濾波抹平——SPEC-008 FR-04「閃爍三次」與該票 acceptance 在實機上不成立，但三張子票測試全綠（無測試能觀察隱含動畫）。同一機制的完整說明已寫於 CursorLocatorEffectWindow.swift:721-724（1.4.0-W3-018 留下），但 BorderFlash 作者在獨立 worktree 中無需讀到該處。附帶問題：detach 契約語意不一致，Spotlight 包在 withoutImplicitAnimations 內、BorderFlash 與 Ripple 裸呼叫 removeFromSuperlayer，Ripple 的「無殘留圖形由圖層移除保證」前提是移除即時，至少一方判斷有誤且無測試可分辨

### Related Files
- macos/Runner/CursorLocatorEffectWindow.swift  # from 1.4.0-W3-021
- macos/Runner/CursorLocatorBorderFlashRenderer.swift  # from 1.4.0-W3-021
- macos/Runner/CursorLocatorSpotlightRenderer.swift  # from 1.4.0-W3-021
- macos/Runner/CursorLocatorRippleRenderer.swift  # from 1.4.0-W3-021
