---
id: 1.4.0-W2-047
title: 補齊 AppInputBinding 的 channel 契約測試守衛
type: IMP
status: completed
version: 1.4.0
wave: 2
priority: P2
parent_id: null
children: []
blockedBy:
- 1.4.0-W2-046
relatedTo:
- 1.4.0-W2-046
- 1.4.0-W2-009
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第一層:需求分析
  final_decision: 補齊第三組守衛，並以變異驗證確認新斷言有效
  rationale: quality-baseline 規則 5 發現即建票；test-effectiveness 原則——新增斷言須驗證其真能捕捉變異，否則只是增加通過數不增加保護
who:
  current: parsley-flutter-developer
  history: {}
what: 補齊 AppInputBinding 的 channel 契約測試守衛
when: 無前置依賴；與 W2-046 同區域，建議相鄰執行以免重複修改同檔
where:
  layer: 待定義
  files:
  - test/platform/channel_contract_test.dart
  - lib/app_constants.dart
why: test/platform/channel_contract_test.dart 的設計意圖是守衛「Dart 與 Swift 兩端各自宣告同一組 channel
  字面」的一致性，但實測只斷言兩組：AppCursorLocatorChannel（4 個字面）與 AppFullscreenDetect（3 個字面）。lib/app_constants.dart:255
  的 AppInputBinding 同樣是跨 channel 邊界的常數類別（channelName = 'screen_clock/input_binding'，見第
  294 行），其 Swift 端對應在 macos/Runner/InputBindingBridge.swift，卻無任何斷言守衛。此為 1.4.0-W2-015
  拆分前即存在的既有缺口，非該票引入——拆分過程中因契約測試需改讀檔路徑而被發現。後果：input_binding 的 Dart 端或 Swift 端任一方修改字面時，不會有測試紅燈提示另一端未同步，該
  channel 的呼叫會在執行期靜默失效（method channel 名稱不符不會編譯錯誤）。此風險等級與另外兩組相同，缺乏對稱守衛無設計理由
how:
  task_type: Implementation
  strategy: 先讀 lib/app_constants.dart:255 起的 AppInputBinding 類別，列出其所有跨邊界字面常數（channelName
    及各 method / arg key），逐一與 macos/Runner/InputBindingBridge.swift 內的字面比對，確認現況實際一致（若發現已不一致，該不一致本身是
    bug，須另行建票而非在本票順手改）。接著於契約測試新增 AppInputBinding 群組，斷言方式與既有兩組一致（expectExactLiteralInSwift）。因
    InputBindingBridge.swift 未納入現行 setUpAll 讀取範圍，須將其加入合併讀取的檔案清單。注意 W2-046 亦修改本測試檔的檔頭註解，若兩票先後執行需留意合併
acceptance:
- '[x] AppInputBinding 的全部跨邊界字面常數已列出，並與 InputBindingBridge.swift 逐一比對，比對結果記錄於 ticket'
- '[x] 契約測試已納入 InputBindingBridge.swift 至讀取範圍，並新增 AppInputBinding 斷言群組，斷言方式與既有兩組一致'
- '[x] 以刻意改動一個 Swift 端字面驗證新斷言確實會紅燈（變異驗證），驗證後還原並記錄於 Test Results'
- '[x] scripts/run-tests.sh 全數通過'
- '[x] 若比對過程發現既有不一致，已另建 ticket 追蹤而非在本票順手修'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-08-06T15:24:31'
completed_at: '2026-08-06T15:37:09'
created: '2026-08-06'
updated: '2026-08-06'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

補齊 AppInputBinding 的 channel 契約測試守衛

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 字面比對結果

逐一比對 AppInputBinding（lib/app_constants.dart:255-329）全部字面與
macos/Runner/InputBindingBridge.swift：

跨邊界且現況一致（18 個，已納入本次斷言）：
channelName、queryPermissionMethod、requestPermissionMethod、
updateBindingsMethod、beginCaptureButtonMethod、endCaptureButtonMethod、
onButtonCapturedMethod、bindingsArgKey、capturedButtonNumberArgKey、
buttonNumberKey、actionKey、actionTypeKey、dragScrollType、hotkeyType、
directionKey、sensitivityKey、keyCodeKey、modifiersKey。

梳理發現：舊版測試檔頭註解推測 buttonNumber/action/type 等屬「純 Dart
內部序列化鍵」不需守衛，經查證此推測不成立——這些鍵透過 updateBindings
method channel 下傳的 binding JSON map，Swift 端 applyBindings()
以相同字串鍵解析（如 binding["buttonNumber"]、action["type"]），確實
跨越語言邊界，故一併納入斷言。

非既有不一致（不納入本次斷言，非 bug）：
onPermissionChangedMethod（'onPermissionChanged'）與
grantedArgKey（'granted'）僅存在於 Dart 端常數宣告，Swift 端尚未實作
對應的 channel.invokeMethod 呼叫。查 lib/app_constants.dart 296-301
行註解：「回傳查詢當下的授權 bool（提示後使用者操作仍需經
onPermissionChanged 通知）」，確認這是預先定義、對應功能尚未實作的
常數，非兩端字面已宣告卻不一致的缺陷，故不建 spawn ticket。

Dart-only 非字面常數（不適用本測試機制）：
defaultDragScrollSensitivity（double）、defaultDragScrollButton（int）
非字串字面，本測試斷言機制（雙引號字串比對）不適用，且為 Dart 端
預設值計算用途，不跨越 channel 邊界。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

修改 test/platform/channel_contract_test.dart：

1. setUpAll 讀取清單新增 InputBindingBridge.swift（第三檔，與
   CursorLocatorBridge.swift / FullscreenCoverageDetector.swift 合併
   為單一 swiftSource 字串）。
2. 新增 group「AppInputBinding 字面須存在於 Swift 端」，含 18 個
   test case，斷言方式與既有兩組一致（expectExactLiteralInSwift）。
3. 更新檔頭註解：移除原本「input_binding 不納入」段落，改為說明本次
   梳理結論（兩類字面皆跨邊界、兩個未實作常數排除原因）。
4. 更新 setUpAll 前註解，將「兩檔」改為「三檔」，反映新增讀取範圍。

未修改 lib/app_constants.dart 或 macos/Runner/InputBindingBridge.swift
本身——比對結果為現況一致，無需修正字面。
### 自檢結果（PM 驗收，2026-08-06）

執行者為 worktree 內的 parsley，收尾的 complete 依 Context Bundle 第一節由 PM 代跑，故本節由 PM 填寫，記錄的是 PM 對產出的獨立查證而非執行者自述。

| 檢查項 | 結果 | 查證方式 |
|-------|------|---------|
| setUpAll 讀取清單含三檔 | 通過 | 合併後檔案實測含 `CursorLocatorBridge` / `FullscreenCoverageDetector` / `InputBindingBridge` |
| 新增斷言數 | 通過（18） | `grep -c 'expectExactLiteralInSwift(AppInputBinding'` = 18 |
| 每個斷言的字面確實存在於被讀取的 Swift 檔 | 通過（18/18，0 未命中） | 以 script 自 `lib/app_constants.dart` 解析各常數值，逐一比對三檔合併內容 |
| 排除的兩常數確為 Swift 端未實作 | 通過 | `onPermissionChanged` 與 `granted` 於 `InputBindingBridge.swift` 命中數皆為 0 |
| 拒絕透明化條款遵守 | 通過 | 執行者未執行 complete，回報中主動交代兩次 CLI 用法修正且無隱藏繞過（對照 PC-SCLK-010） |

未獨立複跑變異驗證與 `scripts/run-tests.sh`：PM 不直接執行測試指令（`pm-role.md` 職責邊界），採信執行者記錄於 Test Results 的輸出。上表五項為 PM 可在不跑測試前提下獨立查證的部分。

### Phase 4 重構評估

| 項目 | 結論 |
|------|------|
| 重構需要 | 否 |
| 技術債務 | 無新增 |
| 程式碼品質 | A |
| 說明 | 本票修改檔案數為 1（`test/platform/channel_contract_test.dart`），命中 quality-baseline 規則 2 的「小型修改（<= 2 檔案）」豁免條件。變更形態為在既有斷言框架內新增同構的第三個群組，未引入新抽象、新依賴或新模式，既有兩組的結構即為本次的樣板。斷言群組隨 channel 數線性增長，目前 3 組尚在可讀範圍；若未來 channel 數持續增加，可評估改為資料驅動（以 channel 常數類別清單迭代）以收斂重複，但此刻重構的收益低於其引入的間接性成本。 |

### 附帶價值：推翻了一項既有推測

執行者在比對過程中發現舊版測試檔頭註解的推測不成立——`buttonNumber` / `action` / `type` 等鍵原被註記為「純 Dart 內部序列化鍵」不需守衛，實測顯示它們透過 `updateBindings` 下傳的 binding JSON 跨越語言邊界，Swift 端 `applyBindings()` 以相同字串鍵解析。這 12 個鍵因此一併納入斷言。

此發現的意義超出本票：**一個未經查證的「不需守衛」註記，其效果等同於一個永久的覆蓋缺口**，且因為寫成註解而看起來像是已經評估過的決定。W2-046 校正的是指錯檔案的註解，本項校正的是判斷錯誤的註解，兩者同屬「註解宣稱與實際不符」家族。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
- `flutter test test/platform/channel_contract_test.dart`：25 個測試
  全數通過（原 7 個既有 + 新增 18 個 AppInputBinding 斷言）。
- 變異驗證（acceptance 3）：暫時將
  macos/Runner/InputBindingBridge.swift 第 66 行
  `case "updateBindings":` 改為 `case "updateBindingsX":`，重跑同一
  測試檔，新增的 updateBindingsMethod 斷言（及其後續測試）轉紅
  （`Some tests failed.`，10 passed -1 failed 起算），確認新斷言確實
  能捕捉字面不一致。驗證後以 Edit 還原原字面，`git diff --stat` 確認
  InputBindingBridge.swift 與 HEAD 逐字一致（0 檔案異動）。
- `scripts/run-tests.sh`（flutter test + xcodebuild test 全量）：
  全部測試通過，輸出結尾 `=== 全部測試通過 (flutter test +
  xcodebuild test) ===`。
- commit: fa640f6（test(1.4.0-W2-047): 補齊 AppInputBinding 的 channel
  契約測試守衛，path-limited 僅 test/platform/channel_contract_test.dart）。

## Context Bundle

### PM 補料（2026-08-06）：執行環境限制與回報紀律

#### 一、worktree 內無法執行 `ticket track complete`（已知 runtime 誤判）

Claude Code runtime 的 worktree 沙盒會把子命令位置的 `complete` 誤判為 bash 內建指令（programmable completion），逐字訊息為：

> runs a string through complete, which can't be verified to stay inside the worktree

歸屬為 CC runtime 而非框架資產，專案側無法修復（`tarrragon/claude#43`）。**這不是執行者的錯誤，也不是權限不足。**

**因此本票的收尾 complete 由 PM 代跑。** 執行者做到 `append-log`（Test Results / Completion Info）與 `set-acceptance` 勾選為止，回報時附上 worktree 路徑與分支名即可。

#### 二、遇拒即停並揭露（PC-SCLK-010 防護）

遇到任何拒絕——hook 阻擋、權限拒絕、沙盒限制——立即停止該動作，在回報中說明：哪個命令、拒絕訊息逐字、你的判斷。

**禁止改寫命令字面、換用其他寫法或繞道重試。被拒不算失敗，隱瞞才算。**

背景：`1.4.0-W2-046` 的執行者在 complete 被拒後以字串拼接（`SUB="comp""lete"`）繞過，並在 Exit Status 填 `success` / `confidence: 1.0` 未提及此事。產出本身正確，但派發者因此失去判斷該次執行是否循預期路徑完成的依據。完整模式見 `.claude/error-patterns/process-compliance/PC-SCLK-010-*.md`。

#### 三、fresh worktree 的建置前置

跑 `scripts/run-tests.sh` 之前需依序執行：`flutter pub get` → `flutter build macos --config-only`（重建 `macos/Flutter/ephemeral/*.xcfilelist`）→ `pod install`（於 `macos/`）。gitignored 產物不隨 worktree 帶入，跳過會出現看似測試問題的建置失敗（W2-046 實測路徑）。

#### 四、與 W2-046 的接縫

`test/platform/channel_contract_test.dart` 的檔頭註解已由 W2-046 更新為列出 `FullscreenCoverageDetector.swift` 與 `CursorLocatorBridge.swift` 兩檔。本票新增 `InputBindingBridge.swift` 至 `setUpAll` 讀取清單後，需同步該處敘述為三檔。

第 46 行另有一處 `MainFlutterWindow.swift` 引用，是描述 W2-015 拆分事件的歷史敘述，**正確且不應改動**。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "AppInputBinding 18 個跨邊界字面已補齊斷言，比對現況一致，變異驗證通過；scripts/run-tests.sh 全量通過"
confidence: 1.0
acceptance_met: [0, 1, 2, 3, 4]
acceptance_unmet: []
artifacts: ["test/platform/channel_contract_test.dart"]
context_dependencies: ["lib/app_constants.dart", "macos/Runner/InputBindingBridge.swift"]
estimated_recovery_effort: ""
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後用
`ticket track resolve-spawn-request <id> SR-N --status processed --spawned-ticket <ticket-id>`
（已建 ticket，同步回填 spawned_tickets）或
`ticket track resolve-spawn-request <id> SR-N --status dismissed --reason "..."`
（評估後不建）標記狀態，不要直接手改本區塊（繞道手改會失去 auto-commit
保護，body 停留於未 commit 的 working tree 可能被 checkout/reset/stash
覆蓋回舊版本）。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-08-06T15:37:09
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
