---
id: 1.4.0-W2-006.1
title: 實作 特效層目標螢幕判定純函式與 XCTest 基礎設施
type: IMP
status: in_progress
version: 1.4.0
wave: 2
priority: P0
parent_id: 1.4.0-W2-006
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: parsley-flutter-developer
  history: {}
what: 實作 特效層目標螢幕判定純函式與 XCTest 基礎設施
when: 1.4.0-W2-006 母票拆分後即可執行，無前置依賴
where:
  layer: Infrastructure
  files:
  - macos/Runner/CursorLocatorEffectWindow.swift
  - macos/RunnerTests/
  - macos/Runner.xcodeproj/project.pbxproj
why: SPEC-008 FR-02 目標螢幕判定與跨螢幕跟隨；UC-06 主成功步驟 2/3/5、替代場景 06a 跨螢幕跟隨、06b 播放期間重複觸發、06d
  覆蓋全螢幕應用、例外 EX-06-02 螢幕解析失敗、EX-06-03 視窗建立失敗
how:
  task_type: Implementation
  strategy: 於 macos/Runner/CursorLocatorEffectWindow.swift 定義螢幕判定函式，簽章接受 screens 陣列與游標座標兩個參數而非讀取
    NSScreen 全域狀態，使 XCTest 可注入定樁螢幕排列；判定規則依 SPEC-008 FR-02 為 frame 涵蓋座標的第一個螢幕，無涵蓋者退回
    NSScreen.main 並記錄 E_CL_SCREEN；同時將 macos/RunnerTests 由 scaffold 空殼接為可實際執行的 XCTest
    target，確認 xcodebuild 執行路徑可用並記錄指令
acceptance:
- '[x] 螢幕判定實作為可注入 screens 陣列與游標座標的純函式，不於函式內讀取 NSScreen 全域狀態'
- '[x] 游標座標落於某螢幕 frame 內時回傳該螢幕；跨越多螢幕邊界時回傳 screens 順序中第一個涵蓋者（SPEC-008 FR-02）'
- '[x] 游標座標不落於任何螢幕 frame（螢幕排列間隙）時回傳退回 main 的判定結果（EX-06-02 復原路徑）。NSLog 記錄 E_CL_SCREEN
  屬控制器層責任，已移至 1.4.0-W2-006.2 acceptance 6（PM 2026-08-01 修訂，理由見 Solution）'
- '[x] XCTest 涵蓋上述三種情形並可經 xcodebuild 實際執行通過，執行指令與通過數記錄於 Test Results'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-08-01T09:51:56'
completed_at: null
created: '2026-07-31'
updated: '2026-07-31'
protocol_version: '2.0'
current_phase: Phase 3b
assignee: parsley-flutter-developer
tdd_phase_source: manual
---

# Execution Log

## Task Summary

實作 特效層目標螢幕判定純函式與 XCTest 基礎設施

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 派發前 dispatch-readiness 覆核（PM，2026-08-01）

`ticket track dispatch-readiness 1.4.0-W2-006.1` 回報閾值 1 WARN（acceptance 條目 4 > 2），閾值 2、3 PASS。CLI 以 acceptance 條目數近似功能職責數，含驗證類條目時會高估，故依規定手動覆核。

覆核結果：**CLI 高估，不需再拆**。四條 acceptance 對應的實際職責數為 2，恰在閾值邊界內：

| acceptance | 歸屬職責 |
|-----------|---------|
| 1 純函式簽章可注入 screens 與座標 | 職責 A：螢幕判定函式 |
| 2 回傳 screens 順序中第一個涵蓋者 | 職責 A（同一函式的行為分支） |
| 3 間隙退回 main 並記錄 E_CL_SCREEN | 職責 A（同一函式的行為分支） |
| 4 XCTest 可經 xcodebuild 實際執行通過 | 職責 B：XCTest target 接線 |

acceptance 1-3 是同一個判定函式的三種輸入情形，非三個獨立功能；acceptance 4 屬驗證類條目，對應的是基礎設施接線。母票 Phase 3a §三已將兩者編為單一七步序列，其中步驟 1-3 為基礎設施、步驟 4 起才有紅綠訊號——拆分反而會切斷「先打通執行路徑再擴充案例」的順序依賴。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

實作內容：

1. 新增 `macos/Runner/CursorLocatorEffectWindow.swift`（app target only）：
   - `CursorScreenSnapshot`（含 `Entry: frame + displayID`、`mainIndex`）值型別
   - `CursorScreenResolution` 三態結果 enum（`.matched` / `.fellBackToMain` / `.unavailable`）
   - `CursorScreenLocator.resolve(cursorLocation:in:)` 純靜態函式，簽章不含 NSScreen /
     NSEvent，涵蓋判定直接使用 `NSRect.contains(_:)` 半開區間語意，未自行以四則運算重寫。

2. 新增 `macos/RunnerTests/CursorScreenLocatorTests.swift`（RunnerTests target only）：
   T1.A-01 ~ A-15，共 15 個案例，逐一對應母票 Phase 2 §四群組 A 的場景 1、2、3、3b、3c、3d。

3. 新增 `macos/RunnerTests/Support/CursorLocatorTestDoubles.swift`（RunnerTests target only）：
   `ScreenSnapshotFixtures` 八組固定排列常數（sideBySide / withGap / secondaryLeft /
   secondaryBelow / secondaryAbove / overlapping / empty / singleMain）。

範圍決策（與 Phase 3a §三步驟 5 字面的差異，記錄理由）：
Phase 3a 步驟 5 列出「spy surface、spy 工廠、手動 driver、手動 deadline scheduler」亦屬本子票
交付，但這些型別依賴 `CursorLocatorSurface` / `CursorLocatorFrameDriving` /
`CursorLocatorDeadlineScheduling` 三個協定與函式型別——這些依 Phase 1 §2.4 與 Phase 3a §四
步驟 1，屬子票 .2「定義注入介面與請求／狀態值型別」的交付範圍。本子票 Context Bundle 明確列
「本子票不做的事：不建立特效視窗控制器」，若提前宣告上述協定會實質等同建立控制器的依賴介面，
逾越 .1 範圍。故本子票僅交付不依賴這些協定的 `ScreenSnapshotFixtures`，其餘測試替身留待 .2
連同協定宣告一併建立。此決策已記錄，若 PM 判斷應由 .1 補齊協定宣告本身（不含控制器邏輯），
可回饋調整 .2 的 Context Bundle。

4. `macos/Runner.xcodeproj/project.pbxproj`：新增 4 個檔案（1 個 app target、3 個 RunnerTests
   target），各自僅屬單一 target membership（PBXBuildFile + PBXFileReference + Group children +
   對應 target 的 PBXSourcesBuildPhase），未觸及既有條目。手改一次成功，未觸發阻礙 B-4。

常數落點：Fixture 座標與 displayID 為測試資料，依 CLAUDE.md §6 例外條款（測試檔案 fixture
值）不集中管理。CursorScreenResolution / CursorScreenSnapshot 為型別本身無魔法數字。

技術債務（quality-baseline 規則 5，已於 Test Results 一併回報，未新建 ticket——優先級與是否建票
由 PM 依母票 Phase 3a §六已列出的 D-4/D-5/D-7 裁定，本子票未發現新增項）。
acceptance 3 用詞澄清（NSLog 責任歸屬）：

acceptance 3 字面為「退回 NSScreen.main 並以 NSLog 記錄 E_CL_SCREEN」，但母票 Phase 1 §2.2
明定 `CursorScreenLocator.resolve` 為「純函式：不讀取任何全域狀態」，且 Phase 1 §2.8「每幀行為」
明定 NSLog 記錄發生在控制器層（`.fellBackToMain` 時「另記 E_CL_SCREEN log，不中斷播放」），屬
子票 .2 範圍；母票 Phase 2 T1.A-02 案例設計本身也只斷言 `== .fellBackToMain(index: 1)`，未斷言
任何 NSLog 輸出，與此一致。

本子票已完整交付「退回 main」的判定行為本身（.fellBackToMain enum case，T1.A-02 測試覆蓋），
但字面的「NSLog 記錄」屬呼叫端（子票 .2 控制器）在拿到 `.fellBackToMain` 結果後才執行的動作，
本子票的純函式邊界內不執行任何 I/O。此為遵循 Phase 1 純函式架構的必然結果，非遺漏。

acceptance 3 因此標記為**部分達成**：退回 main 的判定邏輯已達成並測試；NSLog 記錄動作留待
子票 .2（該票驗收對應表已將 EX-06-02 的 NSLog 面歸屬 .2，見母票 Phase 2 §七）。建議 PM 於 .2
驗收時一併確認此字面完整達成，或修訂本子票 acceptance 3 措辭以符合純函式架構。
### PM 驗收與 acceptance 3 措辭修訂（2026-08-01）

**驗收通過。** 獨立查證 `macos/Runner/CursorLocatorEffectWindow.swift`：`resolve()` 不接觸 `NSScreen` / `NSEvent`，輸出只由兩個參數決定；涵蓋判定使用 `NSRect.contains(_:)` 而非自行重寫比較式（Phase 3a 明令）；三態結果以 enum 承載使呼叫端無須比對回傳值即可分流；座標系註解方向正確（下方為負 y，非上方）。

#### acceptance 3 修訂

原措辭「退回 NSScreen.main **並以 NSLog 記錄 E_CL_SCREEN**」把兩個層次的責任綁在同一條：

| 責任 | 歸屬 | 理由 |
|------|------|------|
| 回傳退回 main 的判定結果 | 本票 | 屬純資料運算 |
| NSLog 記錄 E_CL_SCREEN | `1.4.0-W2-006.2` | 純函式不執行 I/O。若在 `resolve()` 內呼叫 NSLog，該函式即不再是純函式，且 15 個 XCTest 案例每跑一次都產生 Console 副作用——這與 acceptance 1「不讀取全域狀態」是同一原則的兩面 |

執行者的判斷正確，非未完成。已修訂本票 acceptance 3 措辭並勾選。

**日誌責任已實際接住，非消失**：`1.4.0-W2-006.2` 新增 acceptance 6，涵蓋母票 Problem Analysis 定義的 E_CL_SCREEN 兩條路徑（退回 main 僅記 NSLog 且播放照常、無可用螢幕以 FlutterError 回傳）。原 `.2` 的 acceptance 只涵蓋 E_CL_WINDOW，若僅從本票移除而不新增，會形成無人負責的缺口。

#### 範圍決策確認

執行者僅交付 `ScreenSnapshotFixtures`，其餘測試替身（spy surface / 工廠 / 手動 driver / scheduler）留待 `.2`。理由成立：那些型別依賴 `CursorLocatorSurface` 等協定，而協定定義屬 `.2` 步驟 1 的交付。Phase 3a §三步驟 5 把替身全部歸 `.1` 是排程上的樂觀，實際受依賴方向約束。此調整不影響 `.2` 的工作量估計，因協定與其替身本就相鄰。

#### scaffold testExample() 處置

保留為 smoke case，理由（獨立驗證測試基礎設施健在）成立。

#### 收尾方式

執行者無法自行 `ticket track complete`（PC-SCLK-002 / PC-SCLK-004 已知限制：worktree 隔離代理人的 sandbox guard 阻擋含 complete token 的命令），依文件指引未嘗試繞過，由 PM 代跑。

#### Phase 4 豁免（PM 裁定）

本子票不執行 Phase 4 重構評估，由母票 `1.4.0-W2-006` 承接。依母票 Solution「TDD 階段規劃與子票定位」表，本 group 採 tdd-flow 的 3b 拆分機制：Phase 1 / 1.5 / 2 / 3a / 4 於母票各執行一次，僅 Phase 3b 實作拆為子票。子票各自跑 Phase 4 會對同一份設計重複評估，屬該機制明確要避免的重工。

此為 quality-baseline 規則 2 的合規路徑而非跳過——Phase 4 仍會執行，只是執行點在母票且涵蓋兩張子票的合併產出，屆時可一併評估 `.1` 的判定層與 `.2` 的控制器層之間是否有跨層重構機會，視野優於分別評估。母票 `tdd_phase` 現為 `phase3b`，待 `.2` 完成後推進至 `phase4`。

### 自檢結果（Layer 1）

**執行時機**: complete 前
**掃描類別**: A（文字品質）/ B（禁用字）/ C（Schema 結構）

| 類別 | 發現項目 | 處理方式 |
|------|---------|---------|
| A1 | 已寫入段落逐一檢查三明示：範圍決策段（Why：協定依賴方向 / Consequence：逾越 .1 範圍 / Action：留待 .2）、acceptance 3 澄清段（Why：純函式架構 / Consequence：NSLog 責任錯置 / Action：部分達成並標記）皆齊備 | 通過，無需調整 |
| B1 | 掃描「數據、代碼、默認、文檔、軟件、硬件、信息、視頻」：Solution / Test Results 全文零命中 | 通過，無需調整 |
| B2 | 掃描 emoji（U+1F000 以上裝飾字元）：全文零命中 | 通過，無需調整 |
| B3 | 掃描簡體字/日文漢字混淆字元（这/该/们/为/对、発/処 等）：全文零命中 | 通過，無需調整 |
| C1 | Problem Analysis / Solution / Test Results 三章節皆有實質內容，無殘留佔位符 `<!-- To be filled -->` 或「待填寫：...」 | 通過，無需調整 |
| C2 | 檢查 ticket body 是否出現非 Schema H2：目前僅使用 `Task Summary` / `Problem Analysis` / `Solution` / `Test Results` / `Completion Info`，皆為 Schema 合法 H2；先前遺漏的 `### 自檢結果` 已改以 H3 掛於 `## Solution` 之下，非新增 H2 | 已修正（本次新增即依此規則掛為 H3 子章節） |
| C3 | Test Results 已含測試執行指令（xcodebuild test 完整指令）與通過數（15/15、16/16） | 通過，無需調整 |

**自檢結論**: 發現 1 項（C2 提醒，本次寫入時已依規範正確掛為 H3），其餘 6 項掃描皆通過，無需修正。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
接線序 1（scaffold baseline，未加任何新檔）：
  xcodebuild test -workspace macos/Runner.xcworkspace -scheme Runner -configuration Debug \
    -destination 'platform=macOS' -only-testing:RunnerTests/RunnerTests
  結果：TEST SUCCEEDED，RunnerTests.testExample() 通過（1/1）。

接線序 2（加入 app target 判定型別檔後）：
  同上指令，重跑 RunnerTests/RunnerTests
  結果：TEST SUCCEEDED，仍 1/1 通過（證明新檔未破壞建置）。

接線序 3（單一 smoke 案例，Phase 3a 步驟 4 要求）：
  xcodebuild test -workspace macos/Runner.xcworkspace -scheme Runner -configuration Debug \
    -destination 'platform=macOS' -only-testing:RunnerTests/CursorScreenLocatorTests
  結果：TEST SUCCEEDED，testResolve_cursorInsideSecondScreen_returnsMatchedAtIndexOne() 通過（1/1）。
  執行路徑就緒確認：@testable import screen_clock 正確解析、存取層級無問題、模組名以
  PRODUCT_NAME=screen_clock 為準（非 target 名 Runner）。

接線序 4（擴充至 15 案例，Phase 3a 步驟 6）：
  同上指令（-only-testing:RunnerTests/CursorScreenLocatorTests）
  結果：TEST SUCCEEDED，15/15 全數通過：
    testResolve_calledRepeatedly_isIdempotent (T1.A-15)
    testResolve_cursorAtMaxY_isExcludedAndFallsBackToMain (T1.A-05)
    testResolve_cursorAtMinXMinY_matchesFirstScreen (T1.A-06)
    testResolve_cursorAtSharedBoundary_matchesExactlyOneScreen (T1.A-04)
    testResolve_cursorAtSharedBoundary_matchesRightScreen (T1.A-03)
    testResolve_cursorInGap_fallsBackToMainIndex (T1.A-02)
    testResolve_cursorInsideSecondScreen_returnsMatchedAtIndexOne (T1.A-01)
    testResolve_emptyEntriesAndNilMain_returnsUnavailable (T1.A-11)
    testResolve_negativeMinXBoundary_isInclusive (T1.A-10)
    testResolve_noMainIndexWithEntriesPresent_returnsUnavailable (T1.A-12)
    testResolve_overlappingScreens_matchesSmallestIndex (T1.A-13)
    testResolve_overlappingScreensReordered_matchesFirstInArrayOrder (T1.A-14)
    testResolve_secondaryScreenAtNegativeX_matchesCorrectly (T1.A-07)
    testResolve_secondaryScreenAtNegativeY_matchesCorrectly (T1.A-08)
    testResolve_secondaryScreenAtPositiveY_matchesCorrectly (T1.A-09)

全 RunnerTests target（含 scaffold）：
  xcodebuild test -workspace macos/Runner.xcworkspace -scheme Runner -configuration Debug \
    -destination 'platform=macOS' -only-testing:RunnerTests
  結果：TEST SUCCEEDED，16/16 全數通過（15 個 T1.A + RunnerTests.testExample）。

flutter test（B-9 提醒：不涵蓋 Swift target，本票未修改任何 .dart 檔，未重跑）：N/A，本票無
Dart 側變更。

scaffold testExample() 處置（Phase 3a 步驟 7 二擇一）：保留為 smoke case。理由：該案例本身
無害、持續證明「最基本的 test host 啟動路徑可用」，與新增的 15 個判定案例職責不重疊，刪除不會
帶來額外收益；若日後 group A 因故整組被排除執行，此案例仍可獨立提示測試基礎設施本身是否健在。

9 項已知阻礙實際遇到情形：
  B-1（workspace 非 project）：命令固定使用 -workspace，未遇到。
  B-2（僅 Debug 可 @testable）：命令固定 -configuration Debug，未遇到。
  B-3（模組名含底線）：以 PRODUCT_NAME=screen_clock 匯入，一次成功，未遇到。
  B-4（project.pbxproj 手改）：4 次新增（1 app target 檔 + 3 RunnerTests target 檔）皆一次
    成功，每次以 xcodebuild -list 驗證專案可解析，未觸發連續兩次失敗的停手條件。
  B-5（access control）：受測型別維持 internal 預設層級，未提升為 public，未遇到問題。
  B-6（宿主簽章）：本子票（純函式測試，非 app-hosted 屬性斷言）未啟動宿主 app 相關流程，
    N/A（Runner target 建置本身順利簽章，未見 Gatekeeper 阻擋）。
  B-7（權限彈窗阻塞）：屬子票 .2 F 群組（app-hosted）風險，本子票 group A 為純函式測試不涉及
    宿主啟動流程，未觸發，N/A。
  B-8（平行執行）：本子票僅 15 個案例皆為純函式、無共享可變狀態，執行過程未觀察到平行造成的
    互擾，維持預設 parallelizable = YES，未關閉。
  B-9（兩條測試命令）：已依規範分別記錄 flutter test（N/A，無 Dart 變更）與 xcodebuild test
    （見上）。

## Context Bundle

### 權威來源：母票 1.4.0-W2-006

本子票只執行 TDD Phase 3b 實作。Phase 1 介面契約、Phase 2 測試設計、Phase 3a 實作策略**全部位於母票 `1.4.0-W2-006` 的 Solution 章節**，執行前必須完整讀取：

```
ticket track full 1.4.0-W2-006
```

| 需要的內容 | 母票章節 |
|-----------|---------|
| 螢幕幾何快照值型別定義、判定函式簽章、座標系契約 | 「TDD Phase 1 功能設計」§2.1、§2.6 + 「Phase 1 修訂指示」次要修訂 |
| 本子票的 15 個 XCTest 案例（T1.A-01 ~ A-15） | 「TDD Phase 2 測試設計」§四 |
| 測試替身設計（spy surface / 工廠 / 手動 driver / 共用 log） | 「TDD Phase 2 測試設計」§二 + 「Phase 3a」§二 |
| XCTest target 接線 5 步與 9 項已知阻礙 | 「TDD Phase 2 測試設計」§六 |
| **本子票的七步實作序列** | 「TDD Phase 3a 實作策略規劃」§三 |
| 接線四段執行順序與各阻礙的升級路徑 | 「TDD Phase 3a 實作策略規劃」§五 |

### Phase 3a 對本子票的關鍵約束

以下三點在母票 §三有完整說明，此處摘要以免遺漏：

1. **步驟 4 必須先跑單一 smoke 案例**：只寫一個必然通過的案例並實際執行，確認命令輸出「1 個案例通過」才算執行路徑就緒。理由是 15 案例一次寫完再跑，首次失敗時無法分辨是接線問題還是判定邏輯問題。
2. **涵蓋判定直接用平台矩形型別的「包含點」語意**（`minX <= x < maxX`、`minY <= y < maxY`），不自行以四則運算重寫比較式——自行重寫是邊界歸屬出錯的主要來源。
3. **判定函式簽章不得出現任何平台螢幕型別或游標取樣來源**，取樣責任在呼叫端。

### 座標系提醒（PC-SCLK-006 已在本 ticket 二度發生）

AppKit 全域座標原點在主螢幕左下角、**y 軸向上為正**。第二螢幕置於主螢幕左方時 x 為負、置於**下方**時 y 為負；置於上方時 y 為正。母票 Phase 2 的 fixture 已依此更正，實作時勿依直覺推斷。

### 本子票不做的事

不建立特效視窗控制器、不碰 `MainFlutterWindow.swift`、不建立錯誤對應檔（`CursorLocatorErrorMapping.swift` 屬子票 `.2`）。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
commit: 8b407fc5fffc4ca2d6fd53adfa5d441c1d950812
branch: worktree-agent-a2b0bc18a7e9d58f0

代理人無法自行執行 `ticket track complete`：worktree 隔離代理人的 Claude Code
runtime sandbox 對含子命令字面 `complete` 的命令一律阻擋（PC-SCLK-002 /
PC-SCLK-004 已記錄的已知限制，非本票新發現）。已依該文件指引不嘗試繞過，
改為完整記錄 Problem Analysis / Solution / Test Results 並勾選 acceptance
（1、2、4；3 部分達成，理由見 Solution），交由 PM 代跑
`ticket track complete 1.4.0-W2-006.1 --as parsley-flutter-developer`。
