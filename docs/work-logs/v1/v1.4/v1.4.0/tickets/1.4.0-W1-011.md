---
id: 1.4.0-W1-011
title: 評估 schemaVersion 不參與解析所導致的相容性風險
type: ANA
status: pending
version: 1.4.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.4.0-W1-007
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層:設計問題
  final_decision: 升 schema 4 前先評估版本解析機制，不直接沿用現有模式
  rationale: quality-baseline 規則 3 設計問題立即修正；SPEC-009 已標註 INV-03 為最脆弱且無機械強制訊號的一條
who:
  current: sassafras-data-administrator
  history: {}
what: 評估 schemaVersion 不參與解析所導致的相容性風險
when: 1.4.0-W1-007 建立 data-contract 時發現，須於 W1-001 升 schema 4 之前完成評估
where:
  layer: Domain
  files:
  - lib/models/settings_model.dart
  - lib/services/settings_service.dart
  - docs/spec/data-management/SPEC-009-settings-storage-data-contract.md
why: SPEC-009 撰寫過程發現 fromJson() 從不讀取 schemaVersion，相容性完全依賴「欄位缺失取預設值」；此設計無法對同名欄位語意變更做版本分支，而
  1.4.0-W1-001 即將升 schema 至 4
how:
  task_type: Implementation
  strategy: 重現：讀 lib/models/settings_model.dart 的 fromJson 確認 schemaVersion 未被讀取，並以測試驗證舊版
    JSON 反序列化行為；評估三種情境的實際後果（欄位型別變更、同名欄位語意變更、欄位移除）；比對 SPEC-009 的 INV-03（只加欄不改型別）是否足以保護；提出方案（維持現狀並強化契約約束
    / 導入版本分支解析 / 其他）並評估對 W1-001 的影響；另評估 bindingsSeeded 同版擴充（commit 10284df 加欄未提版本）是否應追認為
    schema 版本
acceptance:
- '[ ] 重現實驗結果章節記錄 fromJson 未讀 schemaVersion 的程式碼證據與測試輸出'
- '[ ] 三種變更情境的後果評估完成（型別變更 / 語意變更 / 欄位移除）'
- '[ ] 提出方案並含對 1.4.0-W1-001 的影響判定'
- '[ ] bindingsSeeded 同版擴充的處理決議明確（追認版本或維持現狀並記錄理由）'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-07-29'
updated: '2026-07-29'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

評估 schemaVersion 不參與解析所導致的相容性風險

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（必填：問題發生的直接原因是什麼？影響範圍有多大？）

### 影響範圍

（必填：哪些檔案、模組或功能受影響？已驗證的事實 vs 仍未驗證的假設？）

### 相關 Error Pattern

（必填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: sassafras-data-administrator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.4.0-W1-007 | chars: 55 -->

### Task Reference
- 1.4.0-W1-007 what: 建立 使用者設定持久化的 data-contract 文件

### Related Files
- docs/spec/data-management/  # from 1.4.0-W1-007
### 任務性質

ANA ticket。產出是評估結論與方案建議，不修改任何程式碼。1.4.0-W1-001 blockedBy 本票，結論將決定 schema 4 的實作方式。

### 規範依據

- .claude/methodologies/data-layer-contract-methodology.md — 三層保護模型
- docs/spec/data-management/SPEC-009-settings-storage-data-contract.md — 1.4.0-W1-007 產出的資料契約，A.1 演進表與 A.3 相容性條款是本票的分析起點
- .claude/pm-rules/ticket-body-schema.md — ANA type 必填「重現實驗結果」章節（PC-063）

### 發現來源與已知事實

1.4.0-W1-007 撰寫 SPEC-009 時，由實際讀取 lib/models/settings_model.dart 得出四項事實（本票須獨立重新驗證，不可直接引用，PC-162）：

| 事實 | 待驗證點 |
|------|---------|
| fromJson() 從不讀取 schemaVersion | 讀 lib/models/settings_model.dart 的 fromJson 全文確認 |
| 相容性靠「欄位缺失取預設值」 | 確認每個欄位的缺失處理路徑 |
| bindingsSeeded 加欄未提版本（commit 10284df） | git show 該 commit 確認 |
| schemaVersion 演進：v1 bd83aea 7 欄 → v2 afa3d67 加 lifeTimerMode/birthDate → v3 d05724a 加 bindings | git log 逐 commit 比對 toJson 欄位集 |

SPEC-009 已將 INV-03（只加欄、不改型別）標為「本契約最脆弱的一條，無機械強制訊號」。

### 分析三情境（核心工作）

對以下三種變更，各自回答「現行機制下會發生什麼」與「使用者資料會怎樣」：

| 情境 | 待答問題 |
|------|---------|
| 欄位型別變更（如 double 改 int） | 舊資料反序列化時的實際行為？拋例外、靜默取預設、還是型別錯誤？寫測試驗證 |
| 同名欄位語意變更（如秒改毫秒） | 現行機制能否偵測？後果是資料被誤讀還是崩潰？ |
| 欄位移除 | 舊資料的多餘欄位如何處理？新版讀舊資料、舊版讀新資料兩個方向皆須驗證 |

三情境須以實際測試驗證，不可純推理。測試可寫在 test/ 下臨時檔案驗證後刪除，或用 dart run 直接執行；實際命令與輸出寫入「重現實驗結果」章節。

### 方案評估（至少三案）

| 候選 | 說明 |
|------|------|
| A 維持現狀 + 強化契約約束 | 靠 SPEC-009 的 INV-03 文字約束，不改 code。須回答：無機械訊號時如何防止未來違反？ |
| B 導入版本分支解析 | fromJson 讀 schemaVersion 並依版本走不同解析路徑。須評估既有三版資料的相容性與實作成本 |
| C 其他 | 如 migration 函式鏈、欄位級版本標記等 |

每案須評估：對 1.4.0-W1-001（schema 4 升級）的影響、對既有使用者資料的風險、實作範圍。

### bindingsSeeded 同版擴充的決議

commit 10284df 在不提升 schemaVersion 的情況下加欄，使 v3 存在兩種資料形態，且其 migration 觸發條件是旗標值而非版本號。須決議：追認為獨立 schema 版本、維持現狀並在契約標註、或其他處理。決議理由寫入 Solution。

### 執行約束

- 不修改 lib/ 下任何程式碼（本票是評估，非實作）
- 不修改 SPEC-009；若分析結論要求修改契約，另建 ticket 或於 Solution 標註由 W1-001 承接
- 結論須含對 W1-001 的明確影響判定（可照原計畫 / 須調整實作方式 / 須先做前置修改）
- 若建議方案涉及改 code，於 Solution 列出 spawn 規劃並依 quality-baseline 規則 5 建對應 ticket

### 驗收驗證

| 驗收條件 | 驗證方式 |
|---------|---------|
| fromJson 未讀 schemaVersion 的證據 | 重現實驗結果章節含程式碼片段與行號 |
| 三情境後果評估 | 每情境含實際測試命令與輸出 |
| 方案與 W1-001 影響判定 | Solution 章節含方案比較表與明確判定 |
| bindingsSeeded 決議 | Solution 章節含決議與理由 |

### Commit Policy

單一 commit，訊息格式：docs(1.4.0-W1-011): schemaVersion 相容性風險評估

若分析過程建立臨時測試檔，commit 前刪除；測試輸出保留在 ticket body。
