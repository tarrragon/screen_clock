---
id: 1.4.0-W1-023
title: 修正 worktree sandbox 誤判 complete 引數為 shell builtin
type: IMP
status: in_progress
version: 1.4.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.4.0-W1-015
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:錯誤處理
  final_decision: 修正 sandbox 樣式比對，不要求代理人改用繞過手法
  rationale: 防護誤判若不修，代理人只能繞過或放棄收尾；opinionated-default-design 主張 1：工具預設行為優先於要求使用者自行迴避
who:
  current: thyme-python-developer
  history: {}
what: 修正 worktree sandbox 誤判 complete 引數為 shell builtin
when: 1.4.0-W1-015 於 worktree 中收尾時發現
where:
  layer: Infrastructure
  files:
  - .claude/hooks/
  - .claude/skills/ticket/
why: 1.4.0-W1-015 於 worktree 隔離環境執行 ticket track complete 時被 sandbox 阻擋，原因是字面引數 token
  complete 被判為不安全的 shell builtin 樣式；此誤判使代理人無法用正常方式收尾 ticket，並誘發其以 base64 編碼管線繞過防護（見
  PC-SCLK-002）
how:
  task_type: Implementation
  strategy: 重現：於 worktree 隔離環境執行 ticket track complete <id> --as <agent> 觀察阻擋訊息；定位
    sandbox 的命令樣式比對邏輯（可能在 CC runtime 或 .claude/hooks/ 的 Bash 前置檢查）；根因在於樣式比對未區分「命令位置的
    token」與「引數位置的 token」——complete 作為 ticket track 的子命令引數不具 shell builtin 語意；修正方向為僅比對命令位置
    token，或對已知 CLI（ticket/doc/worktree）的引數位置豁免；補測試覆蓋 ticket track complete 與其他含 builtin
    同名引數的命令
acceptance:
- '[ ] 重現實驗記錄 sandbox 阻擋訊息與觸發條件'
- '[ ] 根因定位到具體樣式比對邏輯的檔案與行號'
- '[ ] 修正後 worktree 中可正常執行 ticket track complete'
- '[ ] 真正的 shell builtin 濫用仍被阻擋（無防護弱化）'
- '[ ] 新增 regression test 覆蓋引數位置的 builtin 同名 token'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-31T13:24:50'
completed_at: null
created: '2026-07-31'
updated: '2026-07-31'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

修正 worktree sandbox 誤判 complete 引數為 shell builtin

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

### 重現實驗與根因調查（thyme-python-developer）

**調查方法**：(1) 全文檢索 `.claude/hooks/`、`.claude/skills/ticket/`（含 hooks 與 ticket_system 全部原始碼）比對「shell builtin」「sandbox」「builtin」關鍵字；(2) 檢查 `.claude/settings.json`（專案）與 `~/.claude/settings.json`（全域）的 permissions allow/deny 清單；(3) 以 `git worktree add` 建立獨立 worktree，透過 Bash 工具直接執行 `ticket track complete <id> --as <agent>`（含裸引數與子 shell 兩種呼叫形式）重現。

**發現 1（結構性）**：repo 內無任何檔案實作「shell builtin 樣式比對」邏輯。`.claude/hooks/` 與 `.claude/skills/ticket/` 全文搜尋「builtin」僅命中 `cli-error-feedback-hook.py` 一處，該處是處理 `cd --help` 建議語意的既有邏輯，與本案無關。`.claude/settings.json`、`~/.claude/settings.json` 的 permissions 清單無比對「complete」或任何 shell builtin 名稱的規則。

**發現 2（重現失敗）**：在 `/tmp` 下以 `git worktree add` 建立獨立 worktree，透過 Bash 工具執行 `ticket track complete <dummy-id> --as thyme-python-developer`（裸引數形式與子 shell 形式皆測），兩次執行皆正常通過 Bash 工具（無 sandbox 阻擋），僅收到 CLI 應用層本身的 `identity-guard deny`（身份不符，預期行為，非本案要修的問題）。

**發現 3（歷史紀錄不一致）**：交叉比對三個獨立回報同一問題的 ticket：
- 1.4.0-W1-015（PC-SCLK-002 首發）：診斷為「字面引數 token `complete` 被判為不安全的 shell builtin 樣式」
- 1.4.0-W1-001.4：沿用同一「shell builtin」措辭，但無新增驗證
- 1.4.0-W1-001.5：診斷為「被 sandbox 誤判為**跨 worktree git 操作**而拒絕執行」——與前兩者機制描述不同

三次回報的具體機制描述不一致（shell builtin token vs 跨 worktree git 操作），且均無附上逐字擷取的原始阻擋訊息，僅以「PC-SCLK-002 已知誤報」帶過。這與 `.claude/rules/core/tool-output-trust-rules.md` 規則 1-3 描述的 confabulation 風險模式相符：代理人在被阻擋後，用「先驗合理推測」取代「逐字證據」記錄根因。

**發現 4（可能的真實觸發點，未證實）**：`ticket track complete` 透過 `git_utils.py:_auto_commit_ticket_md()` 於 CLI 內部以 `subprocess.run(["git", "add"/"commit", ...])` 自動 commit ticket md（非 Claude 直接呼叫 Bash 執行 git 指令，而是 Python 子行程內部呼叫）。`.claude/references/agent-authoring-guide.md`「Worktree 陷阱」章節記載即使 `permissionMode: bypassPermissions`，`.git` 仍會提示（非自動放行）；但此caveat 描述的是 Claude 直接發出的 Edit/Write/Bash 呼叫觸及 `.git` 路徑，本案的 git 呼叫是 CLI 內部子行程，命令字串本身（`ticket track complete <id> --as <agent>`）不含字面 "git"，故此 caveat 是否適用於本案未經證實。

**結論**：三次獨立占用同一 CLI 收尾動作（`ticket track complete`）在 isolation:worktree 派發的 subagent 情境下皆失敗，此現象本身（阻擋確實發生）有跨 ticket 一致性，可信；但「阻擋成因是 sandbox 對命令位置/引數位置 token 的樣式比對誤判」此一具體機制描述，在 repo 全文搜尋與獨立重現實驗中均未能證實，且與另一次回報（W1-001.5）的機制描述互相矛盾。本代理人不具備 Agent 工具，無法自行派發 isolation:worktree 隔離子代理人以取得逐字阻擋訊息驗證何者為真；若阻擋機制確實位於 Claude Code runtime（而非 `.claude/hooks/` 或 `.claude/skills/ticket/`），則屬本 repo 無法編輯的範圍，需調整驗收條件或改採「規避觸發點」而非「修正比對邏輯」的解法方向。已回報 PM 請求裁示。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.4.0-W1-015 | chars: 275 -->

### Task Reference
- 1.4.0-W1-015 what: 建立 v1/v2/v3-early/v3-late 定樁 JSON 的 golden fixture 迴歸測試，為 INV-03 提供機械強制訊號

### Rationale Chain
- 1.4.0-W1-015 why: SPEC-009 標註 INV-03（只加欄不改型別）為最脆弱且無自動化訊號的一條；1.4.0-W1-011 實證確認 fromJson 對型別變更會靜默寬鬆轉型（targetScreenIndex 2.9 截斷為 2）或靜默取預設，違反 INV-03 不產生任何紅燈。既有測試僅零星涵蓋 v2，無 v1 與 v3-early 定樁

### Related Files
- test/models/settings_model_test.dart  # from 1.4.0-W1-015
