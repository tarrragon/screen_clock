---
id: 1.4.0-W1-001.5
title: 修正 play() 簽章對齊 SPEC + NaN 未捕捉例外 + Color 轉換收斂
type: IMP
status: pending
version: 1.4.0
wave: 1
priority: P1
parent_id: 1.4.0-W1-001
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: parsley-flutter-developer
  history: {}
what: 修正 play() 簽章對齊 SPEC + NaN 未捕捉例外 + Color 轉換收斂
when: Phase 4 評估後、1.4.0-W2-006 接上呼叫端之前（呼叫端出現後 A 項改動成本顯著上升）
where:
  layer: Domain
  files:
  - lib/platform/cursor_locator.dart
  - lib/models/settings_model.dart
  - test/platform/cursor_locator_test.dart
  - docs/spec/user-experience/SPEC-008-cursor-locator.md
why: SPEC-008 FR-06 要求三項設定（啟用開關 / 特效時長 / 主色調）持久化並將 schemaVersion 由 3 升為 4；FR-01 與特效播放需經
  method channel 橋接。常數、資料模型與 channel 契約為 W2 特效層與 W4 面板的共同前置，須先定案以免介面反覆
how:
  task_type: Implementation
  strategy: 四項合併為一次重構：(A) play() 簽章由 double durationSeconds 改回 SPEC-008:277 訂的 Duration
    duration，_durationSecondsToMs 移除，改用 duration.inMilliseconds，秒的概念不再進入 platform
    層；(B) _tintToArgb 呼叫移入 try 區塊，使既有 catch-log 路徑能涵蓋轉換期例外；(G) SettingsModel._colorToInt
    手刻位移改為 color.toARGB32()，與 cursor_locator 收斂至同一實作；(J) SPEC-008:275 的 abstract class
    宣告改為 concrete class，與實作對齊（建構子注入已達可測試性，不需為單一實作開抽象層）
acceptance:
- '[ ] play() 簽章改為 Duration duration（對齊 SPEC-008:277），_durationSecondsToMs 移除，內部改用
  duration.inMilliseconds'
- '[ ] cursor_locator.dart 內不再出現秒為單位的參數或區域變數（單位洩漏消除），類別 doc 的六行單位換算註解隨之刪減至必要範圍'
- '[ ] 型別轉換呼叫位於 try 區塊內，NaN / Infinite 等異常輸入走既有 catch-log 路徑而非未捕捉例外；補一筆對應測試'
- '[ ] SettingsModel._colorToInt 改用 color.toARGB32()，手刻位移實作移除；既有 round-trip 測試維持綠燈驗證等價性'
- '[ ] SPEC-008 的 CursorLocator 宣告更新為 concrete class 且簽章與實作一致，於 Solution 記錄改 SPEC
  而非改 code 的理由'
- '[ ] flutter test 全套件通過（基線 146）、flutter analyze 無警告'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-07-31'
updated: '2026-07-31'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修正 play() 簽章對齊 SPEC + NaN 未捕捉例外 + Color 轉換收斂

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（選填：問題如何發現？直接原因是什麼？）

### 影響範圍

（選填：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（選填：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

（選填：修復方式概述、修改的檔案和驗證方式）

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

（必填：測試執行指令、通過數/失敗數或 commit SHA）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: parsley-flutter-developer
**Review Status**: pending
